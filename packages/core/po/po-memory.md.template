# PO memory for <user>

> Installed by `packages/core/scripts/install.sh` on first run. PO appends to this over time.
> It's your (user's) track record of working with PO — not a summary of projects.

## Communication preferences
<!-- e.g. "prefers short bullet summaries", "wants push-back when scope seems too big" -->

## Product taste
<!-- Positive-feedback log. PO appends one line on Step 3 step 14b.
     Format: - (YYYY-MM-DD) <area-tag>: <what worked> · "<user phrase verbatim, any lang>"
     Step 1 reads recent N lines to bias routing toward validated patterns.
     See ~/.productune/sections/memory.md §"Product taste" for full doctrine. -->

## Workflow preferences
<!-- e.g. "always commits own PRs, doesn't want auto-commit", "runs build before every QA" -->

## Recent corrections / to-avoid
<!-- e.g. "(2026-04-23) asked to not over-engineer with Basic Memory when Claude native memory sufficed" -->

## Model/Effort Calibration
<!-- Effort learning loop. PO appends one line per task close.
     Format: - (YYYY-MM-DD) <slug> · <complexity_class> · estimate=<model>/<effort> → actual=<model>/<effort> · QA <pass|fail>(<loops>) · rework=<y|n> · escalation=<none|Path1|Path2|xhigh> · note: <one-line learning>
     Stage 1 reads the recent N lines to bias routing for similar task classes.
     See ~/.productune/po-instructions.md §"Effort learning loop" for full doctrine. -->
