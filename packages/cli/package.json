{
  "name": "@productune/cli",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'cli: no build configured yet'",
    "test": "echo 'cli: no tests configured yet'",
    "lint": "echo 'cli: no lint configured yet'"
  },
  "dependencies": {
    "@productune/core": "workspace:*"
  }
}
