---
target: cli-install
method: install-script
auto: no
---

# Deploy — productune v1 (worktree of productune)

- productune 리포의 linked worktree — v1 신버전 작업 라인 (구 v0.6은 `productune/`).
- 웹 배포 없음. 배포 = install.sh 로 `~/.prdt` 설치, 업데이트 = `prdt update`.
- CI: GitHub Actions fresh-install-smoke.
