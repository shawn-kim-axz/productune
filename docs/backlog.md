# Backlog

알고서 미룬 결정 추적 (PRD·티켓 본문은 고치지 않음). 한 줄 = 제목 + 왜 미뤘는지 + 출처.
P3 close gate 첫 단계에서 각 항목을 이번 버전 적용 / 다음 버전 이월로 분류한다.
RESOLVE/DONE 항목은 줄 제거(2026-06-23 reconcile sweep — backlog↔ticket lifecycle doctrine 티켓 발행 예정).

## near-term

- (2026-06-23) [artifacts 뷰어, quick-win] GUI artifacts 읽기가 flat 전용(`ipc/artifacts.ts` scanDir `readdirSync`+`isFile`, 재귀 없음, `archive/`만 예외) → 페르소나가 flat 규칙 어기고 하위폴더 생성 시 세션 중 GUI에서 조용히 사라짐(CI lint `find -maxdepth 2`만 나중에 잡음). **C안**: non-archive 하위폴더 1단 재귀 발견 → "기타/중첩(flat 위반)" collapsed 섹션(archive 토글 재사용) + ⚠ 위반 플래그로 표시(평탄화 유도, flat invariant 보존). Designer plan-first. 큰 lifecycle 파트(focus-pinning + archive 케이던스)는 v0.6 항목 참조. (출처: 2026-06-23 shawn dogfood 피드백)

- cmd+K cmd+\ split-down 체인(chord)이 OOPIF iframe 포커스 내부에선 동작 안 함 — 단일 accelerator 불가. iframe 안에서 split-down 필요 시 전용 accel 메뉴아이템 추가 검토. (2026-06-08, src: T-PATCH-066 R4)

- doctrine-file 저장 "PO 검수" 자동적용(replace-on-approval) — 승인 시 whole-file 교체하는 mechanical-write 모드가 없음(기존 머신은 append-only). 현재는 PO가 pending 보고 수동 반영. (출처: T-PATCH-022 GAP-2, 2026-06-05)
- doctrine enum 불일치 — promotion-process 5th-retro snapshot은 status ∈ {approved, edited, dropped}인데 lifecycle/p5-close.md는 {approved, edited}로 'dropped' 누락. 정합 필요(designer). (2026-06-23 재검증: 여전히 OPEN — promotion-process.md:9 vs p5-close.md:19). (출처: doctrine sweep grill, 2026-06-05)
- T-PATCH-031 후속 — persona-spec viewer 높이/스크롤(specViewerWrap 360px) 시각 적합성 눈으로 확인 필요. (출처: T-031 dev note, 2026-06-05)
- git ticket-branch 네이밍 drift — git-workflow는 `v<N>-T-<N>-<slug>`, developer habit §2는 `feat/T-NNN-<slug>`. worktree-per-ticket 의무화로 정합 필요(designer). (2026-06-23 재검증: 둘 다 현존 — version-branch vs task-branch 레이어 구분일 수 있어 진짜 drift 인지 명확화 필요). (출처: git-posture grill, 2026-06-05)
- close_gate GUI write-path parity — GUI `phase:approve` IPC(`packages/gui/electron/ipc/state.ts`)가 phase 전환을 TS로 직접 write라 `close_gate` instantiate를 우회함. (2026-06-23 재검증: deterministic 자가치유 hook은 이제 라이브 = 부분 backstop이나, GUI IPC write-path 자체는 hook(PreToolUse) 비경유라 parity 미해결). GUI close_gate 렌더링 ticket과 함께 정합(developer). (출처: T-PATCH-041 designer plan F2, 2026-06-05)
- [near-term] "Invalid tool parameters" 간헐 발생 — OQ(AskUserQuestion) 답변 직후 `claude --resume` 재개 시. CC 코어 InputValidationError(productune 미발생, dist만 매칭). 유력원인: resume 시 코어가 대기중 AskUserQuestion tool_use 재검증 throw(업스트림). productune-side 방어: (a) `electron/ipc/po.ts:105` answerText 미정화 → control-char sanitize, (b) `po-runner.ts:384` silence-timeout(15s)가 OQ대기중 발화→resume race 조율. 근본확정엔 발생시 transcript/캡처 필요. (출처: Explore 진단, 2026-06-17, shawn 보고)

## next-version (v0.x)

- [v0.6] (T-PATCH-255 carry from v0.5) PO 턴 tool 실패(특히 macOS TCC Downloads/Desktop/Documents 접근 거부)가 무응답·무안내 silent 종료 → actionable 배너/메시지 노출(실패 사유 + 다음 행동). dev+QA L2. 티켓 본문 SoT = `docs/tickets/v0.5/T-PATCH-255.md`. (2026-06-25 v0.5 P5 close 시 이월 — shawn 결정)

> v0.6 theme = **도그푸딩 완주 + 안정화**: v0.5가 깐 풀사이클 인프라를 비개발자가 GUI-only로 실제 완주(북극성, observed_result 추적) + 아래 6개 dogfood-stabilize doctrine/process 갭 해소 + 6차 dmg 리빌드(T-254/256/257 미반영).
- [v0.6][dogfood-stabilize][DESIGN-S1] DS 옵션 제시 방식 — 현재 3안을 텍스트+대충 도식으로 줘서 비개발자가 구분 불가 → **처음부터 렌더된 HTML 시안 3개**로 제시. + 3안이 서로 너무 비슷한 문제 → A·B안=기존 doctrine(Fit), **C안=매번 디자인 웹서치 기반 완전히 새 방향(Claude 창의성으로 발산)**. designer phase2-3-ticket-sequence S1 개정. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][DESIGN-S2b] 에셋 생성 2건 — (a) **SVG 고집 금지**: 생성형 PNG(Codex/핸드오프) 우선 + "유저가 PNG 돌려주면 Claude가 SVG로 변환/후처리"하는 루프 신설(현 ③ SVG 폴백으로 너무 빨리 빠짐) (b) 이미지모델 핸드오프 **프롬프트는 user_lang 무관 무조건 영어**(한글 프롬프트=이미지 품질저하). designer S2b + habit §5 개정. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][BUILD] 빌드 완료 → close gate 사이에 "앱 실제 run(dev run) + 유저 눈확인 + **통합 시각 grill**" 단계가 codify 안 됨 → 신설(유저에게 run 띄우고 눈확인 유도). 현 smoke=mount+console만이라 CSS깨짐/간격 못 잡음(fail-pattern T-PATCH-095 5루프). smoke 시각 사각 보강. p3-build + qa doctrine. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][TEST] 테스트 작동 감사 — `type:test` 트리거가 리스크-게이트(auth/payments/PII·≥3스텝·area-tag≥3실패·유저명시)라 GUI엔 거의 미발화 + `.test.ts`들(core/gui: costArchive/useTicketScan/dedupe/smoke.spec…)이 vitest/jest 러너 미배선으로 실행 안 될 가능성(turbo test→core `.mjs` 2개만 실행). 러너 배선 확인·수정 + 트리거 재조정. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][GUI] 워커 페르소나(Designer/Developer/QA) 작업 중에도 **스프라이트가 활성화(working) 안 되고 회색-idle 유지** — subagent active 상태가 sprite presence에 미반영. cf. T-PATCH-252(PO sprite 동일 버그, v0.5 수정)의 워커판 + #9(워커 출력 streaming)와 동일 precondition(워커 subagent 상태→GUI 배선) 공유. dev L2. (2026-06-25 shawn, 스크린샷)
- [v0.6][dogfood-stabilize][GUI] PO 채팅 tool-call 표시의 **최하단 토글(도구 상세 내용: path/limit 등 인자)이 기본 open(expanded)** 되도록 — 현재 접혀 있어 도구 펼쳐도 내용 보려면 한 번 더 펼쳐야 함. 도구 열면 전체 내용 바로 노출. dev L1. (2026-06-25 shawn, 스크린샷)
- [v0.6][dogfood-stabilize][GUI] **po-state.json 실시간 감시(watcher) 부재** — GUI가 프로젝트 열 때 readPoState 1회만 실행(WorkspaceShell, projectDir 변경 시에만), 파일 watcher 없음 → 세션 중 버전 생성/PRD-ready 등 po-state 변경이 GUI 미반영(옛 "v1 대기중" 폴백 고착 → 수동 Cmd+R/프로젝트 재선택 필요). po-state 파일 watcher 추가로 변경 시 GUI 라이브 갱신. = 클러스터(#11/#14)의 precondition (ii)(PO lifecycle→GUI 신호) **구체 backbone** — P2 디자인이 이미 가정함, #14와 한 dev plan. dev L2. (2026-06-25 shawn, 진단 첨부)
- [v0.6][dogfood-stabilize][GUI] PRD 완성(P1 ready)/버전 생성 시 GUI 메인 패널이 **자동으로 PRD를 안 띄우고 버전 진입도 자동 안 됨** → 유저가 수동으로 Version Detail/PRD 섹션 클릭해야 함. 근본: PO(백엔드)가 GUI 메인 패널 뷰를 직접 못 엶 → **PO 라이프사이클 이벤트(version-open·PRD-ready 등)가 GUI 메인 패널을 자동 네비게이트(PRD 오토-오픈/포커스)**하도록 신호 배선. cf. #11(조건부 레이아웃)과 동일 lifecycle→GUI 반응 계열 — 한 dev plan으로 묶을 수 있음. dev L2~L3. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][GUI] (#18, #11 chat-only 정밀화) 첫 PO 대화 시작 화면 = **순수 PO 채팅창만 크게**. (a) chat-only 시 ActivityBar **완전 숨김**(현 v0.6 #11은 dim) (b) 오른쪽 "PO와 대화하며 시작해요" 인트로/WelcomePanel 제거 → PO 채팅만 (c) 첫 대화 시 채팅창 너무 작음 → 크게(primary). v0.6 #11 WelcomePanel/레이아웃 designer 수정 + dev. ※ pnpm dev로 v0.6 #11 실물 확인 후 정확한 delta 확정 권장. (2026-06-30 shawn dogfood)
- [v0.6][dogfood-stabilize][GUI] 조건부 워크스페이스 레이아웃 — PO PRD 인터뷰 루프 중엔 po-state에 current_version/PRD가 아직 없어 다른 패널(Versions/Tickets/PRD/Artifacts)이 빈 화면. **렌더할 게 생기기 전(버전 생성 + PRD 파일)까지는 PO 채팅 UI만 단독 표시** → 패널에 볼 게 생기면 전체 패널 레이아웃으로 확장. dev L2~L3. (2026-06-25 shawn, 스크린샷)
- [v0.6][dogfood-stabilize][GUI] PO 채팅 하단 페르소나 presence 행 — 영역 커지면 빈 공간 많음. 작업 중인 워커 페르소나(PO 제외: Designer/Developer/QA)의 **실시간 터미널/출력 내용을 해당 스프라이트 오른쪽에 streaming** 표시(빈 공간 활용 + 진행 가시성). cf. 기존 backlog "PO Log Terminal 탭 미구현"(2026-06-16)과 출력-캡처 인프라 공유 가능 — 워커 출력 스트림 배선이 선행. dev L2~L3. (2026-06-25 shawn, 스크린샷)
- [v0.6][dogfood-stabilize][INPUT] PO 채팅 입력창에 한글(IME) 조합 중 **Cmd+Enter를 두 번 쳐야 전송**되는 경우 — 첫 Cmd+Enter가 IME 조합 확정에 먹혀 submit 핸들러로 안 감(`isComposing`/keyCode 229 미처리 추정). 한글 조합 중 Enter/Cmd+Enter 전송 핸들링 정합(cf. T-PATCH-196 isComposing 가드 패턴). dev L1~L2. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][INPUT] 유저 메시지가 `-`/`--`로 시작하면 claude CLI가 옵션으로 오파싱 → `error: unknown option '- ...'` + claude exit code 1로 **턴 crash**(스크린샷: "- 타깃: 여러사용자…" 입력 시 발생). po-runner가 유저 텍스트를 CLI arg로 넘길 때 `--` end-of-options 구분자/stdin 전달/이스케이프 미적용 추정. 리딩 대시(및 옵션처럼 보이는 입력) 안전 처리. dev L1~L2. (2026-06-25 shawn)
- [v0.6][dogfood-stabilize][GUI] 메뉴막대(tray) 아이콘 빨간점이 **항상** 떠 있음 → PO가 작업 완료해서 **유저에게 턴을 넘긴 경우에만** 빨간점(attention badge) 표시, 그 외(idle/작업중)엔 제거. tray.ts badge 상태를 PO turn-state(awaiting-user)와 연동. dev L2. (2026-06-25 shawn)
- [near-term debt] (T-PATCH-261 파생) `type:test` 트리거가 p3-build.md(Test trigger) + qa/habit.md §4 **두 곳에 byte-identical 중복** — 현재 "SYNCED PAIR" 인라인 노트로 동기 유지 중이나, 단일 SoT + cross-ref로 collapse하면 desync 위험 제거. (2026-06-25, T-261 designer 권고)
- [v0.6][dogfood-stabilize][CODE-REVIEW] 코드리뷰/리팩토링 게이트 부재 — `type:refactor`(반응형 티켓)+GRILL(loss-risk refactor)은 있으나 diff를 correctness/재사용/단순화 관점으로 훑는 **코드리뷰 패스가 없음**(QA=build/smoke/acceptance/디자인리뷰지 코드품질 아님). 빌드루프 또는 close-gate에 코드리뷰 신설(하네스 `/code-review`·`/simplify` 스킬 활용 검토). (2026-06-25 shawn)
- [v0.6] 정합성 감사 + 페르소나 워크스루 + Nemotron persona distill 모듈 — 품질 3차원 중 Coherence/Usability 빈칸을 메우는 독립 적대자. QA에 2개 신규 모드(coherence-audit·persona-walkthrough) + QA를 P2 소환, designer에 distill 파이프라인. 자체 PRD로 P1부터 태울 것(이 backlog 항목≠즉시구현). 롤아웃 R1(정합성 감사 MVP) → R2(distill→워크스루). 브리프: `/Users/shawn.axz-pc/Documents/dev/ntf-products/prd-master/DESIGN-coherence-persona-module.md`. (2026-06-19, shawn 결정 — v0.6 이월)

- [v0.6] lazyweb UI/UX 레퍼런스 grounding 실험 — Designer를 lazyweb MCP(실앱 281k 화면/flow/competitive) 레퍼런스로 grounding하면 design.md/anchor 품질이 baseline보다 나은가. 방법론 grill 완료(2 blocking flaw 교정): S1→S2 anchor 블록 단위 측정 · A/A+/B 3-arm(교란 격리) · 가설전용 constructs 루브릭 신규 · 인용strip 블라인딩 · 게이트 임계 사전등록 · Step0 MCP 연결 hard go/no-go(현재 미연결, install.sh 또는 수동 config+token 필요). 브리프 SoT: `briefs/lazyweb-design-grounding.md`(v2). (2026-06-23, shawn 결정 — 생각보다 큰 건이라 v0.6 이월)

- [v0.6] artifacts lifecycle — focus-pinning + archive 케이던스. 현 doctrine은 archive 이동이 P2 게이트 닫을 때만(Designer) → 긴 P3 동안 중간 산출물 쌓여 중요 산출물(DS/mockup)이 focus에서 묻힘. 필요: (a) 뷰어가 "현재 중요"(최신 approved DS·surface별 최신 mockup) 상단 고정 + supersede 강등, (b) P3 중 artifact-tidy 케이던스(티켓 close 시 생산 페르소나가 supersede 변종 archived 마킹 / PO가 체크포인트에 sweep)를 lifecycle doctrine에 명문화. near-term 뷰어 quick-win(C)과 한 Designer plan으로 스코프. (출처: 2026-06-23 shawn dogfood 피드백)

- [v0.6] PO가 claude CLI 시스템 메시지를 유저에게 해석·중재 — claude CLI 자체가 내보내는 공지(실예: `"Added user:design:read and user:design:write to your claude.ai login (for the Design MCP connector)"`)가 raw로 PO 채팅에 새어나옴(productune 소스엔 이 문자열 0건 — claude stderr/system 라인을 po-runner가 그대로 surface). 유저는 이게 뭔지 모름. 필요: PO(또는 렌더 레이어)가 claude CLI의 system/stderr 공지를 가로채 ① 무시할만한 하우스키핑은 숨기고 ② 유저 확인·조치 필요한 건 평이한 말로 **해석 + 대안 제시**하도록 중재. 현재 po-runner.ts가 stderr 라인을 분류 없이 채팅에 노출(rate-limit/permission 등 일부만 health 신호로 파싱). (출처: 2026-06-23 dmg deploy 라이브 검증 중 shawn 발견 — Design MCP connector 스코프 자동추가 공지가 PO 응답 하단에 raw 노출)

## pre-deploy
[v0.5 backlog_triage 2026-06-23 — APPLY 결정. Deploy(dmg) 진입 시 티켓 발행.]
- ✅ DONE (2026-06-23) [APPLY v0.5 → Deploy] `uninstall.sh` is_pdt orphan 4개 보강 — `post-po-state-shape-guard`·`post-ticket-status-verify`·`pre-po-state-shape-guard`·`session-start-po-state-migrate` (install.sh strip엔 있는데 uninstall엔 없어 orphan). **해결: uninstall.sh `is_pdt` 함수에 4개 hook endswith 추가(install.sh와 정합).** (정정 2026-06-23: fresh-install CI smoke는 C1로 이미 done — `.github/workflows/fresh-install-smoke.yml` 4 checks 완비.) (출처: 2026-06-23 dmg-deploy triage)
- [APPLY v0.5 → Deploy 후행] 라이브 검증 집중 세션 T-220/231/221 — dmg가 만든 빌드를 cua VM에 설치 후 human VNC 눈확인. ① T-220 onboarding Step2 Next 게이팅+codex 미노출 ② T-231 not-installed/401 분류배너 ③ T-221 15s/90s 침묵 라벨. T-031 persona-spec viewer 높이 + CostArchivePanel row-level 눈확인 잔여 흡수. harness 교훈(VM 설치빌드=검증대상 일치 확인) Tier2 cua-vm-harness.md에 designer+grill. (출처: 2026-06-22 qa-cua-live + 2026-06-23 triage)

## follow-up (dogfood / 티켓 잔여)
- [v0.7 design debt] (T-PATCH-271 후속) usage/session cap을 **전용 PoHealthState 'usage-capped' + UsageCappedBanner**로 — 현재 #17은 cap을 기존 `rate-limited` 배너에 매핑(카운트다운이 cap엔 부적합, reset 없으면 "잠시 후" 문구가 실제 수시간 대기를 과소표현). 'capped'/'auth-required'는 qa-loop status라 emitHealth 불가 → 신규 PoHealthState + 렌더러 배너 필요. + [v0.6 verify] #17 라이브 cap/auth exit 메시지 텍스트가 EXIT_ERROR_PATTERNS 행과 실제 매칭되는지 cua/hands-on(claude 한도 도달·토큰 만료 재현). (2026-06-30 shawn dogfood)
- [v0.6 doctrine refine] (#3 run-prompt 정밀화, T-261 후속) p3-build close-gate **진입 순서 명문화**: precondition = QA grill 완료 → PO가 유저에게 dev 환경 run+눈확인을 **먼저 능동 프롬프트**(PO가 명시적으로 띄움; skippable이나 디폴트로 권함) → 그 후 close-gate steps(backlog_triage…) 진행. 현 T-261 #3은 "build-complete~close-gate 사이 prompted"로만 표현 → "QA-grill-done이 precondition, PO 능동 프롬프트, close-gate 직전" 으로 sharpen. designer doctrine edit + grill. (2026-06-25 shawn dogfood)
- [정정됨, non-issue] (2026-06-25 PO 오인) "GUI dev 턴키 명령 부재"는 틀림 — `cd packages/gui && pnpm dev`가 정식 풀 GUI dev 명령(vite-plugin-electron/simple: main/preload 빌드 + electron 자동 실행 + 핫리로드). `vite.config.ts` 확인. PO가 `gui dev=vite(렌더러만)`로 오판했던 것. 남는 minor: README/docs에 dev 실행법 1줄 명시(발견성) — near-term nicety, 갭 아님. (2026-06-30 정정)
- [v0.6 verify] cluster 웨이브(T-269/270) **cua-vm/hands-on 런타임 검증** 필요 — 헤드리스 불가: #15 watcher 라이브 재읽기(버전 생성 mid-session→GUI 갱신, 렌더스톰 0) · #11 chat-only↔full 전환+pane/tab 보존 · #14 PRD 오토네비 1회+정확한 경로(versions/<v>.md-only 케이스) · #10 워커 스프라이트 idle→working→done(2s)→idle(PO 제외) · #9 responsive 슬롯(≥380 inline/<380 stacked)·per-line ellipsis·collapse·NOISE-0 · #9 sidechain 마커 shape(parent_tool_use_id, T-165/166급 runtime-verify). QA 정적+build+codereview는 pass. (2026-06-25)
- [v0.6 verify] code-quick 웨이브(T-262/263/264/265/267/268) **cua-vm/hands-on 런타임 검증** 필요 — 헤드리스 불가 동작: #7 tray 빨간점 사이클(idle/working=off, onDone=on, user reply=off) · #8 리딩 `-` 입력 crash→정상 · #6 Downloads TCC 거부→actionable 노출 · #12 한글 IME Cmd+Enter 1회 전송 · #13 tool 상세 즉시 노출 · #3 smoke 3테스트 실행. QA 정적+build는 pass. (2026-06-25, v0.6 verify 세션 or 차기 dmg)
- [near-term] (T-PATCH-249 follow-up) design-deliverable kind set를 doctrine SoT로 선언 — `pre-phase-gate-guard.sh` G7이 `{mockup,wireframe,design-system,spec}`를 bash에 하드코딩. `designer/bookshelf/artifact-manifest-schema.md`(현 91/100 — split 주의)에 "design-deliverable kinds" 분류 1줄 선언 + G7이 참조하도록 하면 drift 방지. (QA acceptable-with-followup, 비블로커). (2026-06-24)
- [near-term] settings round-trip regression test (load -> mutate one field -> save -> reload -> all fields intact) — T-PATCH-083 중 loadSettings() integrations silent-wipe 버그 발견+수정됨; 재발 방지 테스트 없음. (2026-06-23 재검증: 여전히 테스트 부재 OPEN). (2026-06-10)
- [v0.6] non-mac tray icon (Tray instance, icon asset, context menu) — deferred from T-PATCH-090 close-to-tray mac-only first cut. (2026-06-10)
- [v0.6+] GUI 자동 업데이트 (electron-updater) — 서명·배포 인프라 필요해서 미룸. 당장은 수동 dmg + 버전 불일치 배너로 감. 외부 배포 시점에 재검토. (2026-06-10, src: harness 구조 피드백 결정 보드 #3-GUI) **[2026-06-22 결정 고정]** GUI 업데이트는 `productune update`(CLI/doctrine 채널)에 끼워넣지 않고 **GUI 자체의 별도 업데이트 채널**(앱 내 update 표시/electron-updater 방향)로 분리한다. dogfood는 전부 CLI라 GUI 미적용은 당장 문제 아님. (T-PATCH-229 abandoned — 잘못된 원인분석 파생)
- [v0.6] launch-at-login openAsHidden (login 시 창 pre-hidden 시작) — T-PATCH-090 first cut 에서 deferred. (2026-06-10)
- (2026-06-12) [T-PATCH-128 follow-up] `persona/designer/bookshelf/phase2-3-ticket-sequence.md` cap breach — curation/restructure to reclaim cap = loss-risk op, needs dedicated doctrine ticket + QA grill. (2026-06-23 재측정: **150**/100 — 악화. 104(pre-128)→147(225/226)→150 현재.)
- (2026-06-12) [T-PATCH-129 follow-up] `ux-principles.md` cap breach — phase2-3-ticket-sequence.md breach와 묶어 doctrine cap-curation 티켓 1건으로 triage (loss-risk, QA grill). (2026-06-23 재측정: **108**/100.)
- (2026-06-15) [brand process, 결정고정] DS 확정 후 로고·에셋 생성 워크플로 = **핸드오프-먼저 + MCP-후속**. ① 근시일: Designer가 영문 로고/에셋 프롬프트 + 스타일 디렉션 brief를 산출물로 author → 션님이 외부 이미지모델(ChatGPT/Gemini/MJ)에 투입(infra 0, 사람 큐레이션 유지). ② v0.6+ 후속: Figma MCP create-design / Vercel AI Gateway 이미지생성 연동으로 반자동화 검토. 실행은 design-system 확정 게이트 이후. codex/CLI 위임안은 폐기(이미지 생성 불가). (출처: 2026-06-15 dogfood 피드백 #4, shawn 결정) — **정정(2026-06-19, T-PATCH-226): Codex agent 이미지 생성 가능 확인(shawn)으로 위임안 부활.** S2b 정책 = 위임-우선(① Codex 위임 → 한도소진/실패 시 ② ChatGPT/Gemini 핸드오프 → ③ 유저 거부 시 Claude 직접 SVG). "codex 위임 폐기"는 이 정정으로 무효.
- (2026-06-15) [T-027 follow-up] `turns.jsonl` append-only 무한증가 — rotation/size-cap/prune 정책 없음(현재 gitignore로 커밋만 차단). 장기 dogfood 시 파일 비대 → rotation 티켓 별도. (2026-06-23 재검증: 여전히 정책 부재; `post-delegate-state-write.sh`가 전체 파일 메모리 로드 dedup만 수행.) (출처: T-027 dev/QA residual risk)
- (2026-06-15) [T-PATCH-136 follow-up] `packages/gui/electron/ipc/skills.ts` `parseSkillFrontmatter`가 tickets.ts와 동일 regex-only 파서 버그 — 인라인 `#` 주석 미처리 + 무조건 unquote(`replace(/^['"]|['"]$/g,'')`). skill frontmatter도 같은 오파싱 가능. tickets.ts 수정(T-PATCH-136)과 동일 패턴으로 별도 티켓 정합 필요(dev). (2026-06-23 재검증: tickets.ts만 fix됨, skills.ts:62 여전히 옛 파서 — 진짜 OPEN.) (출처: T-PATCH-136 dev/QA AC-9 flag)
- (2026-06-16) [GUI feature, v0.6 후보] PO Log "Terminal" 탭 미구현 — `PlaceholderTab`이 type 'terminal'을 "T-P4-054에서 채워짐"으로 렌더, 가리키는 `.productune/logs/po-session.log`는 po-runner가 생성조차 안 함. 제대로 하려면 2단: (a) po-runner가 세션 트랜스크립트를 po-session.log에 기록(로그 생산 배선) + (b) tail IPC + read-only 로그 뷰 컴포넌트. 인터랙티브 shell(node-pty)은 별도 후속(Round6 T-P4-060). 패키징 우선이라 backlog. (출처: 2026-06-16 paepyeong smoke 피드백)
- (2026-06-16) [po-state hook, defense-in-depth] T-153/154 grill 파생 — (a) `session-start-po-state-migrate.sh` load-bearing 생존검증 목록(version/current_phase/slug/request_summary/artifacts/persona_sessions 6개)이 canonical-14 중 persona_session_meta/started_at 등을 누락; transform이 안 건드려 실손실 없으나 훅 주석 "likewise preserved" 보증과 커버리지 어긋남 → 검증목록 보강 또는 주석 정정. (b) active scratch 정리가 close시 current_task→null 신뢰성에 단일 의존(past_tickets처럼 기계적 무조건-차단 아님) → migrate/sweep에 "status done/blocked/abandoned인 current_task scratch 정리" backstop 추가 검토. (출처: T-PATCH-154 GRILL pass-with-issues)
- (2026-06-18) [doctrine trim debt] `packages/core/doctrine/persona/qa/bookshelf/design-review.md` AT 100/100 cap — 향후 슬롭 루브릭 보강/추가 시 트림 선행 필수(현재 한 줄도 못 늘림). close-gate(phase3-close-gate.md)가 이 파일을 bands SoT로 cross-persona 의존 → 트림 시 게이트 임계값이 참조하는 밴드 보존 확인. (출처: T-PATCH-211/212 QA grill risk_flag)
- (2026-06-18) [low, a11y finish] Settings helper text 대비 미달 — `#707070` on `#0F0F0F` = 3.87:1 (AA 4.5:1 미달). T-PATCH-214 #3는 line-height만 변경(색 무관)이라 선재 조건, 티켓 범위 밖. 향후 마감 패스에서 helper text 색 ~`#8A8A8A`로 상향(≥4.5:1). (2026-06-23 재검증: `--text-faint: #707070` 여전, OPEN.) (출처: T-PATCH-214 QA observation)
- (2026-06-23) [near-term, T-PATCH-237 잔여] v0.4 티켓 legacy qa_status(ready/passed/skip/todo/smoke/static-pass) 보유 → **whole-file Write 시 BLOCK**(Write 채널은 제안 FM 전체 검증). 본문 Edit는 FM-diff로 통과하나, 그 파일들의 whole-file 재작성/qa_status 줄 편집은 막힘. v0.4 qa_status를 canon enum으로 마이그(닫힌 티켓이라 우선순위 낮음, T-233式 일괄). (2026-06-23 재측정: **116건** legacy 보유 — 기존 cite 75 정정.) (출처: T-237 GRILL out-of-scope 잔여)
