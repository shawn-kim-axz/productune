---
ticket_id: T-P4-108
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: done
assignee: pdt-developer
estimated_complexity: L1
actual_complexity: L1
risk_flags: fs-mutation
slug: t-p4-104-fixforward-install-sh-doctrine-md
qa_status: ready
qa_loops: 0
created: 2026-05-14
parent_ticket: T-P4-104
prior_partial_commit: 3c6cb35
completed_at: 2026-05-14T10:18:00Z
commit: d23291c
note: Sub-area B/C (po-instructions.md + delegation.md) 는 dev grep 결과 이미 존재 (T-P4-104 partial 단계에서 갱신됨) → skip 처리. install.sh L405-465 merge_claude_settings_hooks() 의 PreToolUse 배열에 pre-chunking-warn.sh entry 추가만 본 commit scope.
---

# T-P4-108 — T-P4-104 fix-forward: install.sh PreToolUse entry + doctrine md 2건

## §1 Background

### 1.1 발단 — T-P4-104 partial commit

commit `3c6cb35`에서 `pre-chunking-warn.sh` hook script 자체(sub-area A)만 land됨.
나머지 2개 항목이 미착지 상태:

| 항목 | 파일 | 상태 |
|---|---|---|
| install.sh PreToolUse merge jq — hook entry 추가 | `packages/core/scripts/install.sh` | **미착지** |
| po-instructions.md §Hard rules — chunking ceiling 1줄 | `~/.productune/po-instructions.md` | **미착지** |
| delegation.md §PRD delegation — chunking cross-ref 2라인 | `~/.productune/sections/delegation.md` | **미착지** |

hook script가 존재하지만 `install.sh` 재실행 시 `~/.claude/settings.json`의 `PreToolUse` 목록에 등록되지 않아 **실질적으로 dead code** 상태. 본 ticket이 나머지 3 sub-area를 완료한다.

### 1.2 현재 install.sh 구조 파악

`packages/core/scripts/install.sh` L405–462의 `merge_claude_settings_hooks()` 함수:

- `is_pdt()`: command가 hooks dir prefix로 시작하거나 알려진 hook basename으로 끝나면 pdt hook으로 판정 → strip 대상.
- `strip_pdt()`: PreToolUse / PostToolUse / PostCompact / Stop 각 배열에서 pdt 항목 제거 후 신규 항목 concat(idempotent).
- **현재 PreToolUse**: `pre-delegate-task-check.sh` 단 1 entry. `pre-chunking-warn.sh` 없음.

---

## §2 Scope — 3 sub-area

### Sub-area A: install.sh — PreToolUse jq filter에 pre-chunking-warn.sh entry 추가

**파일**: `packages/core/scripts/install.sh`

**변경 위치 1: 변수 선언** (현재 L418–422 블록)

```bash
# BEFORE
local fmt="$hooks_dir/post-edit-format.sh"
local doc="$hooks_dir/post-compact-doctrine.sh"
local stop="$hooks_dir/stop-verify.sh"
local statew="$hooks_dir/post-delegate-state-write.sh"
local precheck="$hooks_dir/pre-delegate-task-check.sh"

# AFTER
local fmt="$hooks_dir/post-edit-format.sh"
local doc="$hooks_dir/post-compact-doctrine.sh"
local stop="$hooks_dir/stop-verify.sh"
local statew="$hooks_dir/post-delegate-state-write.sh"
local precheck="$hooks_dir/pre-delegate-task-check.sh"
local chunkwarn="$hooks_dir/pre-chunking-warn.sh"
```

**변경 위치 2: jq `--arg` 파라미터** (현재 L425)

```bash
# BEFORE
if ! jq --arg fmt "$fmt" --arg doc "$doc" --arg stop "$stop" --arg statew "$statew" --arg precheck "$precheck" --arg dir "$hooks_dir/" '

# AFTER
if ! jq --arg fmt "$fmt" --arg doc "$doc" --arg stop "$stop" --arg statew "$statew" --arg precheck "$precheck" --arg chunkwarn "$chunkwarn" --arg dir "$hooks_dir/" '
```

**변경 위치 3: `is_pdt()` basename guard에 pre-chunking-warn.sh 추가** (현재 L426–432)

```jq
# BEFORE
def is_pdt(cmd; dir):
  (cmd | startswith(dir))
  or (cmd | endswith("/scripts/hooks/post-edit-format.sh"))
  or (cmd | endswith("/scripts/hooks/post-compact-doctrine.sh"))
  or (cmd | endswith("/scripts/hooks/stop-verify.sh"))
  or (cmd | endswith("/scripts/hooks/post-delegate-state-write.sh"))
  or (cmd | endswith("/scripts/hooks/pre-delegate-task-check.sh"));

# AFTER
def is_pdt(cmd; dir):
  (cmd | startswith(dir))
  or (cmd | endswith("/scripts/hooks/post-edit-format.sh"))
  or (cmd | endswith("/scripts/hooks/post-compact-doctrine.sh"))
  or (cmd | endswith("/scripts/hooks/stop-verify.sh"))
  or (cmd | endswith("/scripts/hooks/post-delegate-state-write.sh"))
  or (cmd | endswith("/scripts/hooks/pre-delegate-task-check.sh"))
  or (cmd | endswith("/scripts/hooks/pre-chunking-warn.sh"));
```

**변경 위치 4: PreToolUse 배열에 chunkwarn entry 추가** (현재 L439–442)

```jq
# BEFORE
| .hooks.PreToolUse = (strip_pdt(.hooks.PreToolUse; $dir) + [{
    matcher: "Bash",
    hooks: [{type: "command", command: $precheck}]
  }])

# AFTER
| .hooks.PreToolUse = (strip_pdt(.hooks.PreToolUse; $dir) + [
    {matcher: "Bash",
     hooks: [{type: "command", command: $precheck}]},
    {matcher: "Bash",
     hooks: [{type: "command", command: $chunkwarn}]}
  ])
```

> **idempotent 보장**: `is_pdt()` basename guard에 `pre-chunking-warn.sh`가 추가됐으므로 `strip_pdt()`가 기존 entry를 제거 후 재삽입 — 중복 없음.

---

### Sub-area B: po-instructions.md §Hard rules — chunking ceiling 1줄 추가

**파일**: `~/.productune/po-instructions.md`

현재 §Hard rules (L65–82) 말미에 아래 1줄 삽입:

```markdown
-- **Designer dispatch ceiling** — ≤2 designer-owned artifacts per `--agent pdt-designer` call.
  ROADMAP rows / Activity Log appends are PO-mechanical (excluded from count).
  Violation → cancel dispatch, re-split into ≤2-artifact calls before re-delegating.
```

> 이미 L79–81에 위 내용이 존재함을 확인. 만약 미존재 시에만 삽입(grep 후 조건부 append). idempotent.

**실제 현재 상태 재확인 필요**: `grep -n "chunking" ~/.productune/po-instructions.md`. 이미 있으면 skip.

---

### Sub-area C: delegation.md §PRD delegation — chunking cross-ref 2라인 추가

**파일**: `~/.productune/sections/delegation.md`

현재 §PRD delegation (L98–101):

```markdown
## PRD delegation (Designer, clarity loop)

> **Dispatch ceiling**: ≤2 designer-owned artifacts per `--agent pdt-designer` call.
> ROADMAP / Activity Log rows are PO-mechanical (excluded). See §Hard rules in po-instructions.md.
```

> 이미 위 cross-ref 2라인(L100–101)이 존재함을 확인. 만약 미존재 시에만 삽입.

**실제 현재 상태 재확인 필요**: `grep -n "Dispatch ceiling" ~/.productune/sections/delegation.md`. 이미 있으면 skip.

---

## §3 Acceptance Criteria

1. **install.sh 재실행 → hook 자동 등록**
   ```bash
   bash packages/core/scripts/install.sh
   cat ~/.claude/settings.json | jq '.hooks.PreToolUse'
   # pre-chunking-warn.sh entry 보임
   ```

2. **중복 없음 (idempotent)** — install.sh 2회 연속 실행 후 PreToolUse 배열에 pre-chunking-warn.sh entry가 정확히 1개.

3. **기존 hook entry 보존** — `pre-delegate-task-check.sh`, `PostToolUse`, `PostCompact`, `Stop` 항목 소실 없음.

4. **shellcheck pass**
   ```bash
   shellcheck packages/core/scripts/install.sh
   ```

5. **jq parse OK**
   ```bash
   jq empty packages/core/scripts/install.sh  # (jq syntax preview 불필요)
   # 실제: bash install.sh 성공 = jq filter 파싱 OK
   ```

6. **po-instructions.md §Hard rules 안 chunking 룰 1줄 존재**
   ```bash
   grep -c "chunking" ~/.productune/po-instructions.md
   # ≥1
   ```

7. **delegation.md §PRD delegation 안 chunking cross-ref 2라인 존재**
   ```bash
   grep -c "Dispatch ceiling" ~/.productune/sections/delegation.md
   # ≥1
   ```

---

## §4 Out of Scope

| 항목 | 이유 |
|---|---|
| pre-chunking-warn.sh 스크립트 자체 변경 | T-P4-104 partial commit `3c6cb35`에서 완료. 그대로 사용. |
| 다른 hook script 변경 | 본 ticket 범위 외 |
| GUI / store 변경 | 해당 없음 |
| ROADMAP / Activity Log 업데이트 | PO-mechanical. 범위 외. |

---

## §5 Module Map

### 5.1 변경 대상 파일

| 파일 | 변경 내용 | 규모 |
|---|---|---|
| `packages/core/scripts/install.sh` | `merge_claude_settings_hooks()`: 변수 1줄 추가, `--arg chunkwarn` 삽입, `is_pdt()` 1줄 추가, PreToolUse 배열 entry 1개 추가 | 극소 (~6 line 순증가) |
| `~/.productune/po-instructions.md` | §Hard rules 말미 chunking ceiling 1줄 (이미 존재 시 skip) | 조건부 |
| `~/.productune/sections/delegation.md` | §PRD delegation cross-ref 2라인 (이미 존재 시 skip) | 조건부 |

### 5.2 변경 없는 파일

| 파일 | 보존 사유 |
|---|---|
| `packages/core/scripts/hooks/pre-chunking-warn.sh` | T-P4-104 `3c6cb35`에서 완료. 변경 불필요. |
| `~/.claude/settings.json` | install.sh 재실행 시 자동 merge. 직접 편집 금지. |

---

## §6 §1.5 Self-Check (Design System UX Principles)

_본 ticket은 스크립트/doctrine 변경 — end-user UI 변화 없음. §1.5 UX principles 해당 없음._

Anti-pattern 점검 (운영 도구 관점):
- ~~"hook 등록 없는 dead script"~~ → install.sh PreToolUse entry 추가로 해소. ✓
- ~~"doctrine과 동작 불일치"~~ → po-instructions.md / delegation.md 텍스트가 hook 동작과 정합. ✓
- ~~"install 재실행 시 중복 entry"~~ → `is_pdt()` basename guard + `strip_pdt()` idempotent 패턴으로 방지. ✓

---

## §7 Dependencies

| 의존성 | 상태 | 비고 |
|---|---|---|
| **T-P4-104 partial** (`3c6cb35`) | landed | `pre-chunking-warn.sh` 존재. 본 ticket 착수 전 확인 필수. |
| `packages/core/scripts/install.sh` L405–462 구조 | 현행 유지 | `merge_claude_settings_hooks()` 함수 signature 변경 없음 가정. |

---

## §8 QA

### 8.1 QA invoke 판단

- **QA invoke: skip** — 기계적 script/doctrine 업데이트. 별도 `type:test` ticket 불필요.

### 8.2 test target

| 대상 | 검증 방법 |
|---|---|
| shellcheck | `shellcheck packages/core/scripts/install.sh` → 0 warning/error |
| jq filter parse | `bash packages/core/scripts/install.sh` 성공 (jq syntax 포함) |
| hook registration | `cat ~/.claude/settings.json \| jq '.hooks.PreToolUse'` → `pre-chunking-warn.sh` entry 보임 |

### 8.3 사용자 dogfood

```bash
# 1. install.sh 재실행
bash packages/core/scripts/install.sh

# 2. PreToolUse 확인
cat ~/.claude/settings.json | jq '.hooks.PreToolUse'
# 기대: pre-chunking-warn.sh entry + pre-delegate-task-check.sh entry 모두 존재

# 3. doctrine 확인
grep "chunking" ~/.productune/po-instructions.md
grep "Dispatch ceiling" ~/.productune/sections/delegation.md
```

### 8.4 회귀 체크

- `pre-delegate-task-check.sh` entry 보존 (PreToolUse 배열 내 첫 번째 entry 소실 없음).
- `PostToolUse` (`post-edit-format.sh`, `post-delegate-state-write.sh`) 항목 보존.
- `PostCompact` (`post-compact-doctrine.sh`) 항목 보존.
- `Stop` (`stop-verify.sh`) 항목 보존.
- install.sh 2회 연속 실행 → `pre-chunking-warn.sh` entry 중복 없음 (idempotent).

---

## §9 Persona Activity

| 날짜 | 작성자 | 내용 |
|---|---|---|
| 2026-05-14 | pdt-designer | T-P4-108 ticket emit. T-P4-104 partial commit `3c6cb35` 확인 — hook script land, 나머지 3 sub-area 미착지. install.sh L405–462 구조 분석: `is_pdt()` basename guard + `strip_pdt()` idempotent 패턴 파악. Pre-area B/C는 현행 파일에 이미 해당 텍스트 존재 가능 — grep 조건부 처리 지시. Sub-area A만 실질 변경(~6 line). §QA scope: skip(기계적 변경), dogfood 시나리오 명시. |
