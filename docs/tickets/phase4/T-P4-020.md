---
ticket_id: T-P4-020
round: phase4-r2
stage: impl
status: todo
assignee: pdt-developer
created_at: 2026-05-04T07:30:00Z
started_at: null
completed_at: null
duration_min: null
estimated_complexity: L4
risk_flags: git, base-branch-protection
branch: null
worktree_path: null
---

# T-P4-020: Ticket-단위 worktree 자동 생성 + base branch 차단

**Round**: phase4-r2  **Stage**: impl  **Status**: todo  **Assignee**: pdt-developer
**PRD anchor**: [docs/prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future](../../prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future) (§10 Git 추상화 레이어)
**Estimated complexity**: L4  **Risk flags**: git, base-branch-protection
**Doctrine**: [`~/.productune/sections/git-workflow.md`](../../../../.productune/sections/git-workflow.md)

## Request

ticket 발행 = `<project>/.productune/worktrees/<ticket-id>/` worktree 자동 생성 + 내부 branch 자동 명명 (`feature/<ticket-id>/<slug-kebab>` 또는 `fix/<ticket-id>/<slug-kebab>`, risk_flags / stage 기반 자동 분류). `git-rules.json` 로드해 prefix 토글 반영. base branch (프로덕션 = 내부 `main`, `useDevBranch=true` 시 검증용 중간 환경 = 내부 `dev` 도) 직접 작업 차단을 GUI 모듈 + `.git/hooks/pre-push` 이중으로 보장. 이 ticket = git-workflow 모듈의 entry point — 후속 T-P4-021/022/023/024 가 모두 본 ticket 의 worktree/rules API 위에 동작.

## Inputs

- PRD: `docs/prd/productune.md` §10 Git 추상화 레이어
- Doctrine: `~/.productune/sections/git-workflow.md` (Worktree-per-ticket / Base branch 직접 작업 차단)
- Plan: `~/.claude/plans/git-gleaming-lemon.md` §3 (PO doctrine 7줄 규약), §"반영 대상 5축" §4 코드
- 기존 코드: `packages/core/src/github.ts` (OAuth + REST), `packages/gui/electron/main.ts` L417-425 `github:setupRemote` (worktree 후 remote 추가에 동일 패턴 재사용)
- Settings 의존: T-P4-024 (`git-rules.json` r/w) — parallel 발행. T-P4-024 미완 시 default 값 (`useDevBranch=false` / `featureBranchPrefix="feature"` / `fixBranchPrefix="fix"`) 으로 동작.
- Deps: T-P4-014 (GitHub OAuth + private repo 생성 + git remote 설정 — completed)

## Acceptance

- [ ] 새 ticket 발행 시 `<project>/.productune/worktrees/<ticket-id>/` worktree 자동 생성됨 (`git worktree add` wrap)
- [ ] 내부 branch 명 = `feature/<ticket-id>/<slug-kebab>` 또는 `fix/<ticket-id>/<slug-kebab>`. 분류 룰: `risk_flags ≠ none && stage ∈ {qa, refactor}` → `fix`, 그 외 → `feature`. prefix 는 `git-rules.json.featureBranchPrefix` / `fixBranchPrefix` 반영.
- [ ] `po-state.json.current_task` 에 `worktree_path` + `branch` 필드 set
- [ ] Ticket frontmatter 에 `branch:` + `worktree_path:` mechanical update
- [ ] 프로덕션 (내부 `main`) 위 자동저장 시도 → GUI 모듈이 거부 + 친절 모달 ("보호된 환경입니다. 작업공간에서 시작해주세요." + [새 작업 시작] CTA). raw git 에러 노출 X.
- [ ] `useDevBranch=true` 토글 시 검증용 중간 환경 (내부 `dev`) 도 동일 차단
- [ ] `productune init` 시 `.git/hooks/pre-push` 자동 설치. base 직접 push 시 exit 1 + 한국어 안내 메시지.
- [ ] `git-rules.json` 로드 — project 우선, 없으면 `~/.productune/git-rules.default.json` fallback
- [ ] ticket close → worktree 는 삭제 X (history 보존). 추후 archive 정책 별도 ticket.
- [ ] `pnpm -r build` (tsc strict + vite build) 통과

## Out of scope

- 자동 commit 메시지 합성 (T-P4-021)
- [배포 준비] / [배포하기] push & PR (T-P4-022)
- 버전 히스토리 카드 UI (T-P4-023)
- Settings 패널 UI (T-P4-024)
- worktree archive / cleanup 정책 (별도 ticket)
- staging 환경 hook (default off, 출시 후 별도)

## Notes

- 모듈 위치: `packages/core/src/git-workflow/worktree.ts` (생성/삭제/list) + `packages/core/src/git-workflow/hooks.ts` (pre-push 설치) + `packages/core/src/git-workflow/rules.ts` (`git-rules.json` 로드/save).
- worktree 는 `<project>/.productune/worktrees/` 하위 — `.gitignore` 자동 등록 필요 (추가 worktree 자체는 git internal, ignore 대상이 아니지만 향후 cache 산출물 위해).
- pre-push hook = bash script. base branch list 는 GUI 가 매번 install 시 갱신 (`useDevBranch` 토글 시 hook 재작성).
- 차단 모달 카피 / 트리거 — design service-flow §3.2 매핑 표 1:1 강제. `worktree` / `dev` / `merge` / `staging` 어휘 사용자 화면 노출 0 검증.
- `current_task.worktree_path` 채우기는 PO mechanical update 화이트리스트 (`tickets.md`).

## Persona Activity

<!-- PO managed, append-only, structured — Result ≤ 80 chars -->
| When | Persona | Model/Effort | Turn | Result |
|---|---|---|---|---|
