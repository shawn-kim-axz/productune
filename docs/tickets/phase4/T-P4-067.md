---
ticket_id: T-P4-067
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-08T00:00:00Z
started_at: 2026-05-08T00:00:00Z
completed_at: 2026-05-08T00:00:00Z
duration_min: 15
estimated_complexity: L1
risk_flags: ux-feedback
bundle: T-P4-R4-fix
slug: chatpanel-restart-visual-feedback
qa_status: pending
qa_loops: 0
---

# T-P4-067: ChatPanel restart visual feedback — §1.5.4 위반 fix

## Background

T-P4-069 audit C-1 critical violation. RestartSessionModal "Restart now" 클릭 후 IPC 호출 + modal 닫힘 + session id reset 다 실행되지만 ChatPanel 본문엔 아무 visual 변화 X. 사용자가 "재시작이 됐는지 안 됐는지" 확인 불가. design-system §1.5.4 anti-pattern 사례 1 (recent dogfood 2026-05-07) 직접 fix.

## Doctrine references

- `docs/design/design-system.md` §1.5.4 Feedback ("모든 사용자 action 은 즉시 visual feedback")
- `docs/design/design-system.md` §9.2 motion (`pdt-spin`)
- `docs/design/T-P4-069-audit.md` §2.C-1 + §3.M-3

## Acceptance

### A) Inline trace 메시지 inject (사용자 결정)

- [ ] `handleRestartNow` 성공 후 (또는 `usePoChat.setClaudeSessionId(null)` + `clearHealth()` 직후) ChatPanel `messages` 에 trace message 1 줄 inject
- [ ] payload: `{ kind: 'trace', author: 'system', text: t('workspace.chat.restartTrace') }` (또는 기존 TraceLine 패턴)
- [ ] i18n key:
  - `en`: `workspace.chat.restartTrace`: `[system] Session restarted`
  - `ko`: `workspace.chat.restartTrace`: `[system] 세션이 재시작됐어요`
- [ ] 메시지 색: `--text-muted` (TraceLine 기존 패턴 그대로)
- [ ] 추가 메시지 발송 시 정상 sequencing — restart trace 가 다음 user prompt 보다 위에 표시

### B) RestartSessionModal busy 상태 시각 보강 (M-3 묶음)

- [ ] `handleRestartNow` 호출 중 (`busy === true`):
  - "Restart now" button label → `t('common.loading')` (이미 적용됨, 유지)
  - **추가**: lucide `Loader2` icon `pdt-spin` 클래스 inline (label 좌측 14×14, gap 6)
- [ ] busy 동안 "Cancel" / "Open Settings" disabled (이미 적용됐는지 dev 검증)
- [ ] 성공 시 modal close + (A) trace inject

### C) 회귀

- [ ] 기존 PO Chat history 보존 — restart 후 이전 messages 그대로 노출
- [ ] PoFab health badge 정상 reset
- [ ] StatusBar SessionHealthSegment `healthy` 복귀

### D) 검증

- [ ] `pnpm tsc --noEmit` exit 0
- [ ] `pnpm -r build` exit 0
- [ ] locale check 통과
- [ ] manual: paepyeong dogfood 시나리오 — restart click → modal close → ChatPanel trace 메시지 노출 확인

## QA scenarios (light)

1. **happy path** — Restart now click → modal close → trace 메시지 inline 노출
2. **busy 중 cancel 차단** — busy 동안 Cancel disabled 확인
3. **i18n** — en/ko 양쪽 trace 문구 정합
4. **회귀** — 이전 messages 손상 X, PoFab badge 정상

## Out of scope

- SessionHealthBanner success variant 노출 (audit fix 권장 #3 — 별 ticket 후보, 본 ticket 은 inline trace 만)
- restart 진행 toast (Phase 5 enhancement)
- §1.5.4 audit 의 다른 컴포넌트 feedback 부재 (별도 cover)

## Notes

- 본 ticket = audit C-1 + M-3 통합. RestartSessionModal busy 시각 보강 (M-3) 은 C-1 과 같은 modal 다루므로 자연 묶음.
- M-2 footer 순서 / M-4 Esc 정책은 T-P4-074 분리.

## Persona Activity

| datetime | persona | action |
|---|---|---|
| 2026-05-08 | pdt-developer | impl: A) i18n restartTrace (en+ko), B) RestartSessionModal Loader2 spinner + Cancel/Settings disabled during busy + appendMessage trace inject on success. tsc --noEmit exit 0, locale check exit 0. |
