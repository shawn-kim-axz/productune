---
ticket_id: T-P4-066
round: phase4-r4-fix
type: impl
status: todo
assignee: pdt-developer
created_at: 2026-05-08T00:00:00Z
estimated_complexity: L4
risk_flags: state-migration, gui-surface-flow
bundle: T-P4-R4-fix
slug: promotion-lifecycle-bug-fix-gui-ipc-impl-drain-surface
qa_status: pending
qa_loops: 0
---

# T-P4-066: Promotion lifecycle bug fix — GUI/IPC impl + Drain surface

## Background

사용자 dogfood 발견: persona 가 `promotion_candidates` 반환했는데 PO 가 inline surface 못 한 case (background sub-agent 결과 mid-turn, turn closed without prompt window) → drop. project / wiki lesson 누적 X. retrospective 가 보여줄 lesson 없음.

5 단계 fix path 의 doctrine 변경은 이미 land (commit `07a3183` + step 5 lifecycle-mechanics.md). 본 ticket = **GUI / IPC / migration 코드 impl**.

## Doctrine references (이미 land)

- `packages/core/po/sections/memory.md` — `pending_promotions[]` schema (11 sub-keys) + `### Persistence (deferred surface)` sub-section
- `packages/core/po/sections/stages.md` — `Step 1b — Drain pending_promotions[]` (4 응답 분기, Cap 5/turn, 7-day stale-drop)
- `packages/core/po/sections/lifecycle-mechanics.md` — Phase 5 retrospective sequence read sources (5 sources) + retrospective.md template (Approved promotions / Repeated patterns / Surfaced for next Version sections)
- 13 페르소나 spec — top-level JSON 의무화

## Acceptance

### A) IPC + state ops

- [ ] `state:appendPendingPromotion(projectDir, candidate)` — persona 응답 후 inline surface 못하면 enqueue. id = `promo-<YYYYMMDD>-<NNN>` 자동 생성.
- [ ] `state:listPendingPromotions(projectDir)` → `{ status, persona, tier, target, delta, rationale, ... }[]` filter `status:"pending"` only
- [ ] `state:resolvePendingPromotion(projectDir, id, status, finalTarget?)` — `approved | dropped | edited`. `decided_at` 자동. `edited` 시 finalTarget 반영
- [ ] `state:autoDropStale(projectDir)` — 7-day 경과 (`surfaced_at` 기준) `status:"pending"` → `dropped` mass update. turn-start 시 sweep
- [ ] atomic write (tmp + rename) — 기존 ui-settings 패턴 정합

### B) GUI Drain surface

- [ ] WorkspaceShell turn-start 또는 별 banner 영역에 pending list surface
- [ ] Cap 5/turn — 초과 항목 다음 turn batch
- [ ] List item: persona + tier + target + delta (truncate) + rationale (small) + buttons `[Save] [Skip] [Edit]`
- [ ] `Save` → resolve(approved) + mechanical write (C 영역)
- [ ] `Skip` 또는 외부 click / Esc → resolve(dropped)
- [ ] `Edit` → 사용자 수정 입력 modal → resolve(edited, finalTarget) + mechanical write
- [ ] >3 항목 numbered (1, 2, 3...) — bulk approve 옵션 (`1,3` 입력 등)
- [ ] i18n: `workspace.promotion.*` prefix, en/ko 동시

### C) Mechanical write 자동 호출

- [ ] tier=`project` → `printf '%s\n' "$DELTA" >> "$TARGET"` (memory.md `### Mechanical writes` 그대로)
- [ ] tier=`wiki` → `WIKI_BACKEND` 따라 분기 (graphiti / keeper / fs)
- [ ] tier=`work-note` → file write `<TARGET>` (없으면 생성)
- [ ] 성공 시 toast `[PO] saved.`, 실패 시 inline error + Retry CTA

### D) Retrospective read flow

- [ ] VersionDetailView 또는 retrospective viewer 가 approved-promotion archive 표시
- [ ] filter: `pending_promotions[].status ∈ {approved, edited} ∧ decided_at ∈ [version.started_at, version.ended_at]`
- [ ] retrospective.md auto-fill: lifecycle-mechanics.md 의 새 template sections (Approved promotions / Repeated patterns / Surfaced for next Version) 채우기

### E) Migration / backward-compat

- [ ] 기존 po-state.json `pending_promotions[]` 부재 → jq `// []` default (read-side fallback)
- [ ] schema_version T-P4-065 의 1→2 migration 안에 통합 (사용자 환경 한 번 migration). 본 ticket 별도 schema_version bump X.
- [ ] sample fixture (ntf-games po-state.json) 에 pending_promotions[] 빈 배열 박아 회귀 검증

### F) 검증

- [ ] `pnpm tsc --noEmit` exit 0
- [ ] `pnpm -r build` exit 0
- [ ] locale check 두 스크립트 OK
- [ ] dogfood: 의도적 background turn 시뮬레이션 (persona 결과 받으면서 PO 가 inline surface 못함) → enqueue → 다음 turn drain → save → mechanical write 검증
- [ ] 기존 inline surface (memory.md `## Promotion gate` 흐름) 회귀 X

## Migration sequence

1. IPC + state ops (`packages/core/src/state/pending-promotions.ts` 신규 또는 기존 파일 보강)
2. preload.ts + main.ts handler 등록
3. WorkspaceShell drain surface UI + i18n
4. mechanical write 자동 호출 (tier 별 분기)
5. retrospective viewer integration (D)
6. 회귀 검증 (E + F)

## QA scenarios (light)

1. **inline-surface** — foreground persona 호출 → 응답 즉시 inline surface → 사용자 응답 → 기존 doctrine 그대로 동작 (회귀 X)
2. **deferred-enqueue** — background sub-agent persona 호출 → 결과 받자마자 PO 가 prompt 못 띄움 → enqueue → 다음 turn drain
3. **approved-write** — drain 에서 Save → mechanical write → file 확인
4. **dropped** — Skip 또는 외부 click → status:"dropped" 정합
5. **edited** — Edit → 수정 입력 → status:"edited" + finalTarget 반영 + write
6. **stale-drop** — pending 7-day 경과 → 자동 dropped (turn-start sweep)
7. **retrospective** — approved archive Version range filter 정합

## Out of scope

- Doctrine 변경 (이미 land)
- 새 페르소나 spec (output rule 이미 박힘)
- Cap 5/turn / 7-day stale 숫자 변경 (dogfood 후 tune)

## Notes

- T-P4-065 의 schema_version 1→2 migration 안에 본 ticket 의 `pending_promotions[]` 부재 fallback 도 묶음. 별도 schema bump X.
- 페르소나 spec 의 `pending_promotions[]` 직접 read X — PO 만 read/write. persona 는 output 만 emit.
