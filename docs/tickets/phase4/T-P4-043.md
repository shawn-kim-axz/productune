---
ticket_id: T-P4-043
round: phase4-r4
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-04T06:05:00Z
started_at: null
completed_at: null
duration_min: null
estimated_complexity: L3
risk_flags: none
bundle: T-P4-R4-workspace-shell
slug: project-tickets-section-ticket-review-tab-main-pane
qa_status: ready
qa_loops: 0
---

# T-P4-043: Project 탭 Tickets section + ticket-review tab (Main pane)

**Round**: phase4-r4  **Stage**: impl  **Status**: todo  **Assignee**: pdt-developer
**Design**: [docs/design/service-flow-and-screens.md §4.1 Project 탭 Rounds](../../design/service-flow-and-screens.md)
**Bundle**: 본 티켓은 `T-P4-R4-workspace-shell` 번들의 일부.

> **2026-05-06 갱신 (mockup-as-source)**: 별도 ticket board 화면 X. Project 탭 의 Rounds → Tickets sub-items + Main 의 `ticket-review` 탭 2단 구조. 클릭 → `openTab(tabId, 'ticket-review', { ticketId })`.
>
> Project 탭 Rounds 섹션 안 Tickets(N) → 각 ticket `pp-row` (indent 46px) + status sbadge → 클릭 시 Main pane 에 `ticket-review` 탭 열림. 별도 보드 화면 없음.

## Acceptance

### Project 탭 — Rounds → Tickets sub-items
- [ ] `pp-sec-hdr` Tickets (N) + 펼치기 화살표
- [ ] 각 ticket `pp-row` — indent 46px + ticket-id label + status `sbadge` (todo gray / in-progress info / review warn / done success / blocked err / abandoned gray strikethrough)
- [ ] 클릭 → `openTab(tabId, 'ticket-review', { ticketId })` → active pane 에 탭 추가

### Main — `ticket-review` 탭
- [ ] `tr-body` — ticket frontmatter 요약 (ticket-id / status / assignee / created_at / estimated_complexity) + ticket body markdown 렌더
- [ ] Persona Activity 표 (`## Persona Activity` 파싱 — persona / model / effort / turn / timestamp 컬럼)
- [ ] 담당 페르소나 chip (persona 색) + model+effort chip
- [ ] `created_at` 절대시각 + `duration_min` (없으면 "—" + warning badge)
- [ ] `tr-actions` footer — [승인] / [수정 요청] / [반려] 버튼 (Design Gate 단계 연동)
- [ ] 산출물 링크 클릭 → 해당 파일 `markdown` 탭 open

## Data source

- `docs/tickets/<round>/T-NNN.md` frontmatter + body parse
- `po-state.json` `past_tickets` / `current_task.persona_session_meta`

## Out of scope

- 티켓 생성 / 편집 UI
- drag-and-drop ticket reorder
- 독립형 ticket board 뷰 (이 슬라이스에서 X)

## Notes

Slice 순서: T-P4-047 (Project 탭 전체) 내부 하위 작업으로 묶는 것 권고 — Rounds 섹션 펼침 로직과 함께 구현.

## Persona Activity

| Time | Persona | Model/Effort | Action | Result |
|---|---|---|---|---|
| 2026-05-06T01:22:14Z | pdt-designer | codex/high | docs-align | mockup-source docs aligned |
