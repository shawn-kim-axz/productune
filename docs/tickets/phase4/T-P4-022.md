---
ticket_id: T-P4-022
version: v0.4-meta-dogfood
round: phase4-r2
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-04T07:32:00Z
started_at: 2026-05-13T09:55:00Z
completed_at: 2026-05-13T10:18:00Z
duration_min: 23
estimated_complexity: L4
actual_complexity: L4
risk_flags: github-api, deploy
branch: feature/T-P4-022/deploy-pr-vercel
worktree_path: .productune/worktrees/T-P4-022/
slug: 2
qa_status: ready
qa_loops: 0
oq_resolutions:
  OQ-T022-1: "(b) Vercel token in settings.json integrations.vercel.token + legacy env fallback"
  OQ-T022-2: "(a) deploy tab singleton via openTab dedup"
  OQ-T022-3: "(c) MVP skip rollback (Phase 5 lock)"
  OQ-T022-4: "(a) auto-merge after mergeability check"
  OQ-T022-5: "(a) Conflict modal Esc/backdrop = abort+preserve"
---

# T-P4-022: [배포 준비] / [배포하기] 2단계 게이트

**Round**: phase4-r2  **Stage**: impl  **Status**: todo  **Assignee**: pdt-developer
**PRD anchor**: [docs/prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future](../../prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future) (§10 [배포 준비] / [배포하기])
**Estimated complexity**: L4  **Risk flags**: github-api, deploy
**Doctrine**: [`~/.productune/sections/git-workflow.md`](../../../../.productune/sections/git-workflow.md) §"2단계 게이트"

## Request

ticket close → 자동저장 commit (push X). 사용자 명시 클릭으로만:
- **[배포 준비]** = ticket worktree push + (toggle 시) 검증용 중간 환경 (내부 `dev`) 매핑. Option A (default) 면 [배포 준비] hidden / no-op.
- **[배포하기]** = 프로덕션 환경 (내부 `main`) PR 자동 생성 + **squash merge** + Vercel deploy 트리거.

PO 자동 결정 X. 사용자 명시 클릭만 트리거. squash 정책 — N개 자동저장 commit → 프로덕션에서 ticket-id summary 1줄. open question (squash vs merge) close 결정 = squash (사용자 화면 누적 메시지 1개로 표시되는 게 자연어 매핑과 정합).

## Inputs

- PRD: `docs/prd/productune.md` §10 [배포 준비] / [배포하기]
- Doctrine: `~/.productune/sections/git-workflow.md` §"2단계 게이트"
- Plan: `~/.claude/plans/git-gleaming-lemon.md` §"권장 — 2 옵션 + default" (Option A default)
- ROADMAP open question close: T-P4-022 squash vs merge → squash (`docs/tickets/phase4/ROADMAP.md` Open questions)
- Deps: T-P4-020 (worktree / rules), T-P4-021 (자동저장)
- 기존 코드: `packages/core/src/github.ts` (REST API + OAuth) — PR 생성에 재사용

## Acceptance

- [ ] [배포 준비] 버튼 (Option B 활성 시만 노출) → ticket worktree branch push + 검증용 중간 환경 (내부 `dev`) 매핑 (auto-merge to dev). conflict 시 자연어 메시지 + [수정 후 다시 시도] CTA.
- [ ] [배포하기] 버튼 (항상 노출) → 프로덕션 PR 자동 생성 (GitHub REST API) + **squash merge** + 머지 후 Vercel deploy 트리거
- [ ] PR title = ticket title, body = ticket Acceptance + Persona Activity 표 요약 (페르소나 trace inline)
- [ ] 사용자 명시 클릭만 트리거. PO turn 안에서 자동 [배포하기] 호출 금지 — code path 차단 + lint
- [ ] Option A (default, `useDevBranch=false`): [배포 준비] hidden, [배포하기] = ticket worktree → 프로덕션 PR 직접
- [ ] Option B (`useDevBranch=true`): [배포 준비] visible, [배포하기] = 검증용 중간 환경 → 프로덕션 PR
- [ ] conflict / API 실패 / Vercel 빌드 실패 → 사용자 친화 메시지 + 다음 액션 (3개 이하)
- [ ] 사용자 화면에서 PR / branch / merge / squash 어휘 노출 0 (lint 통과)
- [ ] `pnpm -r build` 통과

## Out of scope

- 자동 [배포하기] (PO 트리거) — 영원히 사용자 명시 클릭만
- 멀티 ticket 동시 [배포하기] (Phase 5; current = ticket 1개씩)
- staging 환경 (`useStagingEnv=true` — default off, 첫 런칭 후 별도 ticket)
- [배포 취소] / rollback (별도 ticket)
- non-Vercel deploy provider (Round 8)

## Notes

- 모듈 위치: `packages/core/src/git-workflow/deploy.ts` (push + PR 생성 + squash merge + deploy trigger).
- GitHub PR API: `POST /repos/{owner}/{repo}/pulls` + `PUT /repos/{owner}/{repo}/pulls/{n}/merge` with `merge_method: "squash"`.
- Vercel deploy trigger = `packages/core/src/github.ts` 의 webhook 가 자동 — 본 ticket 은 push & merge 까지. Vercel 직접 호출은 Round 8 T-P4-081.
- conflict 메시지 카피 = design service-flow §9 Error 패턴 (3개 버튼: [수정 후 다시 시도] / [다른 작업으로 전환] / [도움말]).
- squash commit 메시지 = `T-NNN: <ticket title>` + body 에 자동저장 N개 요약. ticket 단위 1 commit.
- [배포하기] 클릭 = 사용자 의도 100% 명시. 더블 confirm 모달 (Option A): "프로덕션 환경에 배포하시겠습니까? 되돌리려면 새 작업 ticket 발행이 필요합니다." → [배포] / [취소].

## Persona Activity

<!-- PO managed, append-only, structured — Result ≤ 80 chars -->
| When | Persona | Model/Effort | Turn | Result |
|---|---|---|---|---|
