---
ticket_id: T-P4-041
version: v0.4-meta-dogfood
round: phase4-r4
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-04T06:05:00Z
started_at: null
completed_at: null
duration_min: null
estimated_complexity: L4
risk_flags: streaming, ipc
bundle: T-P4-R4-workspace-shell
slug: po-ui-6-ctx-fab
qa_status: ready
qa_loops: 0
---

# T-P4-041: PO 채팅 UI — 단일 세션 + 6 메시지 타입 + ctx 라인 + FAB

**Round**: phase4-r4  **Stage**: impl  **Status**: todo  **Assignee**: pdt-developer
**PRD anchor**: [docs/prd/productune.md#phase-4](../../prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future)
**Design**: [docs/design/service-flow-and-screens.md §4.3 Right panel](../../design/service-flow-and-screens.md)
**Bundle**: 본 티켓은 `T-P4-R4-workspace-shell` 번들의 일부로 함께 출고.
**Sibling**: T-P4-049 (Persona presence bar — `.rp-ctx` 직하 24px strip; 본 티켓은 그 외 영역 담당).

> **단일 PO 세션 — 프로젝트당 하나, 멀티 채팅방 X** (2026-05-06 결정).
>
> **2026-05-06 갱신 (mockup-as-source)**: Right panel 340px 우측 고정. 6 메시지 타입 좌측 2px accent border-left 색상 구분. `rp-hdr` + `rp-ctx` (stage chip + round-N + active ticket) + `rp-msgs` + `rp-input` 4-영역 구조. Minimize / Close → 우하단 FAB (`💬 PO`) 로 회수. T-P4-042 deprecated, 이 티켓이 단일 세션 기능 전체 담당.
>
> **2026-05-07 갱신**: Persona presence bar (`.rp-persona-bar` 24px) 는 분리 ticket T-P4-049 로 별도 출고. 본 티켓은 `rp-hdr` / `rp-ctx` / `rp-msgs` / `rp-input` 4-영역 그대로.

## Acceptance

### 레이아웃 (340px 고정)
- [ ] `rp-hdr` 35px — `[P]` PO badge (20×20 PO-orange bg) + "PO Chat" 제목 + minimize (`─`) + close (`×`) 버튼
- [ ] `rp-ctx` — stage chip (pill, stage 색 bg/text) + `round-N · T-NNN <action>` 텍스트 (10px caption, 8px gap)
- [ ] `rp-msgs` flex-1 scroll — 메시지 list (아래 6 타입)
- [ ] `rp-input` — `rp-ta` textarea (placeholder "메시지 입력… (/ 명령어)") + `rp-psel` persona selector dropdown + `rp-send` Send button (PO-orange bg)

### 메시지 6 타입 (border-left 2px accent)
- [ ] `po` — `var(--po)` `#FF6B2B` orange border
- [ ] `designer` — `var(--designer)` `#A78BFA` purple border
- [ ] `dev` — `var(--dev)` `#38BDF8` sky border
- [ ] `qa` — `var(--qa)` `#34D399` green border
- [ ] `trace` — border 없음, mono caption gray ("→ delegating …" / "→ new task …")
- [ ] `user` — border 없음, **우측 정렬**, gray-overlay bg
- [ ] 각 메시지 `cm-head` (persona name 페르소나 색 + timestamp) + `cm-bubble` (7px 10px padding, border-radius 0 5 5 5)

### 입력
- [ ] Enter = 전송, Shift+Enter = 줄바꿈
- [ ] `rp-psel` — `@ pdt-po / @ pdt-designer / @ pdt-developer / @ pdt-qa` 선택 (다음 위임 대상)
- [ ] streaming — `claude --output-format stream-json` 또는 SSE; 자동 스크롤 to bottom, 사용자 스크롤 시 잠금

### Minimize / FAB
- [ ] minimize 버튼 → panel width 0 + 우하단 FAB `rp-fab` ("💬 PO") 노출
- [ ] close 버튼 → 동일 동작 (전체 hide)
- [ ] FAB 클릭 → panel 복원

### 저장
- [ ] 세션 메시지 `<projectDir>/.productune/chat.json` 영속화 + 앱 재시작 후 복원
- [ ] markdown 렌더 (bold/italic/code/list)

## Storage

```json
{
  "messages": [...],
  "claude_session_id": "...",
  "updated_at": "..."
}
```

## IPC

- `chat:getSession(projectDir)` → `{ messages, claudeSessionId, updatedAt }`
- `chat:appendMessage(projectDir, message)` → void
- `chat:setClaudeSessionId(projectDir, sessionId)` → void
- `chat:clearSession(projectDir)` → void
- `po:sendMessage(text)` → streaming token events
- `po:onToken(cb)` / `po:onAnnounce(cb)` / `po:onDone(cb)`

## Out of scope

- 멀티 채팅방 (T-P4-042 deprecated)
- Persona presence bar (`.rp-persona-bar` 24px strip) — **T-P4-049 로 분리** (2026-05-07)
- `/command` slash command palette (향후)

## Notes

Streaming 구현체: Electron IPC bridge main → renderer 토큰 push. claude CLI 호출은 main 에서. FAB 위치: 우하단 고정, z-index 최상위. persona selector 는 "다음 메시지의 위임 대상" 의미 — 전송 후 PO 가 delegate 라우팅.

## Persona Activity

| Time | Persona | Model/Effort | Action | Result |
|---|---|---|---|---|
| 2026-05-06T01:22:14Z | pdt-designer | codex/high | docs-align | mockup-source docs aligned |
| 2026-05-07T00:00:00Z | pdt-designer | opus/xhigh | scope-update | persona presence bar T-P4-049 분리, Out of scope 정정 |
