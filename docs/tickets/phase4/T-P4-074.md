---
ticket_id: T-P4-074
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-08T00:00:00Z
completed_at: 2026-05-08T00:00:00Z
duration_min: 8
estimated_complexity: L1
risk_flags: ux-modal
bundle: T-P4-R4-fix
slug: restart-session-modal-escape-footer
qa_status: pending
qa_loops: 0
---

# T-P4-074: RestartSessionModal Esc 닫기 + footer 순서 정합 — §1.5.5 + §8.5

## Background

T-P4-069 audit M-2 + M-4. RestartSessionModal:
- M-2: footer 버튼 순서 [Restart now] [Open Settings] [Cancel] — design-system §8.5 권장 ([Cancel] 좌측, [primary] 우측) 위반
- M-4: Esc 키 binding 부재. session 재시작은 ctx 손실은 있으나 데이터 손실 X 이므로 destructive 가 아님 — 사용자 결정: **Esc 로 닫히게**

## Doctrine references

- `docs/design/design-system.md` §1.5.5 Escape ("모든 modal 은 Esc + 외부 click + Cancel 3 출구")
- `docs/design/design-system.md` §8.5 Modal recipe (footer 버튼 순서)
- `docs/design/T-P4-069-audit.md` §3.M-2 + §3.M-4

## Acceptance

### A) Esc 닫기 (M-4)

- [ ] modal mount 시 `keydown` listener 등록 — `key === 'Escape'` 시 `onClose()` 호출
- [ ] busy 중 (`busy === true`) 일 때 Esc **무시** (restart 진행 중 cancel 방지 — busy 종료 후 Esc 정상 동작)
- [ ] unmount 시 listener cleanup
- [ ] (선택) 외부 click overlay 닫기 — 현재 미구현이면 추가, 이미 있으면 유지
- [ ] focus trap 정합 — modal mount 시 첫 button (또는 Cancel) focus, focus 가 modal 안에 머물기 (기존 패턴 따름)

### B) Footer 순서 정합 (M-2)

design-system §8.5: `[Cancel] [secondary] [primary]` 좌→우.

- [ ] 현 순서 [Restart now] [Open Settings] [Cancel] → [Cancel] [Open Settings] [Restart now]
- [ ] 또는 단순 2 버튼 form 채택: [Cancel] [Restart now] + Settings 링크 본문 inline 으로 강등 (사용자 결정 위임 — dev 1차 plan 단계 제안 후 PO confirm). default = §8.5 그대로 [Cancel] [Open Settings (secondary)] [Restart now (primary)]

### C) i18n 정합 유지

- [ ] 기존 i18n key `workspace.restartModal.{title, body, restartNow, openSettings}` 그대로
- [ ] `common.cancel` 사용 (이미 i18n 존재 가정 — 없으면 추가)

### D) 검증

- [ ] `pnpm tsc --noEmit` exit 0
- [ ] `pnpm -r build` exit 0
- [ ] locale check 통과
- [ ] manual: Esc 시 modal close 확인 (busy 아닐 때) + busy 중 Esc 무시 확인

## QA scenarios (light)

1. **Esc 닫기** — modal open → Esc → close (overlay click 도 동일 경로면 같이)
2. **busy 중 Esc 무시** — Restart now click → busy 동안 Esc 눌러도 close X
3. **focus trap** — Tab 으로 cycle, Shift+Tab 역방향, modal 밖 focus 이동 X
4. **footer 순서** — 좌측 Cancel, 우측 primary
5. **i18n** — en/ko 양쪽 라벨 정합

## Out of scope

- session 재시작 자체를 destructive 분류 변경 (현재 = non-destructive, 사용자 결정 유지)
- 외부 click overlay 닫기 정책 결정 (이미 있으면 유지, 없으면 추가 OK — dev 판단)
- T-P4-067 의 busy spinner / inline trace fix 와 분리 (별 ticket)

## Notes

- T-P4-067 (C-1 fix) + 본 ticket = RestartSessionModal 두 개 ticket 동시 작업 시 conflict 가능 — dev 가 본 ticket 을 T-P4-067 land 후 직렬 처리 권장. 또는 dev 가 두 ticket 함께 한 PR 로 묶어도 OK (chunking 5-10min L1 + L1 = L2 적정).

## Persona Activity

| turn | persona | action | output |
|---|---|---|---|
| 1 | pdt-developer | impl | A) Esc keydown listener (useEffect + cleanup, busy 무시) + Cancel focus on mount (useRef); B) footer 순서 [Cancel][Open Settings][Restart now] §8.5 정합; tsc --noEmit exit 0; locale checks OK |
