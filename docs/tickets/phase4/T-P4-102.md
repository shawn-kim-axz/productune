---
ticket_id: T-P4-102
version: v0.4-meta-dogfood
round: phase4-r4
type: impl
status: todo
assignee: pdt-developer
estimated_complexity: L3
risk_flags: none
slug: v1-tab-is-ticket-dashboard
qa_status: todo
qa_loops: 0
plan_path: docs/design/T-P4-102/plan.md
revises: docs/design/T-P4-097/plan.md §2(a)
created: 2026-05-13
---

# T-P4-102 — v1 탭 = TicketDashboardView (ticket-review reuse, not kanban)

## §1 Background

### 1.1 발단 — 사용자 dogfood 피드백

v0.4-meta-dogfood 진행 중, 사용자가 사이드패널 현재 버전(v1) row를 클릭했을 때 열리는 탭이
"버전 히스토리 — v1" 헤더를 가진 kanban board임을 인식. 이는 두 가지 문제를 동시에 드러냄:

1. **탭 타입 mismatch** — 현재 버전 row가 `version-history` type으로 열리지만, 실제로 보여주는 내용은 ticket dashboard 패턴임.
2. **VersionHistoryView 내부 kanban 분기** — `if (isCurrentVersion)` 블록이 kanban board를 VersionHistoryView 안에 embed시키고 있어 view 책임이 불분명함.

### 1.2 T-P4-097 §2(a) 결정 오류 — 전제가 틀렸다

T-P4-097은 `ticket-review` 재사용(Option A1)을 다음 이유로 기각했다:

> "ticket-review = 단일 ticket detail viewer. 의미 mismatch." → ✘ 부적합

그러나 코드를 확인한 결과:

```ts
// packages/gui/src/components/workspace/main/panes/TicketReviewTab.tsx
export default function TicketReviewTab({ props: _props }: Props) {
  const poState = useWorkspace((s) => s.poState)
  return <TicketDashboardView poState={poState} />
}
```

`ticket-review` tab type의 실제 renderer = **`TicketDashboardView`** (전체 kanban board, 6 status column).
단일 ticket detail viewer가 아니다. 따라서 T-P4-097 §2(a)의 기각 사유 자체가 사실 오류.

### 1.3 올바른 결론

**Option A1 채택**: 현재 버전 row click → `ticket-review:{versionId}` tab open + `versionFilter` prop으로 해당 버전 티켓만 scope. 신규 TabType 추가 없음. 신규 component 없음. VersionHistoryView = 과거 버전 전용 linear card view로 단순화 (~300 line 감소).

---

## §2 Scope

### (a) SidePanelCurrentVersion.onClick — openTab 호출 변경

**변경 위치 2곳**:

1. `SidePanelCurrentVersion.tsx` mount `useEffect` (lines 52–57) — direct `openTab` call
2. `LeftSidebar.tsx` `handleVersionClick(id, true)` path (lines 77–82) — delegated `openTab` call

```ts
// BEFORE (T-P4-097 A3 — 폐기)
openTab(
  `version-current:${versionId}`,   // tab id
  'version-history',                  // tab type ← 잘못됨
  { mode: 'current' },               // props
  versionId,                          // title
)

// AFTER (본 ticket A1 채택)
openTab(
  `ticket-review:${versionId}`,      // tab id
  'ticket-review',                    // tab type ← 올바름
  { versionFilter: versionId },       // props
  versionId,                          // title = "v1" (verbatim version id)
)
```

`version-current:*` tab id pattern은 완전 폐기. 신규 발급 없음.

### (b) TicketDashboardView — `versionFilter` prop 수용

`TicketDashboardView.tsx`에 `versionFilter?: string` prop 추가.
`TicketReviewTab.tsx`가 `tabProps.versionFilter`를 읽어 전달.

```ts
// TicketDashboardView.tsx — Props 변경
interface Props {
  poState: PoState | null
  versionFilter?: string    // 신규 — 설정 시 해당 version 티켓만 표시
}

// allTickets useMemo 수정 (~4 line)
const allTickets = useMemo(() => {
  const raw = collectAllTickets(poState, scannedTickets)
  if (!versionFilter) return raw                             // 기존 전체 board 경로 보존
  return raw.filter((t) => t.version === versionFilter)     // 버전 필터 적용
}, [poState, scannedTickets, versionFilter])
```

```ts
// TicketReviewTab.tsx — versionFilter 전달 (~2 line 추가)
export default function TicketReviewTab({ props: tabProps }: Props) {
  const poState = useWorkspace((s) => s.poState)
  const versionFilter = tabProps?.versionFilter as string | undefined
  return <TicketDashboardView poState={poState} versionFilter={versionFilter} />
}
```

**기존 tab 동작 보존 확인**:

| 진입처 | versionFilter | 동작 |
|---|---|---|
| `ticket-review:board` (activity bar / mount) | undefined | ALL tickets ✓ |
| `ticket-review:{ticketId}` (quick-open) | undefined | ALL tickets ✓ |
| `ticket-review:v1` (현재 버전 row — NEW) | `'v1'` | v1 tickets only ✓ |

**Tab id 충돌 없음**: `'board'` ≠ version regex(`^v\d+`), `'T-P4-xxx'` ≠ version regex. ✓

**defaultTitle 확인**: `ticket-review` case returns `props?.ticketId ?? 'Tickets'`. 신규 경로는 `openTab` 4번째 인자(`versionId`)를 명시 전달 → defaultTitle 폴백 미도달. tab strip 제목 = `v1`. ✓

### (c) VersionHistoryView — isCurrentVersion → kanban 분기 전체 제거

`VersionHistoryView.tsx`에서 아래 항목 전부 삭제 (~300 line 순감소):

| 삭제 대상 | 위치 |
|---|---|
| `if (isCurrentVersion) { return ... }` 블록 | lines ~742–767 |
| `KanbanBoard` function component | lines ~397–437 |
| `KanbanColumn` function component | lines ~368–391 |
| `KanbanCard` function component | lines ~339–361 |
| `KanbanStatus` type alias | line ~272 |
| `KANBAN_COLUMNS` const | line ~274 |
| `kanbanColumnLabelKey()` | lines ~276–283 |
| `kanbanColumnAccentColor()` | lines ~285–292 |
| `personaChipStyle()` | lines ~294–314 |
| `qaChipStyle()` | lines ~316–332 |
| `KanbanCardProps`, `KanbanColumnProps`, `KanbanBoardProps` interfaces | various |
| kanban CSS style objects (`kanbanBoard`, `kanbanColumn`, `kanbanColumnHeader`, …) | lines ~1147–1253 |
| `isCurrentVersion` 변수 선언 및 참조 전체 | — |

**보존 대상**: deploy fetch effect의 `isCurrentVersion` guard → `(!isUnassigned && selectedVersionId)`로 단순화 (OQ-3 default 채택). `isUnassigned` 관련 로직 전체 보존.

**결과**: `VersionHistoryView` = 과거 버전 + 미배정 전용 linear card list view로 단순화. View 책임 명확화.

### (d) workspace store TabType — 변경 없음

- `'version-history'` type: 그대로 보존 (과거 버전 + 미배정 전용).
- `'ticket-review'` type: 그대로 보존 (기존 + 현재 버전 신규 진입점 추가).
- `workspace.ts` `TabType` union: 변경 없음.
- `workspace.ts` `defaultTitle` switch: 변경 없음.
- `workspace.ts` `updateTabId` helper 로직: 변경 없음.

### (e) Tab title — v1 탭 title = versionId, header에 "버전 히스토리" NO

- Tab strip title = `versionId` (예: `"v1"`) — `openTab` 4번째 인자로 명시 전달.
- `TicketDashboardView` header = `t('workspace.tickets.title')` — 기존 그대로. "버전 히스토리" 문자 없음.
- `VersionHistoryView` header = `{t('workspace.versionHistory.title')} — {versionLabel}` — 과거 버전 tab에서만 렌더되므로 그대로 유지.
- **선택적 힌트** (OQ-1 default: 추가 X): `versionFilter` 활성 시 subtitle `"v1 · 작업 N"` 추가는 별도 dogfood 후 결정. 본 ticket 강제 사항 아님.

### (f) updateTabId 호출처 — ticket-review:* 패턴으로 변경

`LeftSidebar.tsx` version rename 처리 경로:

```ts
// BEFORE
updateTabId(`version-current:${prev}`, `version-current:${cv}`, cv)

// AFTER
updateTabId(`ticket-review:${prev}`, `ticket-review:${cv}`, cv)
```

`updateTabId` helper 자체(store)는 변경 없음 — string 비교 기반 in-place swap, id 패턴 무관하게 동작.

### (g) ticket-review 진입점 전수 정합 확인 및 최종 dispatch table

| 진입처 | tab id | props | 변경 여부 |
|---|---|---|---|
| `WorkspaceShell.tsx` activity bar `'tickets'` icon | `ticket-review:board` | `undefined` | 변경 없음 |
| `WorkspaceShell.tsx` mount auto-open | `ticket-review:board` | `undefined` | 변경 없음 |
| `WorkspaceShell.tsx` quick-open palette | `ticket-review:{ticketId}` | `{ ticketId, ticketPath? }` | 변경 없음 |
| `VersionHistoryView.tsx` KanbanBoard.handleOpenTicket | `ticket-review:{ticketId}` | `{ ticketId, ticketPath }` | **삭제** — (c)에 의해 dead code 소멸 |
| **NEW** `SidePanelCurrentVersion.tsx` mount effect | `ticket-review:{versionId}` | `{ versionFilter: versionId }` | **추가** |
| **NEW** `LeftSidebar.tsx` `handleVersionClick(id, true)` | `ticket-review:{versionId}` | `{ versionFilter: versionId }` | **변경** (was `version-history`) |

세 진입 패턴 모두 단일 진입처. 중복 없음.

---

## §3 Acceptance Criteria

1. **현재 버전 row click** → main pane에 `ticket-review:{versionId}` 탭 열림. 이미 열려 있으면 focus.
2. **탭 strip 제목** = versionId 문자열 그대로 (예: `v1`). "버전 히스토리" 문자 없음.
3. **탭 내부 헤더** = `t('workspace.tickets.title')` (예: "작업" / "Tickets"). `VersionHistoryView` header 없음.
4. **kanban board 내용** = 해당 version 티켓만 표시 (`versionFilter` 적용). 다른 버전 티켓은 나타나지 않음.
5. **기존 `ticket-review:board`** (activity bar icon) = ALL tickets 그대로. 동작 변화 없음.
6. **기존 `ticket-review:{ticketId}`** (quick-open) = versionFilter 없음. 기존 동작 유지.
7. **과거 버전 row click** = `version-history` type tab 열림. 기존 linear card view 그대로. kanban 없음.
8. **VersionHistoryView** = `if (isCurrentVersion)` 분기 없음. kanban 관련 component / style / type 코드 전부 삭제됨.
9. **version rename** (`v0.1` → `v1`) 발생 시 열린 `ticket-review:v0.1` tab이 `ticket-review:v1` (title: `v1`)로 in-place swap됨.
10. **신규 TabType 없음.** `workspace.ts` TabType union 변경 없음.
11. **신규 파일 없음.** 기존 5개 파일 변경만.

---

## §4 Out of Scope

| 항목 | 이유 |
|---|---|
| `version-unassigned:main` tab id 분리 | OQ-4 — 별도 turn. 현 scope = current version tab type 수정만 |
| `TicketDashboardView` header에 version label 추가 (`"작업 — v1"`) | OQ-1 default = 추가 X. 별도 dogfood 후 결정 |
| Card click → 개별 ticket tab open | OQ-2 default = 현 board behavior 유지. 추후 dogfood 트리거 시 별도 ticket |
| `TabContent.tsx` dispatcher 변경 | `ticket-review` case 이미 존재. 추가 dispatch 없음 |
| i18n key 추가 | header 변경 없음. 신규 문자열 없음 |
| `SidePanelPastVersions.tsx` 변경 | 과거 버전 path 변경 없음 |
| `WorkspaceShell.tsx` 변경 | `ticket-review:board` 진입점 변경 없음 |
| `VersionRow.tsx` 변경 | row 렌더링 변경 없음. onClick 위임 구조 그대로 |

---

## §5 Module Map

### 5.1 변경 대상 파일 (5개)

| 파일 | 변경 내용 | 규모 |
|---|---|---|
| `packages/gui/src/components/workspace/SidePanelCurrentVersion.tsx` | mount `useEffect` 의 `openTab` 호출: `version-current:${cv}` / `'version-history'` / `{mode:'current'}` → `ticket-review:${cv}` / `'ticket-review'` / `{versionFilter:cv}` | 소 (~5 line) |
| `packages/gui/src/components/workspace/LeftSidebar.tsx` | `handleVersionClick(id, true)` path: `openTab` args 변경 (동일). `updateTabId` 호출: `version-current:${prev}` / `version-current:${cv}` → `ticket-review:${prev}` / `ticket-review:${cv}` | 소 (~6 line) |
| `packages/gui/src/views/VersionHistoryView.tsx` | `if (isCurrentVersion)` block 전체 제거 + kanban sub-components / type aliases / style objects 모두 삭제. `isCurrentVersion` 변수 제거. deploy fetch effect guard → `(!isUnassigned && selectedVersionId)` 단순화 | 대 (~300 line 감소) |
| `packages/gui/src/components/workspace/TicketDashboardView.tsx` | `Props` interface에 `versionFilter?: string` 추가. `allTickets` useMemo에 version filter 로직 추가 | 소 (~8 line) |
| `packages/gui/src/components/workspace/main/panes/TicketReviewTab.tsx` | `tabProps.versionFilter` 읽어 `TicketDashboardView`에 전달 | 극소 (~2 line) |

### 5.2 변경 없는 파일 (명시적 보존)

| 파일 | 보존 사유 |
|---|---|
| `packages/gui/src/store/workspace.ts` | `TabType` union 변경 없음. `ticket-review` 이미 존재. `updateTabId` helper 로직 변경 없음 |
| `packages/gui/src/components/workspace/main/TabContent.tsx` | `case 'ticket-review': return <TicketReviewTab props={tab.props} />` 그대로. dispatcher 추가 없음 |
| `packages/gui/src/components/workspace/SidePanelPastVersions.tsx` | 과거 버전 path 변경 없음. `version-history` type 계속 사용 |
| `packages/gui/src/views/WorkspaceShell.tsx` | `ticket-review:board` 진입점 변경 없음 |
| `packages/gui/src/components/workspace/VersionRow.tsx` | row 렌더링 변경 없음. onClick 위임 구조 그대로 |

### 5.3 신규 파일 없음

신규 component, 신규 view, 신규 TabType: **없음**. 기존 `ticket-review` type + `TicketDashboardView` 재사용 only.

---

## §6 §1.5 Self-Check (Design System UX Principles)

| Sub-rule | 검증 | 결과 |
|---|---|---|
| **§1.5.1 Few Things** | v1 tab = `TicketDashboardView` 그대로. 신규 component 없음. 신규 tab type 없음. 사용자가 배울 새 패턴 없음. `versionFilter` prop으로 scope만 좁힘 — 같은 board 패턴. | ✓ pass |
| **§1.5.2 Familiar** | `ticket-review` tab type = 이미 activity bar `'tickets'` icon에서 열리는 것과 동일. "v1 클릭 → tickets board 열린다" = 직관적. "버전 히스토리 — v1" 낯선 label 제거 → 단순화. | ✓ pass |
| **§1.5.3 Predictability** | 현재 버전 row click → 항상 ticket dashboard (v1 filter). tab title = version id로 명확 구분. 과거 버전 row click → 기존 "버전 히스토리" tab (변경 없음). 동작 일관성 유지. | ✓ pass |
| **§1.5.4 Feedback** | row click → tab open (즉시 시각 변화). selected 표식 변경 (기존 메커니즘). `versionFilter`로 티켓 scope 좁혀지므로 "v1 티켓만 보이는" 상태 명확. | ✓ pass |
| **§1.5.5 Escape** | tab X 클릭 = 닫기 (기존). transient close 시 tab 자동 닫기 없음 — 사용자 명시 close 유지. row click = non-destructive. | ✓ pass |

**Anti-pattern 점검**:

- ~~"action with no visual feedback"~~ → row click 시 tab open 즉각 (기존 동작 그대로). ✓ 없음
- ~~"같은 tab type에 두 가지 다른 header 패턴"~~ → 제거됨. `ticket-review` = 항상 `TicketDashboardView` header. `version-history` = 항상 `VersionHistoryView` header. 명확 분리. ✓ 없음
- ~~"VersionHistoryView가 context에 따라 kanban / linear 두 가지 뷰 전환"~~ → 제거. VersionHistoryView = linear only. ✓ 없음
- ~~"탭 제목 '버전 히스토리'인데 내용은 kanban board" (title-content mismatch)~~ → 제거됨. v1 tab = `ticket-review` type + TicketDashboardView = 의미 일치. ✓ 없음

design-direction `§reduce-surface` 준수: 기존 type/component 재사용, 신규 surface 추가 없음. Tab type count: 변경 없음. VersionHistoryView에서 kanban 코드 ~300 line **순감소**. ✓

---

## §7 Dependencies

| 의존성 | 상태 | 비고 |
|---|---|---|
| **T-P4-097** (SidePanel current/past version row — VersionHistoryView open) | 반드시 land 후 착수 | `SidePanelCurrentVersion.tsx` + `LeftSidebar.tsx` 의 openTab 호출처가 T-P4-097이 신설한 코드임. T-P4-097 merge 확인 후 본 ticket 시작. |
| T-P4-098 (SidePanel 레이아웃 구조) | land 권장 | SidePanel 컴포넌트 구조 안정화 후 작업. |
| T-P4-099 (LeftSidebar handleVersionClick 위임) | land 권장 | handleVersionClick 위임 구조 확인 필요. |
| T-P4-095 (version id regex / naming convention) | 참조만 | `versionFilter` 값은 T-P4-095 version id 형식(`^v\d+(\.\d+)?$`) 따름. store 변경 없음. |

---

## §8 QA

### 8.1 Manual Smoke (post-impl)

| # | 시나리오 | 기대 결과 |
|---|---|---|
| S1 | 사이드패널 현재 버전(v1) row 클릭 | tab strip에 `v1` 탭 열림. 내부 = ticket board. 헤더 = "작업". "버전 히스토리" 문자 없음. |
| S2 | S1 탭 내 kanban board | v1 버전 티켓만 표시. 다른 버전(`v0.1` 등) 티켓 미노출. |
| S3 | activity bar `'tickets'` icon 클릭 | 기존 `ticket-review:board` 탭 = ALL tickets. versionFilter 없음. 동작 변화 없음. |
| S4 | quick-open에서 개별 ticket 열기 | `ticket-review:{ticketId}` 탭 = ALL tickets. versionFilter 없음. |
| S5 | 과거 버전(v0.1) row 클릭 | `version-history` type 탭. linear card view. kanban 없음. 헤더 = "버전 히스토리 — v0.1". |
| S6 | version rename (v0.1 → v1) 후 열린 v0.1 탭 확인 | 탭 id + title이 `v1`으로 in-place swap. 탭 scroll/focus 보존. |
| S7 | 현재 버전 tab 이미 열린 상태에서 row 재클릭 | 중복 탭 열리지 않음. 기존 탭 focus. |
| S8 | `VersionHistoryView` 소스 확인 (코드 리뷰) | `KanbanBoard`, `KanbanColumn`, `KanbanCard`, `isCurrentVersion` 관련 코드 전무. |

### 8.2 회귀 체크

- `ticket-review:board` ALL tickets 동작 (S3) 깨지지 않음.
- `version-history` type tab 과거 버전 linear view (S5) 깨지지 않음.
- TypeScript 컴파일 오류 없음.
- ESLint `no-unused-vars`: `isCurrentVersion` 잔존 없음.

---

## §9 Persona Activity

| 날짜 | 작성자 | 내용 |
|---|---|---|
| 2026-05-13 | pdt-designer | Plan 작성 (`docs/design/T-P4-102/plan.md`). T-P4-097 §2(a) 전제 오류 분석 — `ticket-review` renderer = `TicketDashboardView` (전체 board) 코드 확인으로 증명. Decisions (a)–(g) 전부 A1 채택으로 재정의. 5파일 변경, 신규 파일 없음, TabType 변경 없음. VersionHistoryView kanban ~300 line 순감소. §1.5 self-check 전항목 pass. OQ 4개 (1–3 minor default 채택, 4 scope-out). |
| 2026-05-13 | pdt-designer | 본 ticket emit (T-P4-102). plan→ticket 전환. All OQ resolved with defaults. |
