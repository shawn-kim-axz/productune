---
ticket_id: T-P4-116
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: todo
assignee: pdt-developer
created_at: 2026-05-15T00:00:00Z
estimated_complexity: L2
risk_flags: none
slug: t-p4-112-conflict-resolve
qa_status: todo
qa_loops: 0
parent_ticket: T-P4-112
depends_on:
  - T-P4-112
  - T-P4-113
  - T-P4-114
---

# T-P4-116: T-P4-112 Conflict Resolve — Dev-QA Loop IPC + preload bridge land

> T-P4-112 partial merge (c396216) 에서 충돌 발생 — 4개 파일 main HEAD 유지(T-P4-114/T-P4-113 우선)로
> T-P4-112 의 QA envelope 파싱 + dev-qa loop IPC + preload bridge 변경분이 누락됨.
> 본 ticket = fix-forward — po-runner.ts 에 QA-specific IPC 추가 +
> preload.ts 에 3 bridge 추가 + ChatPanel dead-code 제거. BrowserTab/TabContent 변경 없음.

---

## §1 Background

### 1.1 충돌 사고 경위

| 타임라인 | 내용 |
|:--|:--|
| T-P4-112 work branch | `BrowserTab.tsx` / `po-runner.ts` / `preload.ts` / `TabContent.tsx` 변경분 포함 |
| T-P4-113 merge (선행) | `po-runner.ts` todo IPC + preload `poOnTodoItems` land |
| T-P4-114 merge (선행) | `BrowserTab.tsx` 신규 + `TabContent.tsx` `case 'browser'` + `po-runner.ts` artifact/ticket-focus + preload bridges land |
| c396216 — T-P4-112 partial merge | 4개 충돌 파일 모두 main HEAD 유지 → T-P4-114/T-P4-113 변경 보존 |
| **누락** | T-P4-112 의 QA envelope 파싱 (`browser_url` / `verify_url` / `auth_required`) + 3 IPC 채널 + preload 3 bridge |

### 1.2 현재 코드베이스 상태 (확인됨)

**이미 landed (T-P4-113 + T-P4-114):**

| 파일 | 내용 |
|:--|:--|
| `packages/gui/electron/po-runner.ts` | `parseTodoItems` / `parseTicketFocusItems` / `parseArtifactFiles` + `onTodoItems` / `onTicketFocus` / `onArtifactOpen` callbacks + `po:todo-items` / `po:ticket-focus` / `po:artifact-open` IPC |
| `packages/gui/electron/preload.ts` | `poOnTodoItems` / `poOnTodoDismiss` / `poOnTicketFocus` / `poOnArtifactOpen` / `browserOpened` bridges |
| `packages/gui/src/views/WorkspaceShell.tsx` | `api.poOnArtifactOpen` subscription — artifact auto-open (docs/*.md max 3, T-P4-114 §A) |
| `packages/gui/src/store/useArtifacts.ts` | ArtifactFile store ✅ (partial merge landed) |
| `packages/gui/src/store/useQaLoop.ts` | QaLoopEntry store ✅ (partial merge landed) |

**ChatPanel.tsx T-P4-112 block (partial merge landed — broken):**

```
lines 185–260: Plan-Do-See IPC subscriptions
  api?.onArtifactFiles  → ❌ preload bridge 없음 (dead code — WorkspaceShell covers it)
  api?.onBrowserOpen    → ❌ preload bridge 없음
  api?.onUserVerify     → ❌ preload bridge 없음
  api?.onQaLoopUpdate   → ❌ preload bridge 없음
```

**Missing in po-runner.ts:**

- `onBrowserOpen` / `onUserVerify` / `onQaLoopUpdate` callbacks in `RunCallbacks`
- `parseQaEnvelope()` — QA persona result 파싱 (`browser_url`, `verify_url`, `auth_required`, `qa_loops`, `qa_status`)
- `emitToWebContents()` 에 `po:browser-open` / `po:user-verify` / `po:qa-loop-update` emit

---

## §2 Scope

### A. BrowserTab.tsx / TabContent.tsx — 변경 없음

T-P4-114 land BrowserTab API 그대로 사용. `TabContent.tsx` `case 'browser'`도 T-P4-114 그대로 OK.

---

### B. `po-runner.ts` — QA envelope 파싱 + 3 callback + 3 IPC emit

T-P4-113 의 `onTodoItems` / T-P4-092 worktree wire 위에 순수 additive 추가.

#### B-1. `RunCallbacks` 인터페이스 확장

```ts
interface RunCallbacks {
  // ... 기존 T-P4-113 / T-P4-114 callbacks 유지 ...

  /** T-P4-116: QA envelope browser_url 감지 → browser tab auto-open. */
  onBrowserOpen: (
    url: string,
    ticketId: string,
    purpose: 'qa-smoke' | 'user-verify',
  ) => void

  /** T-P4-116: QA pass + verify_url 감지 → user-verify flow. */
  onUserVerify: (
    url: string | undefined,
    description: string,
    ticketId: string,
  ) => void

  /** T-P4-116: QA loop 상태 변화 감지 → BackgroundTaskSegment badge 갱신. */
  onQaLoopUpdate: (entry: {
    ticketId: string
    attempt: number
    maxAttempts: number
    status: 'dev-running' | 'qa-running' | 'pass' | 'fail' | 'capped' | 'auth-required'
    lastFailReason?: string
  }) => void
}
```

#### B-2. `parseQaEnvelope()` 신규 함수

```ts
interface QaEnvelope {
  persona?: string
  ticket_id?: string           // QA persona가 대상 ticket 명시
  qa_status?: 'pass' | 'fail' | 'running'
  qa_loops?: number            // 현재 retry 횟수 (1-based)
  browser_url?: string | null
  verify_url?: string | null
  verify_description?: string | null
  auth_required?: {
    service: string
    instruction: string
    type: 'manual' | 'oauth' | 'env-var'
  } | null
  fail_reason?: string | null
}

/**
 * QA persona 결과 텍스트에서 QA envelope 추출.
 * 판별 조건: persona === 'pdt-qa' OR qa_status 키 존재 OR browser_url 키 존재.
 * 없으면 null 반환 (noop).
 */
function parseQaEnvelope(text: string): QaEnvelope | null {
  for (const candidate of extractJsonCandidates(text)) {
    try {
      const parsed: unknown = JSON.parse(candidate)
      if (!parsed || typeof parsed !== 'object') continue
      const obj = parsed as Record<string, unknown>
      if (
        obj.persona === 'pdt-qa' ||
        obj.qa_status !== undefined ||
        obj.browser_url !== undefined
      ) {
        return obj as QaEnvelope
      }
    } catch { /* ignore */ }
  }
  return null
}
```

#### B-3. `type === 'result'` 블록에 QA envelope emit 추가

기존 `parseTodoItems` / `parseTicketFocusItems` / `parseArtifactFiles` 호출 뒤에 추가:

```ts
// T-P4-116: QA envelope dispatch
const qaEnv = parseQaEnvelope(resultText)
if (qaEnv) {
  const ticketId = typeof qaEnv.ticket_id === 'string' ? qaEnv.ticket_id : ''

  // C-2: browser_url 있으면 browser-open
  if (typeof qaEnv.browser_url === 'string' && qaEnv.browser_url) {
    cb.onBrowserOpen(qaEnv.browser_url, ticketId, 'qa-smoke')
  }

  // E-2: qa_status === 'pass' → user-verify
  if (qaEnv.qa_status === 'pass') {
    cb.onUserVerify(
      typeof qaEnv.verify_url === 'string' ? qaEnv.verify_url : undefined,
      typeof qaEnv.verify_description === 'string'
        ? qaEnv.verify_description
        : '구현 결과 확인',
      ticketId,
    )
  }

  // B-2: qa_loops 또는 qa_status 변화 → qa-loop-update
  if (qaEnv.qa_loops !== undefined || qaEnv.qa_status !== undefined) {
    const rawStatus = qaEnv.qa_status
    const loopStatus: QaLoopStatus =
      rawStatus === 'pass'    ? 'pass'       :
      rawStatus === 'fail'    ? 'fail'       :
      rawStatus === 'running' ? 'qa-running' :
      'qa-running'
    cb.onQaLoopUpdate({
      ticketId,
      attempt: typeof qaEnv.qa_loops === 'number' ? qaEnv.qa_loops : 1,
      maxAttempts: 3,
      status: loopStatus,
      lastFailReason: typeof qaEnv.fail_reason === 'string'
        ? qaEnv.fail_reason
        : undefined,
    })
  }

  // D-2: auth_required → po:todo-items (기존 onTodoItems 채널 재사용)
  if (qaEnv.auth_required && typeof qaEnv.auth_required === 'object') {
    const { service, instruction, type } = qaEnv.auth_required
    cb.onTodoItems([{
      id: `qa-auth-${ticketId}-${Date.now()}`,
      description: `인증 필요: ${service} — ${instruction}`,
      type: type === 'env-var' ? 'text-input' : 'check',
    }])
  }
}
```

`QaLoopStatus` import: `useQaLoop` store type 과 일치해야 하므로 po-runner.ts 에 로컬 타입 또는 별도 import. po-runner 는 main process이므로 store import 불가 — `'dev-running' | 'qa-running' | 'pass' | 'fail' | 'capped' | 'auth-required'` 로컬 정의 또는 인라인 string union 사용.

#### B-4. `emitToWebContents()` 3 IPC emit 추가

```ts
export function emitToWebContents(wc: WebContents): RunCallbacks {
  return {
    // ... 기존 T-P4-113/T-P4-114 emit 유지 ...

    // T-P4-116: QA loop IPC
    onBrowserOpen: (url, ticketId, purpose) =>
      wc.send('po:browser-open', { url, ticketId, purpose }),
    onUserVerify: (url, description, ticketId) =>
      wc.send('po:user-verify', { url, description, ticketId }),
    onQaLoopUpdate: (entry) =>
      wc.send('po:qa-loop-update', entry),
  }
}
```

---

### C. `preload.ts` — 3 bridge 추가 + ChatPanel dead-code 정리

#### C-1. 3 신규 bridge 추가

```ts
// ── QA loop IPC (T-P4-116) ────────────────────────────────────────────────────

/** QA envelope browser_url 감지 시 emit — browser tab auto-open trigger. */
onBrowserOpen: (cb: (payload: {
  url: string
  ticketId: string
  purpose: 'qa-smoke' | 'user-verify'
}) => void) => {
  const listener = (_e: Electron.IpcRendererEvent, payload: any) => cb(payload)
  ipcRenderer.on('po:browser-open', listener)
  return () => ipcRenderer.removeListener('po:browser-open', listener)
},

/** QA pass + verify_url 감지 시 emit — user-verify flow trigger. */
onUserVerify: (cb: (payload: {
  url?: string
  description: string
  ticketId: string
}) => void) => {
  const listener = (_e: Electron.IpcRendererEvent, payload: any) => cb(payload)
  ipcRenderer.on('po:user-verify', listener)
  return () => ipcRenderer.removeListener('po:user-verify', listener)
},

/** QA loop 상태 갱신 — BackgroundTaskSegment attempt badge 갱신용. */
onQaLoopUpdate: (cb: (payload: {
  ticketId: string
  attempt: number
  maxAttempts: number
  status: 'dev-running' | 'qa-running' | 'pass' | 'fail' | 'capped' | 'auth-required'
  lastFailReason?: string
}) => void) => {
  const listener = (_e: Electron.IpcRendererEvent, payload: any) => cb(payload)
  ipcRenderer.on('po:qa-loop-update', listener)
  return () => ipcRenderer.removeListener('po:qa-loop-update', listener)
},
```

#### C-2. `onArtifactFiles` dead-code 처리

ChatPanel `lines 192–207` 의 `api?.onArtifactFiles` 구독은 preload bridge 가 없어 **noop** 상태. `WorkspaceShell.tsx` 가 `api.poOnArtifactOpen` (T-P4-114 §A) 으로 동일 기능을 이미 커버.

**처리**: `ChatPanel.tsx` 의 해당 `offArtifactFiles` 블록 제거 (`pushArtifactFiles` / `markArtifactOpened` 역시 ChatPanel 내에서 더 이상 사용하지 않으면 import 제거). `WorkspaceShell` 의 T-P4-114 artifact 구독은 유지.

---

### D. TabContent.tsx — 변경 없음

T-P4-114 가 `case 'browser'` 를 이미 추가. T-P4-112 의 추가 case 없음. 그대로 OK.

---

### E. 사용자 dogfood 조건 (E.목표)

`pnpm dev` → PO 에게 impl ticket 작업 지시 → dev 완료 → QA 자동 dispatch:
1. QA 가 `browser_url` 포함 envelope 반환 → main panel 에 browser tab 자동 열림
2. QA fail → `attempt N/3` BackgroundTaskSegment badge (amber) 갱신
3. QA pass + `verify_url` → browser tab "확인 필요" 열림 + TodoChip badge +1

---

## §3 Acceptance

### B. po-runner.ts
- [ ] `RunCallbacks` 에 `onBrowserOpen` / `onUserVerify` / `onQaLoopUpdate` 3 callback 추가. TypeScript strict 통과.
- [ ] QA envelope JSON (`persona === 'pdt-qa'` or `qa_status` key) 포함 result → `parseQaEnvelope` 파싱 성공
- [ ] `browser_url` 있음 → `po:browser-open` IPC emit. `browser_url === null` or absent → noop
- [ ] `qa_status === 'pass'` → `po:user-verify` IPC emit (`verify_url` null 이면 url 없이 emit)
- [ ] `qa_loops` / `qa_status` 있음 → `po:qa-loop-update` IPC emit (attempt/maxAttempts/status 포함)
- [ ] `auth_required` 있음 → `po:todo-items` IPC emit (기존 `onTodoItems` 채널 재사용). `type: 'env-var'` → `'text-input'`, 그 외 → `'check'`
- [ ] QA envelope 미포함 result → 기존 `parseTodoItems` / `parseTicketFocusItems` / `parseArtifactFiles` 동작 회귀 없음

### C. preload.ts
- [ ] `onBrowserOpen` bridge: `po:browser-open` IPC 구독 + unsubscribe 함수 반환
- [ ] `onUserVerify` bridge: `po:user-verify` IPC 구독 + unsubscribe 함수 반환
- [ ] `onQaLoopUpdate` bridge: `po:qa-loop-update` IPC 구독 + unsubscribe 함수 반환
- [ ] ChatPanel `api?.onArtifactFiles` dead-code 제거 → `pnpm tsc --noEmit` 경고 없음

### E. 통합
- [ ] `pnpm dev` 기동 후 PO QA 자동 dispatch 시 ChatPanel `onBrowserOpen` / `onUserVerify` / `onQaLoopUpdate` 구독이 실제 이벤트 수신
- [ ] `useQaLoop.setEntry` 호출 → BackgroundTaskSegment popup row 에 `attempt N/3` badge 반영 (amber token)
- [ ] QA pass → TodoChip count badge +1 (verify todo push 확인)
- [ ] `pnpm -r build` + TypeScript strict 통과

---

## §Out of scope

- **BrowserTab.tsx 변경** — T-P4-114 land 그대로. webview / nav bar API 변경 없음.
- **TabContent.tsx 변경** — T-P4-114 `case 'browser'` 그대로. 추가 case 없음.
- **useArtifacts / useQaLoop 스토어 변경** — partial merge 에서 이미 landed. 스펙 변경 없음.
- **SidePanelArtifacts.tsx 구현** — T-P4-112 원본 scope (§2 A-5). 본 ticket 범위 밖. 추후 별도 ticket.
- **BackgroundTaskSegment.tsx QA badge UI** — T-P4-112 원본 scope (§2 B-3). `useQaLoop` store IPC wire만 본 ticket. UI 렌더 별도 ticket 또는 same developer 자율 판단으로 추가 가능 (L1).
- **Status type `'user-verify'` 추가** — T-P4-112 원본 scope (§2 F-1). types.ts 변경 별도 ticket 또는 T-P4-112 미완 항목으로 처리.
- **pdt-developer.md / pdt-qa.md envelope doctrine 갱신** — 본 ticket 은 IPC wire 만. doctrine 갱신은 T-P4-112 parent scope.
- **`pnpm dev` spawn (C-5)** — T-P4-112 §2 C-5 별도 scope. 본 ticket 범위 밖.
- **StreamIPC 중 inject 큐잉** — T-P4-113 out-of-scope 그대로.

---

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | `po-runner.ts` QA envelope 파싱 (parseQaEnvelope) + 3 IPC emit + `preload.ts` 3 bridge 노출 |
| **사용자 dogfood** | pnpm dev → PO 에게 impl ticket 지시 → dev 완료 → QA 자동 dispatch + browser link 자동 열림 확인 (§E) |
| **regression check** | `po-runner.ts` 기존 `parseTodoItems` / `parseTicketFocusItems` / `parseArtifactFiles` 동작 회귀 없음. `preload.ts` 기존 IPC bridge (todo / ticket-focus / artifact-open) 동작 회귀 없음. |

---

## §5 Module Map

| 파일 | 변경 유형 | 내용 |
|:--|:--|:--|
| `packages/gui/electron/po-runner.ts` | **수정** — additive | `RunCallbacks` 에 `onBrowserOpen` / `onUserVerify` / `onQaLoopUpdate` 추가. `parseQaEnvelope()` 신규 함수. `type === 'result'` 블록에 QA emit 로직 추가. `emitToWebContents()` 에 3 채널 추가. |
| `packages/gui/electron/preload.ts` | **수정** — additive | `onBrowserOpen` / `onUserVerify` / `onQaLoopUpdate` bridge 3개 추가. |
| `packages/gui/src/components/workspace/ChatPanel.tsx` | **수정** — removal | `api?.onArtifactFiles` dead-code 블록 제거 (WorkspaceShell T-P4-114 커버). `pushArtifactFiles` / `markArtifactOpened` import 불필요 시 제거. |

**변경 규모**: 수정 파일 3 (additive + removal). 신규 파일 없음. 총 예상 변경 줄 ≈ 80–100줄.

### IPC event bus 추가 맵 (T-P4-116)

| IPC channel | 방향 | 소스 | 수신처 | payload |
|:--|:--|:--|:--|:--|
| `po:browser-open` | main → renderer | po-runner.ts (`onBrowserOpen`) | ChatPanel useEffect (`api.onBrowserOpen`) | `{ url, ticketId, purpose: 'qa-smoke' \| 'user-verify' }` |
| `po:user-verify` | main → renderer | po-runner.ts (`onUserVerify`) | ChatPanel useEffect (`api.onUserVerify`) | `{ url?: string, description: string, ticketId: string }` |
| `po:qa-loop-update` | main → renderer | po-runner.ts (`onQaLoopUpdate`) | ChatPanel useEffect (`api.onQaLoopUpdate`) | `{ ticketId, attempt, maxAttempts, status, lastFailReason? }` |

---

## §6 §1.5 UX self-check

| 원칙 | 판정 | 근거 |
|:--|:--|:--|
| **Few Things** | ✅ | IPC wire 전용 변경. UI 추가 없음. ChatPanel dead-code 제거로 오히려 감소. |
| **Familiar** | ✅ | 기존 IPC 패턴 (po:todo-items, po:ticket-focus 등) 과 동일 구조 재사용. |
| **Predictability** | ✅ | `browser_url` → browser tab 열림. `qa_status: pass` → verify flow. `auth_required` → todo push. 1:1 매핑 명확. |
| **Feedback** | ✅ | QA 결과 즉시 IPC → UI 즉각 반영 (browser tab, todo badge). |
| **Escape** | ✅ | 모든 IPC 구독에 unsubscribe 함수 반환 (메모리 누수 없음). |

위반 없음.

---

## §7 Open Questions

| # | 질문 | 권장 결정 |
|:--|:--|:--|
| OQ-1 | QA envelope 에 `ticket_id` 필드가 실제로 포함될지? doctrine 미업데이트 상태. | `qaEnv.ticket_id ?? qaEnv.ticketId ?? ''` 로 graceful fallback. 빈 문자열이면 IPC 는 emit 하되 GUI 에서 tabId 생성 시 fallback 처리. doctrine 업데이트 별도 (T-P4-112 parent). |
| OQ-2 | `parseQaEnvelope` 파싱 — PO result 에 dev persona + QA persona 결과가 함께 있을 때 양쪽 all parse 되지 않도록 persona 필드 guard 필요한지? | `persona === 'pdt-qa'` 우선 판별 후 qa_status 기반 fallback. dev envelope 은 `changed_files` 가 있으나 `qa_status` / `browser_url` 없으므로 오탐 낮음. |
| OQ-3 | BackgroundTaskSegment QA attempt badge 렌더 — 본 ticket scope 밖이나, 동일 개발자가 L1 수준으로 추가할지? | 본 ticket Acceptance 에 포함하지 않음. `useQaLoop` store 에 데이터만 wire. BackgroundTaskSegment 수정은 자율 판단 (L1) 또는 별 ticket. |
| OQ-4 | ChatPanel `offArtifactFiles` 제거 시 `pushArtifactFiles` / `markArtifactOpened` Zustand action 이 ChatPanel 에서만 사용했다면 import 제거. 다른 곳(SidePanelArtifacts 등)에서 사용 여부 glob 확인 필요. | 구현자가 glob 확인 후 미사용 import 제거. useArtifacts store 자체는 유지. |

---

## §8 Dependencies

| Ticket | Status | 관계 | Blocking? |
|:--|:--|:--|:--|
| **T-P4-112** | done (partial) | 본 ticket 의 parent — conflict 로 누락된 변경분 land. `useArtifacts` / `useQaLoop` store, ChatPanel 구독 코드 이미 landed. | Non-blocking — 본 ticket 이 wire 만 추가 |
| **T-P4-113** | done | `po:todo-items` IPC + `onTodoItems` callback + `useUserTodo.pushItems` — T-P4-116 §B-3 D-2 (auth_required → todo) 에서 기존 채널 재사용. | Non-blocking (landed) |
| **T-P4-114** | done | `BrowserTab.tsx` + `TabContent.tsx` `case 'browser'` + `po:artifact-open` — T-P4-116 A. 변경 없음 그대로 사용. WorkspaceShell artifact 구독 커버 → ChatPanel dead-code 제거 근거. | Non-blocking (landed) |
| **T-P4-092** | done | `worktree:create` IPC + preload bridge — po-runner.ts 같은 파일 수정. 충돌 없음 (별 섹션 additive). | Non-blocking (landed) |

---

## §9 Persona Activity

<!-- PO managed, append-only -->
| When | Persona | Model/Effort | Turn | Result |
|:--|:--|:--|:--|:--|
| 2026-05-15 | pdt-designer | opus/low | 1 | T-P4-116 ticket emit — T-P4-112 conflict resolve, QA IPC wire (po-runner + preload), ChatPanel dead-code removal (L2) |
