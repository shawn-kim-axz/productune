---
ticket_id: T-P4-131
title: "G1 — design-system / wireframe / mockup author skill (productune self-authored)"
version: v0.4-meta-dogfood
round: phase4-r4
type: doctrine
status: todo
assignee: pdt-designer
estimated_complexity: L2
model: opus
effort: medium
risk_flags: none
slug: skill-g1-design-author
area_tags: [doctrine/skills, design/skill-author]
depends_on: [T-P4-124]
created_at: 2026-05-19
---

## §1 Request

**Origin**: T-P4-124 OQ-K gap candidate G1 — pdt-designer `## Skills` section currently reads `(none currently)` after `mattpocock/design-an-interface` was dropped (2026-05-19). G1 fills the gap with a productune-native skill authored in-house.

**Scope**: author `~/.claude/skills/productune/design-system-author/SKILL.md` — self-executed by pdt-designer at impl turn. Supplementary reference doc optional at designer discretion.

**Why needed**: pdt-designer has no skill loaded at dispatch time → pattern-matching for design-system creation, wireframe spec, and hi-fi mockup HTML/CSS is purely from base model. A `design-system-author` skill provides:
- Canonical token-naming conventions (spacing / color / typography / radius scales)
- Wireframe skeleton patterns (ASCII + Excalidraw JSON conventions)
- Hi-fi mockup recipe (HTML/CSS structure, data-token attributes, responsive breakpoints)
- productune DS vocabulary (`service-design-system.md` anchors)

## §2 Acceptance

### A1. Skill file authored
`~/.claude/skills/productune/design-system-author/SKILL.md` exists with valid frontmatter:
```yaml
name: design-system-author
description: >
  Productune design system token conventions, wireframe skeleton patterns
  (ASCII + Excalidraw JSON), and hi-fi mockup recipe (HTML/CSS). Use when
  creating or updating DS docs, flow diagrams, or mockup screens.
personas: [designer]
```

### A2. Skill body covers 4 areas
1. **Token doctrine** — spacing / color / typography / radius naming convention (consistent with `docs/design/design-system.md` §2–§9)
2. **Wireframe recipe** — ASCII skeleton layout rules + Excalidraw JSON scaffolding pattern
3. **Hi-fi mockup recipe** — HTML/CSS structure, `data-token` attribute convention, key responsive breakpoints
4. **UX §1.5 checklist** — Few Things / Familiar / Predictability / Feedback / Escape embedded as inline checklist for self-review before emitting design doc

### A3. Designer agent `## Skills` section updated
`packages/core/agents/variants/graphiti/pdt-designer.md` + `~/.claude/agents/pdt-designer.md` (home mirror):
- Replace `(none currently — ...)` note with:
  `- productune/design-system-author — DS tokens / wireframe / hi-fi mockup recipe.`
- Retain the `(T-P4-124 OQ-K: G1 shipped ...)` one-line note for audit trail.

### A4. Both mirrors in sync
Repo (`packages/core/agents/variants/graphiti/pdt-designer.md`) and home mirror (`~/.claude/agents/pdt-designer.md`) identical after update.

## §Out of scope

- Updating `po-instructions.md` — designer skills are not referenced by PO glob patterns (PO skills auto-match by persona attribute in skill frontmatter, not filename glob).
- Authoring the actual design docs / mockups for any feature — skill is a recipe doc, not a deliverable doc.
- Batch 2 other gaps (G2, G3, G4, G7, G9, G10) — separate tickets.

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | — |
| **사용자 dogfood** | pdt-designer dispatch 후 SkillMatrixTab 에서 `design-system-author` 행 노출 확인 (persona scope = designer). |
| **regression check** | `~/.claude/agents/pdt-designer.md` `## Skills` section — repo + home mirror 동일 확인. |

## §Open Questions

- OQ-1: Skill body length cap — T-P4-126 doctrine 100-line cap은 main agent file 기준. Skill md 는 별도 파일이므로 cap 없음. 단 간결성 유지 (200 lines 이하 권장).
- OQ-2: supplementary reference doc (`~/.claude/skills/productune/design-system-author/EXAMPLES.md`) 필요 여부 — impl turn 에서 designer 판단.

## §Impl notes (for designer at impl turn)

1. Read `docs/design/design-system.md` §2–§9 for token naming source of truth before authoring skill body.
2. Path: `~/.claude/skills/productune/design-system-author/SKILL.md` — `mkdir -p` via Write (Write tool creates parent dirs as needed).
3. After writing SKILL.md, update both designer agent md files (Read first → Write).
4. No plan.md needed (L2 trivial — this ticket IS the spec).

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-19T00:00Z | pdt-designer | sonnet/high | 1 | ticket emit (backlog entry) |
