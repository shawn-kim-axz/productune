---
ticket_id: T-P4-152
title: "Agent doctrine compress — caveman lite + sub-file extraction (token-opt-3)"
version: v0.4-meta-dogfood
round: phase4-r4
type: impl
status: todo
assignee: pdt-developer
estimated_complexity: L3
model: sonnet
effort: high
risk_flags: none
slug: agent-doctrine-compress-token-opt-3
area_tags: [agent-doctrine, token-opt]
depends_on: [T-P4-150]
plan_path: docs/design/T-P4-152/plan.md
created_at: 2026-05-20
---

# T-P4-152 — Agent doctrine compress (token-opt-3)

## §1 Request

Compress all 9 persona variant files to ≤100 lines each. Three shared verbose sections
extracted to new `packages/core/po/sections/_details/` sub-files (auto-picked up by
`bootstrap-doctrine.sh` + `init.ts` — no code changes). One truncated file
(`graphiti/pdt-developer.md`) restored and compressed.

Expected cache_creation savings: 30-40% per fresh persona dispatch (~$0.375 → $0.25).

**Must run after T-P4-150** (that ticket adds ~3-4 lines/file to output format sections;
T-P4-152 compressions are sized to absorb those additions).

Full specification: `docs/design/T-P4-152/plan.md`.

---

## §2 Acceptance

### A1 — All 9 variant files ≤100 lines (post-T-P4-150)

```bash
wc -l packages/core/agents/variants/{graphiti,keeper,fs}/pdt-{designer,developer,qa}.md
# All must print ≤100
```

### A2 — Three new sub-files created

```bash
ls packages/core/po/sections/_details/{promotion-rule,qa-scope-table,qa-gui-fields}.md
# All three files must exist with content per plan §A1–§A3
```

### A3 — `graphiti/pdt-developer.md` complete

File has:
- `## Memory promotion` section with **project**, **work-note**, and **wiki (Graphiti)** tiers
- Promotion rule ref line (sub-file A1)
- Wiki write gate caveman (plan §C)
- `## Refuse rules` section

### A4 — Key sections preserved in all compressed files

Each compressed file must still contain:
- YAML frontmatter (name, tools, model, permissionMode, color)
- `## Effort matrix` table
- `## Memory` section
- `## Output format` JSON schema (with T-P4-150 additions)
- `## Refuse rules`

### A5 — User-global mirrors present

```bash
ls ~/.productune/sections/_details/{promotion-rule,qa-scope-table,qa-gui-fields}.md
# All three mirror files must exist
```

### A6 — Bootstrap sub-file count

```bash
find packages/core/po/sections -type f -name '*.md' | grep _details | wc -l
# Must be ≥ 13 (10 existing + 3 new)
```

### A7 — Build passes

```bash
pnpm -F core build
# Exit 0 (sanity gate — no TS changes but confirm no inadvertent side effects)
```

---

## §Inputs

- `docs/design/T-P4-152/plan.md` — full specification (sub-file content, per-file cuts)
- `packages/core/agents/variants/graphiti/pdt-designer.md` (read + edit — B1)
- `packages/core/agents/variants/keeper/pdt-designer.md` (read + edit — B5)
- `packages/core/agents/variants/fs/pdt-designer.md` (read + edit — B6)
- `packages/core/agents/variants/graphiti/pdt-qa.md` (read + edit — B2)
- `packages/core/agents/variants/keeper/pdt-qa.md` (read + edit — B4)
- `packages/core/agents/variants/fs/pdt-qa.md` (read + edit — B3)
- `packages/core/agents/variants/graphiti/pdt-developer.md` (read + edit — B7)
- `packages/core/agents/variants/fs/pdt-developer.md` (read — B7 reference for missing content)
- `packages/core/agents/variants/keeper/pdt-developer.md` (read — B7 reference for missing content)
- `packages/core/po/sections/_details/` directory (create 3 new sub-files)
- `~/.productune/sections/_details/` directory (create 3 mirror sub-files)

---

## §Out of scope

- `pdt-po.md` — PO scope; external-doctrine pattern already in use
- `pdt-wiki-keeper.md` (97 lines) — already under cap; no-touch
- `fs/pdt-developer.md`, `keeper/pdt-developer.md` — under cap post-T-P4-150; reference only
- Semantic content changes to any section (compress + verbatim extract only)
- Changes to `bootstrap-doctrine.sh` or `init.ts` (already handle `_details/` recursively)
- Cache measurement infrastructure

---

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `manual smoke only` |
| **test target** | `wc -l packages/core/agents/variants/**/*.md` → all ≤100; `ls packages/core/po/sections/_details/` → 3 new files present |
| **사용자 dogfood** | 다음 persona dispatch 후 graphiti designer/qa/developer 시스템 프롬프트 라인 수 ≤100 확인 |
| **regression check** | `cat ~/.productune/sections/_details/promotion-rule.md` 존재 확인; `graphiti/pdt-developer.md` — `## Refuse rules` 섹션 존재 확인 |

---

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-20T00:00Z | pdt-designer | opus/xhigh | 1 | plan.md v1 + ticket v1 |
