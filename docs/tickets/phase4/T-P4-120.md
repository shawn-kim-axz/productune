---
ticket_id: T-P4-120
title: "Alternative-reporting protocol — knowledge-state anchored pros/cons doctrine"
version: v0.4-meta-dogfood
round: phase4-r4-doctrine
type: doctrine
status: done
assignee: pdt-designer
estimated_complexity: L3
model: opus
effort: high
risk_flags: none
slug: alternative-reporting-protocol
qa_status: skip
qa_loops: 0
area_tags: [doctrine/po-instructions, doctrine/po-memory, doctrine/sections, doctrine/delegation]
started_at: 2026-05-15T01:10Z
completed_at: 2026-05-15T03:30Z
duration_min: 140
---

## §1 Request

**사용자 directive verbatim (2026-05-15):**
> "이런식으로 문제를 해결하는 대안을 유저한테 보고할 때 유저의 지식상태를 기반으로 대안의 장단점을 명시적으로 유저가 받아야돼.(지식 상태 또한 po의 메모리에 update되어야함) ... 이 과정도 productune의 feature에 넣자."

### Trigger context

직전 turn (T-P4-119 race-fix 설계 dispatch) 에서 PO 가 A/B/C 3개 alternative 를 vague descriptor
(`cleanest`, `simpler`) 위주로 제시. user push-back. user 가 직접 자기 knowledge state
(Electron IPC fluency, React lifecycle 익숙도, race condition 개념 친숙, Zustand 자유로움) 를
계열별로 caveman-lite 로 enumerate → PO 가 그 anchor 기반으로 대안을 재설명. user 가 이 패턴을
1회성 우회가 아닌 **productune 영구 doctrine feature** 로 promote 요구.

### What this ticket establishes

1. **`~/.productune/po-memory.md` 의 `## User knowledge state (engineering)` 섹션이
   doctrine-mandatory** — PO가 Step 1 startup 마다 load. 2026-05-15 baseline 6 axis (Electron
   IPC / React lifecycle / Zustand / Race conditions / Architecture trade-offs / Unclear gaps)
   는 normative schema. user knowledge correction 발생 시 PO가 mid-session capture.

2. **신규 `~/.productune/sections/alternative-reporting.md`** — N≥2 대안 보고 시 mandatory
   format: 각 옵션에 Pros + Cons 블록 강제, 각 항목은 user knowledge state axis anchor 인용,
   vague descriptor blacklist (`cleanest` / `simpler` / `easier` / `more-elegant` / `cleaner` /
   `nicer`), 추천 라인 끝에 anchor citation.

3. **`po-instructions.md` mechanical 변경** — Files 리스트에 `sections/alternative-reporting.md`
   on-demand 추가, Hard rules 에 protocol 1줄 추가, PO loop §6 Alternatives 참조 갱신.

4. **`sections/po-loop.md` Step 1·3 변경** — Step 1.1 (Memory) 의 load 대상에 User knowledge
   state 명시, Step 1 #6 Alternatives 를 protocol 파일로 위임, Step 3 step 14 옆에 14c
   "Knowledge state correction capture" 추가.

5. **delegation prompt 반영** — PO 가 Designer 호출 시 `[ctx]` 에 `user_knowledge_state` 1줄
   요약 snapshot 포함 → Designer 가 plan 의 Alternative 블록 작성 시 같은 protocol 적용
   (T-P4-119 plan §2 같은 ticket 내 Architecture decision 표가 같은 anchor schema 따르도록).

전체 section-by-section diff spec → **`docs/design/T-P4-120/plan.md`**.

### Boundary

이 티켓 = doctrine spec authoring + 실 편집. Turn 2 dispatch (2026-05-15T03:30Z) 에서 user
directive override 로 Designer 가 PO mechanical 대신 직접 ~/.productune/po-instructions.md /
sections/po-loop.md / sections/alternative-reporting.md (신규) / sections/delegation.md /
po-memory.md 5개 file 편집 완료. repo mirror (`packages/core/po/**`) 는 별도 turn.

---

## §2 Acceptance Criteria

| # | 검증 항목 | 결과 |
|:--|:---------|:--|
| AC1 | `docs/design/T-P4-120/plan.md` 가 §3 모든 항목 (a/b/c/d/e) 별 section-by-section diff spec 을 담는다. 각 spec 은 insert/replace 위치 line-range 또는 anchor 헤더로 명시. | ✓ Turn 1 |
| AC2 | `~/.productune/po-memory.md ## User knowledge state (engineering)` 섹션 schema 가 normative 로 문서화: line format, 6 baseline axis taxonomy + 확장 규칙, timestamp / supersede 규칙, update trigger (correction-pushback / fluency-demonstration) 명시. | ✓ Turn 1 spec + Turn 2 implemented in po-memory.md head comment |
| AC3 | 신규 `sections/alternative-reporting.md` 의 mandatory format 이 plan 에 verbatim 으로 포함: 각 option 의 Pros / Cons 블록 구조, anchor citation 문법, 추천 라인 anchor 인용 규칙, vague descriptor blacklist enum. | ✓ Turn 1 plan + Turn 2 file created |
| AC4 | plan 이 `po-instructions.md` 변경 지점 3 곳 (Files / Hard rules / PO loop §6 reference) 의 before→after diff 를 보여준다. | ✓ Turn 1 + Turn 2 applied |
| AC5 | plan 이 `sections/po-loop.md` Step 1.1 + Step 1 #6 + Step 3 #14c 변경의 before→after diff 를 보여준다. | ✓ Turn 1 + Turn 2 applied |
| AC6 | plan 이 delegation `[ctx]` JSON shape 에 `user_knowledge_state` 필드를 추가하는 spec 을 명시. | ✓ Turn 1 + Turn 2 applied (jq snippet + persona obligations section) |
| AC7 | plan 이 Designer/Developer/QA persona 가 자기 산출물 (plan / 응답) 에서 Alternative 표/블록 작성 시 같은 protocol 따라야 함을 명시 (persona-side enforcement). | ✓ alternative-reporting.md §Persona-side enforcement 표 + delegation.md `[ctx]` 설명 |
| AC8 | plan 이 vague-descriptor 위반 검출 → user 측 reject 사유로 명시. PO 가 retry 시 anchor citation 누락 검출 시 self-reject 후 재작성하는 규칙. | ✓ alternative-reporting.md §Detection rule + §User-side reject signal |
| AC9 | plan §Out of scope 와 본 ticket §Out of scope 가 GUI rendering / persona-side knowledge state 추적은 별도 ticket 으로 명시 배제. | ✓ |
| AC10 | plan + ticket 양쪽이 사용자 verbatim directive 를 §1 에 보존. | ✓ |

---

## §Out of scope

- **GUI rendering** — User knowledge state 를 GUI 의 사이드패널 / Team tab 등에서 시각화하는 작업. 본 doctrine 은 markdown SoT 만 정의.
- **Persona-side knowledge state** — Designer/Developer/QA persona 가 각자 별도로 user knowledge state file 을 갖는 패턴. 현재는 PO 메모리만 SoT. persona 는 `[ctx]` 로 snapshot 받기만.
- **Automatic NLP-based knowledge inference** — 자동 분석 / scoring. 본 doctrine 은 PO 가 의미적으로 판단하고 1줄 line append 하는 규칙만.
- **Cross-user / 팀 단위 knowledge state** — 본 doctrine 은 single-user `~/.productune/po-memory.md` scope.
- **Repo mirror sync** — `packages/core/po/**` 측 doctrine mirror (install.sh seed 대상) 는 별도 turn. live `~/.productune/**` 는 본 turn 에서 완료.
- **PRD 변경** — 본 directive 는 PRD scope 가 아닌 doctrine feature. PRD 영향 없음.
- **GUI 변경** — 어떤 `packages/gui/**` 도 touch 안 함.

---

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | doctrine doc read-only verification — plan §3 모든 항목이 ticket §Acceptance AC1–AC10 cover. live file edit 완료 후 read-back 으로 self-verify (Turn 2). |
| **사용자 dogfood** | (1) 후속 turn 에서 의도적으로 새 N≥2 alternative reporting 시나리오 (예: 다음 plan dispatch) 유도 → 결과 form 이 protocol 준수하는지 사용자가 직접 verify. (2) PO 가 user 발언 중 새 knowledge correction 신호 감지 시 `User knowledge state` 에 1줄 append 했는지 memory.md tail 로 확인. (3) repo mirror sync turn 에서 `diff -r ~/.productune/ packages/core/po/` 결과 1:1 매칭 확인. |
| **regression check** | 기존 `sections/escalation.md` "Alternatives only when 2 defensible paths" rule (po-loop.md Step 1 #6) 와 신규 protocol 의 trigger 가 충돌 없음 — Step 1 #6 가 alternative 를 surface 할지 말지 결정, alternative-reporting.md 가 surface 할 때의 format. 두 rule layered. delegation.md `[ctx]` jq 구문 valid (한 줄 JSON; quote balance + comma) — Turn 2 self-verified. |

---

## §5 Module Map (doc-only — 코드 touch 0)

```
docs/design/T-P4-120/plan.md                                    ← 신규 (본 ticket 의 source of truth)
docs/tickets/phase4/T-P4-120.md                                 ← 본 ticket

(Turn 2 에 실 편집 완료 — live SoT)
~/.productune/po-instructions.md                                ← Files / Hard rules / Step 1 reference 3곳 ✓
~/.productune/sections/alternative-reporting.md                 ← 신규 (mandatory format + blacklist + anchor schema + persona enforcement + UKS schema) ✓
~/.productune/sections/po-loop.md                               ← Step 1.1 / Step 1 #6 / Step 3 #14c ✓
~/.productune/sections/delegation.md                            ← [ctx] minimal template 의 jq 가 user_knowledge_state 필드 포함; 새 ### user_knowledge_state field (T-P4-120) 단락 추가 ✓
~/.productune/po-memory.md                                      ← User knowledge state section 헤더 + 주석 normative; placeholder "Alternative-reporting protocol (T-P4-NEW candidate)" 섹션 제거 ✓

(별도 turn — repo mirror sync 대상)
packages/core/po/po-instructions.md                             ← ~/.productune/po-instructions.md mirror
packages/core/po/sections/*.md                                  ← 동일 mirror (alternative-reporting.md 포함 11→12 파일)
packages/core/po/po-memory.md.template (있다면)                  ← User knowledge state section seed
```

---

## §6 UX Self-Check (§1.5 design-system)

| Principle | 상태 |
|:----------|:-----|
| **Few Things** | 1개 신규 section file + 4개 기존 doctrine file 의 anchor-citation 추가만. 새 mental model 추가 0 (anchor citation 은 기존 markdown 인용 패턴). ✓ |
| **Familiar** | "당신의 지식 상태에 맞춰 설명" 은 일반 학습 경험과 일치. anchor 인용은 reference desk 패턴. ✓ |
| **Predictability** | 모든 N≥2 alternative 보고가 동일 format. user 가 한 번 익히면 어디서나 동작 예측. ✓ |
| **Feedback** | vague descriptor 위반 / anchor 누락 시 user reject 가 명시적 negative feedback loop. PO self-reject 도 explicit. ✓ |
| **Escape** | user 가 protocol 무시하고 caveman-lite 답변만 원하면 `/short` 또는 의도 표명으로 우회 가능 (escalation.md 의 user prefix 와 동일 family). plan §Open Questions 에 명시. ✓ |

---

## §7 Open Questions

| # | 질문 | 현재 결정 |
|:--|:----|:---------|
| OQ1 | User 가 매번 anchor citation 보기 싫다고 할 가능성? | plan §Open Questions 에 `/short` 류 escape prefix 검토. 본 ticket scope 아님 — 별도 follow-up. |
| OQ2 | Designer/Developer 가 plan 내 Alternative 표 작성 시에도 같은 anchor citation 의무인가? | YES — plan §3.e 에서 명시. `[ctx]` 로 knowledge state snapshot 받아 anchor 가능. |
| OQ3 | "vague descriptor" enum 이 너무 strict 하면 어휘 빈곤? | blacklist + permitted-when-anchored 규칙. 예: `simpler than B because user 의 React-lifecycle fluency 상 useEffect double-mount 추적이 부담 → 안 함` 이면 OK. naked `simpler` 만 금지. |
| OQ4 | User knowledge state 6 axis 가 너무 engineering 편향? planner mode user 에게? | plan §3.c 에서 "extensible — 새 axis appendable" 로 spec. 도메인별 axis (design vocabulary, marketing terms 등) 추가 가능. baseline 6 은 v0.4 dogfood 단계 reflect. |
| OQ5 | persona-side knowledge state (Designer 가 user 의 design vocabulary fluency 별도 추적) 필요? | 현재 scope out (§Out of scope). PO 단일 SoT 로 시작. dogfood 결과 따라 별도 ticket. |
| OQ6 | repo mirror (`packages/core/po/**`) 측 sync 가 언제? | 별도 turn (user 또는 PO 가 작업). install.sh 재실행 시 ~/.productune/ overwrite 위험 회피용. 가능하면 다음 sub-task 직전. |

---

## §8 Dependencies

| Ticket | 관계 |
|:-------|:-----|
| T-P4-119 | 본 directive 의 trigger 가 된 race-fix turn 의 A/B/C 보고 — vague descriptor 사용한 baseline 사례. plan §1 reference. |
| — | (이 doctrine 은 기존 escalation.md / po-loop.md 와 layered — 충돌 ticket 없음.) |

---

## §9 Persona Activity

| Turn | Persona | Action |
|:-----|:--------|:-------|
| 1 | pdt-designer | T-P4-120 ticket + plan emit. po-memory.md User knowledge state 기존 stub 확인 (2026-05-15 baseline 6 axis 이미 존재) → doctrine 화 normative schema 작성. sections/alternative-reporting.md 신규 mandatory format 명세. po-instructions.md + po-loop.md before→after diff. delegation `[ctx]` user_knowledge_state 필드 spec. |
| 2 | pdt-designer | **User directive override**: PO mechanical 예정이었으나 user 가 Designer 에게 직접 doctrine edit 위임. plan §3 따라 ~/.productune/po-instructions.md (3곳) + sections/po-loop.md (3곳) + sections/alternative-reporting.md (신규 verbatim) + sections/delegation.md (jq + persona obligations 단락 추가) + po-memory.md (UKS 헤더 주석 normative, placeholder protocol 섹션 제거) 5개 file 실편집. self-verify: read-back. repo mirror (`packages/core/po/**`) 는 별도 turn. |
| 3 | (사용자) | 후속 turn 에서 alternative reporting 시나리오 발생 시 form 준수 verify. PO 가 새 knowledge correction 자동 capture 하는지 memory.md tail 로 확인. repo mirror sync 별도 dispatch. |

## Persona Activity

| When (UTC) | Persona | Model/Effort | Result |
|---|---|---|---|
| 2026-05-15T01:10Z | pdt-designer | opus/high | plan + ticket emit · session 5be4cbe1 (resumed) · alt-reporting protocol doctrine spec · conf=high |
| 2026-05-15T03:30Z | pdt-designer | opus/high | doctrine edits applied (5 files: po-instructions / po-loop / alt-reporting NEW / delegation / po-memory) · self-verify pass · conf=high |

---

## §Outcome

Alternative-reporting protocol 이 productune 영구 doctrine feature 로 채택 + live `~/.productune/**` 에 실 적용 완료.

**Adoption surface (Turn 2 — live SoT):**
- `~/.productune/po-instructions.md` — Files on-demand 리스트에 `alternative-reporting.md` 추가; Hard rules 에 protocol 1줄 추가; PO loop Step 1 (Instruction) 에 User knowledge state load 명시.
- `~/.productune/sections/po-loop.md` — Step 1.1 의 memory load 대상에 `## User knowledge state (engineering)` 추가; Step 1 #6 Alternatives 가 protocol 파일에 format 위임; Step 3 #14c (User knowledge state correction capture) 신설 — `user-asserted` / `inferred` raise / `inferred` lower 3개 trigger.
- `~/.productune/sections/alternative-reporting.md` (**신규**) — mandatory format (option 별 Pros/Cons + anchor `[<axis>]` 또는 `[<axis> · <level>]` + recommendation line anchor citation); permitted-when-anchored 예외; PO self-reject detection rule; user-side reject signal + 2× pushback → po-memory append rule; `/short` escape (anchor citation 여전히 의무); UKS line schema; persona-side enforcement matrix (po/designer/dev/qa).
- `~/.productune/sections/delegation.md` — minimal template 의 jq 가 `user_knowledge_state` 필드 포함 (`UKS_AXES` env 로 axes_relevant top-3 override 가능); `### user_knowledge_state field (T-P4-120)` 신설 단락 — persona obligations 명시.
- `~/.productune/po-memory.md` — `## User knowledge state (engineering)` 헤더 주석을 normative schema (line format + level enum + update triggers) 로 normalize. placeholder `## Alternative-reporting protocol (T-P4-NEW candidate)` 섹션 제거 (protocol body 가 sections/ 로 promote 됐으므로).

**Verification (self-verify, Turn 2):**
- 5 file read-back: schema match plan §3. ✓
- alternative-reporting.md 가 `~/.productune/sections/` 에 존재. ✓
- po-instructions.md Files 섹션이 `alternative-reporting.md` reference. ✓
- delegation.md `[ctx]` jq 구문 — 신규 `user_knowledge_state` 필드의 `--argjson uks "$UKS"` 가 valid; UKS 자체 build 도 `jq -nc` 로 stand-alone valid JSON. ✓ (실 실행 검증은 다음 PO delegate turn 에서.)

**Follow-ups:**
- repo mirror (`packages/core/po/**`) sync — 별도 turn. install.sh 재실행 시 live overwrite 회피 필요.
- 사용자 dogfood — §QA scope (1)(2) 항목 verify. 다음 N≥2 alternative reporting dispatch 시 form 준수 확인.
- OQ1 — `/short` enum 화 후속 ticket (escalation.md user prefix family 와 정합).
- OQ4 — planner-mode user 도메인 axis (design vocabulary / marketing 등) extensibility 는 Phase 5 retro 평가.

**Calibration trace:** 본 ticket 은 L3 / opus / high 견적 → 실제 opus / high · 2 turn 분할 (Turn 1 plan+ticket emit 60min, Turn 2 doctrine edit 80min) · cumulative duration 140min · escalation none · rework none · QA skip. 견적 정확 — doctrine 작성 + live edit 5 file 분량 sweet spot.
| 2026-05-18T01:00Z | (direct claude) | sonnet | **[wiki promo success]** Episode `knowledge-state-anchored-alternatives` queued in group `persona-designer` via direct `claude --print` (no --agent). 6 회 dispatch 끝에 root cause 다층 발견: (a) productune.env 의 GRAPHITI_LLM_PROVIDER 미설정 (PO mechanical 으로 ollama 4줄 복원), (b) `claude mcp add` 미실행 — agent frontmatter `mcpServers:` 블록 무시 (claude code 2.1.142), (c) hyphenated server 이름과 agent 화이트리스트 namespace mismatch (graphiti-designer vs graphiti), (d) `claude --agent` 서브에이전트가 project-local MCP server inherit 안 함 — 우회는 `claude` 직접 호출. |
