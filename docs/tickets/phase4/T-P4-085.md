---
ticket_id: T-P4-085
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: todo
assignee: pdt-developer
created_at: 2026-05-11T00:00:00Z
started_at: null
completed_at: null
duration_min: null
estimated_complexity: L1
risk_flags: doctrine-vocabulary, rename
bundle: T-P4-R4-fix
slug: rename-rounds-to-versions-gui
qa_status: pending
qa_loops: 0
---

# T-P4-085: GUI 어휘 정합 — Rounds → Versions (doctrine SoT 정합)

## Background

T-P4-043 (Project Rounds section + ticket-review tab) land 직후 사용자 dogfood (2026-05-11):

사용자가 productune 의 "라운드" 어휘 폐기 + "Version" 으로 통일했었던 것 catch. doctrine SoT 확인:
- `poState.versions[]` — productune 의 사용자 가시 단위 (한 PRD 사이클, `outcome` / `retrospective_path` 등)
- `pdt-designer.md` Phase 1 / 5a / 6 모두 `versions[N]` 어휘
- `v1-to-v2.sh` migration 이 `versions[]` 5개 cap

그러나 T-P4-043 의 `ProjectRoundsSection` 이 `poState.versions` 를 **"Rounds"** 라벨로 표시 + i18n key `workspace.rounds.*` 박힘 — doctrine 어휘 mismatch.

ROADMAP 의 "Round 0/1/2/3/4" (Phase 4 ticket 분해 단위) 는 **별 axis** (developer-facing internal) 라 본 ticket scope 밖. 사용자 가시 단위 (productune.versions) 만 정리.

## Doctrine references

- `packages/core/po/state.schema.json` (가정) / `pdt-designer.md` — `versions[]` SoT
- T-P4-043 land 산출물 (`ProjectRoundsSection.tsx` / `workspace.rounds.*` i18n keys)
- ROADMAP line 22 (T-P4-084 후 갱신) — "단일 UI + 2 tone" 정합

## Acceptance

### A) 파일 rename

- [ ] `packages/gui/src/components/workspace/ProjectRoundsSection.tsx` → `ProjectVersionsSection.tsx`
- [ ] export default 컴포넌트명 `ProjectRoundsSection` → `ProjectVersionsSection`
- [ ] `LeftSidebar.tsx` 의 import / 사용처 변경 (`ProjectRoundsSection` → `ProjectVersionsSection`)
- [ ] 다른 import 처 grep 으로 확인 후 모두 변경

### B) i18n key migration

- [ ] `en.json` / `ko.json` 의 `workspace.rounds.*` 모든 key → `workspace.versions.*` 로 rename
- [ ] 컴포넌트 안 사용처 (`t('workspace.rounds.xxx')`) 도 같이 변경
- [ ] 라벨 텍스트: "Rounds" → "Versions" (en) / "라운드" → "버전" (ko). 단 ROADMAP/doctrine 의 별 axis "Round 0/1/2/3/4" 어휘 정합 위해 ko 도 "버전" 권장
- [ ] locale parity 유지 (en/ko key 수 동일, T-P4-057 보호어 위반 0)

### C) 내부 변수 / 주석 / 함수명 정리

- [ ] `RoundRow` / `roundRow` / `roundIndex` 같은 내부 식별자 모두 `VersionRow` / `versionRow` / `versionIndex` 로 rename
- [ ] 컴포넌트 안 `TicketsSubSection` 정합 (Tickets sub-section 자체는 영향 X)

### D) 회귀

- [ ] T-P4-043 ProjectRoundsSection 의 동작 (round expand + Tickets sub-section + 클릭 → ticket-review tab open) 정합 유지
- [ ] ROADMAP 의 "Round 0~9" 어휘는 변경 X (Phase 4 ticket 분해 axis 별)
- [ ] ticket md 의 `round: phase4-r4` 같은 frontmatter 필드 — 변경 X (별 axis)

### E) 검증

- [ ] `cd packages/gui && pnpm exec tsc --noEmit` exit 0
- [ ] `cd packages/gui && pnpm build` exit 0
- [ ] locale check 통과
- [ ] manual: Side Panel Project tab → 기존 "Rounds" 가 "Versions" / "버전" 으로 표시, 펼침 / Tickets 클릭 정합

## QA scenarios (light)

1. **rename 정합** — file / 컴포넌트 / import 모두 ProjectVersionsSection
2. **i18n key parity** — en/ko `workspace.versions.*` 동일 + 보호어 위반 0
3. **라벨 표시** — 사용자 화면 "Versions" / "버전" (locale 따라)
4. **회귀 — expand + ticket click** — 기존 동작 정합
5. **회귀 — ROADMAP/doctrine round** — Phase 4 R0~9 axis 어휘는 변경 X

## Out of scope

- ROADMAP / ticket md 의 `round: phase4-r4` 필드 (별 axis, developer-facing)
- doctrine 의 `versions[]` schema 변경 (이미 SoT — 정합 작업만)
- T-P4-084 (user mode + tone) 의 어휘 매핑 표 통합 (별 ticket)
- light theme

## Notes

- 사용자 dogfood (2026-05-11): T-P4-043 land 후 catch. doctrine `versions[]` SoT, GUI 어휘 mismatch fix forward.
- L1 scope (rename + i18n key migration).

## Persona Activity

| turn | persona | action | output |
|---|---|---|---|
| 1 | pdt-developer | impl | ProjectVersionsSection.tsx (rename), LeftSidebar.tsx (import + i18n keys), en.json + ko.json (workspace.rounds.* → workspace.versions.* + phaseStrip.rounds* → versions*); tsc exit 0, pnpm build exit 0 |
