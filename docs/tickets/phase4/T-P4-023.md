---
ticket_id: T-P4-023
round: phase4-r2
stage: impl
status: todo
assignee: pdt-developer
created_at: 2026-05-04T07:33:00Z
started_at: null
completed_at: null
duration_min: null
estimated_complexity: L3
risk_flags: ui, git-log-parse
branch: null
worktree_path: null
---

# T-P4-023: [버전 히스토리] 카드 UI

**Round**: phase4-r2  **Stage**: impl  **Status**: todo  **Assignee**: pdt-designer + pdt-developer
**PRD anchor**: [docs/prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future](../../prd/productune.md#phase-4--terminal-무의존-gui-풀-사이클-future) (§10 [버전 히스토리])
**Estimated complexity**: L3  **Risk flags**: ui, git-log-parse
**Doctrine**: [`~/.productune/sections/git-workflow.md`](../../../../.productune/sections/git-workflow.md) §"Round 묶음 (선택)"

## Request

git log 를 ticket-id 단위 그룹 + 페르소나 trace inline 의 자연어 카드 UI 로 렌더링. 사용자가 PR / 브랜치 / 커밋 / merge / worktree / dev / staging 어휘 한 번도 보지 않고 "어제 어떤 작업이 있었지" 답변 가능. round 필드 활용해 "이번 라운드 배포 작업 N개" group 표시. `past_tickets` (timeline) + ticket worktree git log (commit 시퀀스) 두 소스 결합 — timeline 자체는 `past_tickets` 에서 derive (`lifecycle.md` 원칙 유지), git log 는 ticket 단위 detail 에서만 인용.

## Inputs

- PRD: `docs/prd/productune.md` §10 [버전 히스토리]
- Doctrine: `~/.productune/sections/lifecycle.md` §"Timeline / project history" (timeline source = past_tickets), `~/.productune/sections/git-workflow.md` §"Round 묶음"
- Design: `docs/design/service-flow-and-screens.md` §3.2 어휘 매핑 표 + §4 K2 (버전 히스토리 카드)
- Deps: T-P4-020 (worktree path), T-P4-021 (자동저장 commit 메시지 형식), T-P4-022 (squash merge 결과)

## Acceptance

- [ ] [버전 히스토리] 탭 신설. 카드 UI — ticket-id 별 그룹 + 그 안 페르소나 trace inline (turn 별 1줄 summary).
- [ ] round 필드 활용 — "이번 라운드 배포 작업 N개" 그룹 헤더 표시
- [ ] 카드 클릭 → expand → 해당 ticket 의 자동저장 commit 시퀀스 (페르소나 turn 별 1줄) 표시
- [ ] timeline 정렬 = ticket `created_at` 내림차순. ticket 안 commit 정렬 = chronological.
- [ ] 사용자 화면 어휘 lint — `branch` / `commit` / `PR` / `merge` / `worktree` / `dev` / `staging` 노출 0 검증
- [ ] commit 메시지 (`T-NNN [persona/turn N] <summary>`) 의 `<summary>` 부분만 자연어 표시. prefix (T-NNN / persona / turn) 는 카드 메타 영역으로.
- [ ] empty state — "아직 작업 기록이 없습니다. [새 작업 시작] 으로 첫 ticket 을 발행하세요." (design §9 Empty 패턴)
- [ ] git log parse 실패 / worktree 없음 → 친절 fallback ("작업 기록을 불러올 수 없습니다. 다시 시도하기")
- [ ] `pnpm -r build` 통과

## Out of scope

- commit revert / rollback UI (별도 ticket)
- 페르소나 turn 별 산출물 diff 보기 (Phase 5 — Round 7 메모리 편집기와 통합 가능성)
- 라운드 간 비교 view (Phase 5)
- export to markdown / PDF (Phase 5)

## Notes

- 모듈 위치: `packages/gui/src/views/VersionHistoryView.tsx` + `packages/core/src/git-workflow/history.ts` (git log parse + ticket grouping).
- git log parse = `git -C <worktree> log --format='%H|%s|%ai'`. summary 추출 = `T-NNN [persona/turn N] <summary>` 형식 정규식.
- `past_tickets` join = ticket-id 키. ticket 의 created_at / completed_at / duration_min 카드 메타로.
- 페르소나 trace = ticket frontmatter 의 `## Persona Activity` 표 그대로 inline. 본 ticket 은 표 데이터 reuse — 신규 trace 생성 X.
- 어휘 lint = design service-flow §3.2 매핑 표. lint helper = `packages/core/src/lint/vocabulary.ts` (T-P4-024 와 함께 추가 권장 — 본 ticket 에서는 manual smoke 로 검증해도 OK).

## Persona Activity

<!-- PO managed, append-only, structured — Result ≤ 80 chars -->
| When | Persona | Model/Effort | Turn | Result |
|---|---|---|---|---|
