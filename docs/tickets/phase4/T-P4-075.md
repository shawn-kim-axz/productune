---
ticket_id: T-P4-075
version: v0.4-meta-dogfood
round: phase4-r4-fix
type: impl
status: done
assignee: pdt-developer
created_at: 2026-05-08T00:00:00Z
completed_at: 2026-05-08T00:00:00Z
duration_min: 30
estimated_complexity: L2
risk_flags: visual-regression, cross-component
bundle: T-P4-R4-fix
slug: ux-minor-token-misc-patch
qa_status: pending
qa_loops: 0
---

# T-P4-075: §1.5 minor violation 통합 patch — token + 미세 fix

## Background

T-P4-069 audit §3 minor 11건 중 critical/medium fix 와 분리된 8건 통합. 대부분 §2 token hardcode + 키 binding 누락. 사용자 가시 영향 약 (가독성/일관성 보강) 이라 critical 3건 fix (T-P4-067, T-P4-073, T-P4-074) 후 별 patch 1회로 묶어 처리.

본 ticket 에서 다루는 항목:

| # | 컴포넌트 | sub-rule | 패치 |
|---|---|---|---|
| M-1 | ChatPanel | 3-1 Predict | `ctxRow` `#101010` / `#1f2a3a` 등 token 외 hex → `--surface-*` 또는 `--bg-*` 정합 |
| M-5 | SessionHealthBanner | 3-3 Escape | dismiss `×` 만 → Esc 키 binding 추가 (banner mount 시 listener) |
| M-6 | PendingPromotionDrain | 2-1 Few | row 당 [Save] [Edit] [Skip] 3 CTA → Edit 을 hover-only 또는 kebab menu 강등 |
| M-7 | PendingPromotionDrain | 3-3 Escape | bulk input Esc → input clear binding 추가 |
| M-8 | LeafPane | 3-1 Predict | active border `#FF6B2B66` (40% alpha hardcode) → `color-mix(in srgb, var(--accent) 40%, transparent)` |
| M-9 | StatusBar | 3-1 Predict | `projectName` color `#5A5A5A` → `--text-muted` (`#A0A0A0`) |
| M-10 | SettingsView | 3-1 Predict | active tab bg `#1E2A3A` → `--accent` 12% alpha pill recipe (§8.2) |
| M-11 | WorkflowRulesPanel | 1.5.5 Escape | "Phase 5 lock" 라벨 disabled → tooltip / hint 추가 ("Phase 5 에서 활성화" or 동등) |

## Doctrine references

- `docs/design/design-system.md` §1.5 + §2 token + §8.2 pill recipe + §8.5 modal recipe
- `docs/design/T-P4-069-audit.md` §3 minor list

## Acceptance

### A) Token 정합 (M-1, M-8, M-9, M-10)

- [ ] M-1 ChatPanel `ctxRow`:
  - 기존 hex (`#101010`, `#1f2a3a`) → `--surface-2` / `--surface-3` 또는 `--bg-elevated` 등 token. dev 가 design-system §2 매핑 확인 후 결정. 시각 회귀 0
- [ ] M-8 LeafPane active border:
  - 기존 `#FF6B2B66` → `border-color: color-mix(in srgb, var(--accent) 40%, transparent)`
  - 또는 `--accent-40` token 신설 검토 (designer 결정 사항 — 현재 inline color-mix 권장)
- [ ] M-9 StatusBar projectName:
  - 기존 `#5A5A5A` → `var(--text-muted)` (`#A0A0A0`). metadata 영역이라 secondary 수준 권장
- [ ] M-10 SettingsView active tab:
  - 기존 `#1E2A3A` → `background: color-mix(in srgb, var(--accent) 12%, transparent)` + text `--accent`
  - §8.2 pill recipe 정합

### B) 키 binding (M-5, M-7)

- [ ] M-5 SessionHealthBanner Esc dismiss:
  - banner mount 시 `keydown` listener — `Escape` → `onDismiss()` 호출
  - busy / 진행 중 상태 무시 정책 (있으면 dev 판단)
  - unmount cleanup
- [ ] M-7 PendingPromotionDrain bulk input:
  - input `onKeyDown` 에 `Escape` → input value clear (또는 textarea 의 경우 동일)
  - Enter 동작 (이미 있는 submit) 유지

### C) UX 강등 (M-6)

- [ ] PendingPromotionDrain row CTA 3 → 2 + Edit 강등:
  - Option A: Edit 을 row hover 시에만 노출 (CSS `opacity: 0 → 1 on row:hover`)
  - Option B: kebab menu (⋯) 안으로 Edit 이동
  - default = Option A (간단). dev 가 시각 검증 후 B 가 더 자연스러우면 B
  - Save / Skip 은 항상 노출

### D) 강등 라벨 (M-11)

- [ ] WorkflowRulesPanel Phase 5 lock:
  - disabled visual 만 → 추가: `title` attribute 또는 hover tooltip ("Phase 5 에 잠금 해제" 류)
  - i18n key `workspace.workflowRules.phase5LockHint` 추가 (en/ko)
  - 시각: lock icon (`lucide` `Lock`) 라벨 좌측 14×14 권장

### E) 회귀

- [ ] 기존 token 정합 컴포넌트 영향 X (cross-component visual regression 검증)
- [ ] dark mode 정합 — color-mix 사용 시 alpha 정합
- [ ] 단축키 (Esc) 가 모달 / banner / drain 다 적용 시 priority 충돌 없음 (modal 우선 → banner → drain)

### F) 검증

- [ ] `pnpm tsc --noEmit` exit 0
- [ ] `pnpm -r build` exit 0
- [ ] locale check 통과
- [ ] manual: 8개 항목 시각 회귀 + Esc binding 동작 검증

## QA scenarios (light)

1. **token 회귀** — ChatPanel / LeafPane / StatusBar / SettingsView 시각 변경 확인 + dark mode
2. **Esc binding** — SessionHealthBanner Esc → dismiss / PendingPromotionDrain bulk input Esc → clear
3. **CTA 강등** — PendingPromotionDrain row hover 시 Edit 노출, 평소 미노출 (또는 kebab menu)
4. **lock hint** — WorkflowRulesPanel Phase 5 영역 hover → tooltip 보임

## Out of scope

- M-2 footer 순서 / M-3 busy spinner / M-4 Esc 정책 — T-P4-067 + T-P4-074 별 ticket 으로 분리
- §2 token 마이그레이션 전체 (Phase 5 또는 R5 candidate)
- ESLint custom rule (R5)
- 컴포넌트 wrapper 라이브러리화 (R5)

## Notes

- 본 ticket = §1.5 minor 다수를 묶은 patch. T-P4-067 / T-P4-073 / T-P4-074 land 후 직렬 처리 권장. 8개 spot 변경 = chunking ceiling 안 (token 다수가 mechanical sed 류 변경이라 실 작업 시간 적정).
- M-6 Edit 강등 결과가 사용자 dogfood 시 어색하면 별 R5 ticket 으로 다시 다룰 수 있음.
- M-1 token 매핑 전 dev 가 design-system §2 실제 surface token 명세 확인 필요.

## Persona Activity

| date | persona | action |
|---|---|---|
| 2026-05-08 | pdt-developer | Impl: 8 spots patched — A1 `--surface-body`, A2 `color-mix` accent border, A3 `--text-muted`, A4 `--accent` 12% pill, B1 Esc→dismissBanner, B2 Esc→bulkInput clear, C1 Edit hover-only (CSS inject), D1 Lock icon + title + i18n phase5LockHint. tsc exit 0. |
