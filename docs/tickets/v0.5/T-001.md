---
ticket_id: T-001
version: v0.5
phase: 1
type: design
status: done
assignee: pdt-designer
created_at: 2026-06-01T00:00:00Z
started_at: 2026-06-01T00:00:00Z
completed_at: 2026-06-01T08:41:00Z
duration_min: 521
estimated_complexity: L4
risk_flags: multi-track, doctrine-sync
slug: prd-authoring-v0-5
qa_status: skipped
qa_loops: 0
model: opus
effort: max
ambiguity_score: 0.04
clarity_iter: 2
---

# T-001: PRD authoring v0.5

> Auto-ticket emitted at Phase 1 entry. This ticket is the **comms vehicle** for the
> PRD clarity loop (`docs/designer/bookshelf` → `prd-clarity-loop.md`). `## Plan` below
> is the loop itself. PRD body lands once ambiguity score `A ≤ 0.05`.
>
> **Bilingual convention (decision-2, 2026-06-01)**: every section carries an English
> master block followed by a `(KR)` Korean block in this same ticket md. No LLM runtime
> translation, no sidecar file.

---

## Request

Author the **v0.5 section** of the productune self-PRD. v0.5 is the first serious pass at
**planner UX** — the GUI/PO absorb backend↔frontend technical detail and surface it as
*layered, simple information + a clear next-action at every moment*, so a non-developer
planner never has to look at the technical seam directly.

Scope = **14 items across 3 tracks** (carried verbatim from the brief):

- **Track A — Planner UX (9):** A1 left-panel reorder (Project / Artifacts(new) / Team /
  Explorer / Settings, fixed 5-tab order) · A2 new Artifacts tab (list → main-panel viewer:
  md / mermaid / html via `<webview>`) · A3 Project-menu cleanup (Open Project = direct OS
  dialog; Open New Window = home; Recent single-source align) · A4 CLI statusline (phase +
  in-phase ticket progress, e.g. `[v0.5 P1·3/12]`) · A5 chat rate-limit display
  (Anthropic `retry-after` parse → recovery countdown) · A6 cmd+p header expansion (inline
  search bar; search tickets / tabs / skills / MCP / artifacts / personas) · A7 ticket
  detail view (cmd+p select → detail page in main panel, not Tickets tab; KR body + dispatch
  progress visual) · A8 cmd 1/2/3/4 (jump to Nth tab in active tab-group) · A9 PO chat
  upgrade (markdown render polish — tables/code/list/blockquote on design-system tokens +
  inline action buttons / native AskUserQuestion cards + inline promotion approve/reject).
- **Track B — Persona / infra (4):** B1 GUI ↔ doctrine sync (4-tier, 9 ticket types, 7
  status; absorbs T-P4-146/147/148) · B2 Skill 2-layer (explicit allowlist vs auto-invoke;
  mapping expansion; domain-irrelevant category skip; SkillMatrix `[explicit]/[auto]/[unused]`
  labels) · B3 OS notifications (dispatch-done / escalation / phase-gate) · B4 unsigned
  `.dmg` distribution (electron-builder + `.icns` + `dist:mac` + Gatekeeper bypass guide).
- **Track C — Infra hardening (1):** C1 fresh-install CI smoke (isolated `$HOME`,
  `install.sh < /dev/null`, symlink absolute-path grep block, env-key parity, post-install
  PO dispatch-cost-strip check).

### Request (KR)

productune self-PRD 의 **v0.5 섹션** 작성. v0.5 = **기획자 UX 1차 본격화** — GUI/PO 가
backend↔frontend 기술 디테일을 흡수해 *계층화된 단순 정보 + 매 순간 명확한 다음 action* 으로
전달. 비개발자 기획자가 기술 경계면을 직접 볼 일이 없게.

범위 = **3 트랙 14 항목** (브리프 그대로):
- **A 트랙 — 기획자 UX (9):** A1 좌측 패널 재배치 · A2 산출물 탭 신설 · A3 Project 메뉴 정리 ·
  A4 CLI statusline · A5 채팅 rate-limit 표시 · A6 cmd+p header 확장 · A7 티켓 상세 화면 ·
  A8 cmd 1/2/3/4 · A9 PO 대화창 개선.
- **B 트랙 — 페르소나/인프라 (4):** B1 GUI↔doctrine 동기화 · B2 Skill 2-layer 명시화 ·
  B3 OS 알림 · B4 무서명 .dmg 배포.
- **C 트랙 — 인프라 안정화 (1):** C1 fresh-install CI smoke.

---

## Acceptance

- [ ] PRD clarity loop converges (`A ≤ 0.05`) **or** hits the 5-iteration hard cap with an
      `## Open Questions` block + `confidence < 0.7`.
- [ ] `docs/prd/productune.md` v0.5 section appended (English master, overwrite-per-cycle).
- [ ] `docs/artifacts/v0.5/PRD.html` authored (user-lang KR, translated view).
- [ ] All 14 items each carry: intent · scope (in/out) · solution shape · per-item AC.
- [ ] Carried-forward v0.4 deferrals reconciled (T-P4-091/092 dogfood, T-P4-146/147/148
      absorbed into B1) — explicit in/out call per item.
- [ ] v0.5 `version_outcome` (north star + input metrics + validation_method) defined and
      derivable from `## Success metrics` for PO to mirror into `po-state.json`.

### Acceptance (KR)

- [ ] clarity loop 수렴 (`A ≤ 0.05`) 또는 5-iter 캡 도달 시 `## Open Questions` + `confidence < 0.7`.
- [ ] `docs/prd/productune.md` v0.5 섹션 append (영문 master).
- [ ] `docs/artifacts/v0.5/PRD.html` 작성 (한국어 view).
- [ ] 14 항목 각각: 의도 · 범위(in/out) · 솔루션 형태 · 항목별 AC 포함.
- [ ] v0.4 이월건 정리 (T-P4-091/092 도그푸드, T-P4-146/147/148 → B1 흡수) in/out 명시.
- [ ] v0.5 `version_outcome` (north star + input metrics + validation_method) 정의.

---

## Plan

PRD-as-clarity-convergence loop (mandatory at P1). Target `A ≤ 0.05`, hard cap 5 iter.

### P1-entry reads (done)

- `docs/designer/feature-history.md` — v0.4 closed entries + 6 carried-forward deferrals.
- `docs/qa/bookshelf/fail-patterns.md` — **empty** (no QA fail clusters → no `type:test`
  auto-trigger this version).
- Existing PRD master `docs/prd/productune.md` (Phase 1–4 structure).

### Codebase resolution (observe-don't-assume — done iter 1)

Resolved the brief's anticipated open-questions that were *codebase facts*, not user
decisions, via GUI source inspection:

| Brief OQ | Resolution | Evidence |
|---|---|---|
| "탐색" 탭 = ? | = current **Explorer** (file tree) | `ActivityBar.tsx:22` |
| "탭 그룹" (cmd 1/2/3/4) | = active **LeafPaneNode**, `leaf.tabs[N]` | `store/workspace.ts:36` |
| cmd+p 현황 | already implemented; searches files/tickets/personas/skills | `QuickOpenPalette.tsx` |
| html viewer | `browser` tab type uses `<webview>` (reuse) | `TabContent.tsx` |
| rate-limit / OS-notif / electron-builder | none exist → net-new | grep: 0 hits |

### Iteration log

- **iter 1** — score `A = 0.075`. Lowest-clarity × weight slot = **Success metrics** (0.75):
  the v0.5 acceptance gate is genuinely planner-owned and not safely inferable. Emit
  `state:"needs-info"` + 1 `next_question` (success-gate). Await PO→user→resume.
- **iter 2** — user answer landed (brief `## User answers` Q1): v0.5 gate = **(b) full-cycle
  non-dev dogfood** — 14 items + planner-only GUI cycle PRD→Close, incorporating the
  carried-forward Round 9 dogfood (T-P4-091/092). Re-score: Success-metrics clarity
  0.75→~0.97, and the Acceptance slot tightened (now anchored to the dogfood gate). New
  **`A ≈ 0.04` ≤ 0.05 → converged**. `state:"ready"`. PRD body written: master EN section
  + KR HTML view. `version_outcome` derived (north star + 5 input metrics + validation).

### Slot weighting (V1 MVP default; no override)

Problem/user .18 · JTBD .14 · Scope .13 · AC .12 · Risk .10 · Success .09 · Solution .08 ·
Deps .06 · Brand/UX .05 · Ops .05.

### Plan (KR)

PRD = clarity 수렴 루프 (P1 필수). 목표 `A ≤ 0.05`, 하드캡 5회.
- P1 진입 read 완료 (feature-history / fail-patterns(빈 파일) / 기존 PRD).
- "observe-don't-assume" — 브리프 예상 OQ 중 **코드베이스 사실** (탐색 탭 / 탭 그룹 / cmd+p /
  webview) 은 GUI 소스 확인으로 해소. 사용자 결정 사항만 next_question 으로 surface.
- iter 1: `A = 0.075`. 최저 clarity 슬롯 = **Success metrics** → success-gate 1문항 질의 후 대기.

---

## Out of scope (this ticket)

- PRD HTML full-template work (sidebar TOC, rich nav) — deferred to v0.6 (decision-3).
- Implementation of the 14 items — Phase 2/3 dev tickets, post PRD-ready.
- LLM runtime translation of tickets — banned (decision-2).

---

## Outcome

**PRD ready — clarity loop converged at iter 2 (`A ≈ 0.04 ≤ 0.05`).**

Artifacts landed:
- `docs/prd/productune.md` — **v0.5 section** appended (English master): Why / Who / How
  (3 tracks) / What (14 items, each intent · in/out · solution · per-item AC) /
  carried-forward reconciliation / Acceptance gate / Success metrics → version_outcome.
  Activity-log entry added.
- `docs/artifacts/v0.5/PRD.html` — user-lang **KR** view (self-contained, design-system
  tokens; lean single-column — rich nav/TOC template deferred to v0.6 per decision-3).

**version_outcome** (for PO to mirror into `po-state.json`):
- **north_star**: non-developer planner completes one GUI-only full cycle (PRD→Close) with
  zero terminal use and zero raw-technical-seam reads — the carried-forward Round 9
  full-cycle non-dev dogfood (T-P4-091/092), run to completion.
- **input_metrics**: (1) 14/14 items pass per-item AC · (2) forced terminal drops = 0 ·
  (3) B3 OS notifications (dispatch-done / escalation / phase-gate) fire at correct moments ·
  (4) all cycle navigations (Artifacts viewer / ticket-detail / cmd+p ×6 / cmd 1–4) reachable
  GUI-only · (5) Round 9 13-AC evidence collected.
- **validation_method**: observed full-cycle non-dev dogfood (1 planner, 1 real project,
  PRD→Design→Build→Deploy→Close, GUI-only) + 14-item per-AC checklist pass.

Acceptance checklist (this ticket) — all met:
- [x] clarity loop converged (`A ≈ 0.04 ≤ 0.05`).
- [x] `docs/prd/productune.md` v0.5 section appended (English master).
- [x] `docs/artifacts/v0.5/PRD.html` authored (KR view).
- [x] all 14 items carry intent · scope(in/out) · solution shape · per-item AC.
- [x] carried-forward v0.4 deferrals reconciled (T-P4-091/092 dogfood = gate; T-P4-146/147/148
      → B1; T-P4-050~055 partial → A2; rest OUT) — explicit in/out per item.
- [x] `version_outcome` defined and derivable from `## Success metrics`.

> Lifecycle/status transition (in-progress → done) and `po-state.json` mirror left to PO.

### Outcome (KR)

**PRD ready — clarity loop iter 2 에서 수렴 (`A ≈ 0.04 ≤ 0.05`).** 산출물 = PRD master EN
v0.5 섹션 + `docs/artifacts/v0.5/PRD.html` (KR). version_outcome (north star + 5 input
metric + validation method) 도출 — PO 가 `po-state.json` 에 mirror. 이 티켓 acceptance 6항목
전부 충족. status 전환은 PO lifecycle.

---

## Persona Activity

| When | Persona | Model/Effort | Turn | Result |
|---|---|---|---|---|
| 2026-06-01 | pdt-designer | opus/xhigh | clarity iter 1 | P1 reads + GUI codebase resolution; scored `A=0.075`; emitted needs-info (success-gate question). T-001 comms-vehicle ticket land. |
| 2026-06-01 | pdt-designer | opus/xhigh | clarity iter 2 | User answer (gate=(b) full-cycle non-dev dogfood) folded in; re-scored `A≈0.04` → converged. Wrote PRD master EN v0.5 section + `docs/artifacts/v0.5/PRD.html` (KR); derived version_outcome. state:ready. |

---

**End of ticket.**
