---
ticket_id: T-002
version: v0.5
phase: 2
type: design
status: done
assignee: pdt-designer
created_at: 2026-06-01T00:05:58Z
started_at: 2026-06-01T00:05:58Z
completed_at: 2026-06-01T13:50:00Z
duration_min: null
estimated_complexity: L6
risk_flags: user-facing
slug: design-a2-artifacts-viewer
qa_status: n/a
qa_loops: 0
model: opus
effort: xhigh
---

# T-002: Design — A2 Artifacts viewer

> P2 lean-design ticket. PO emits the shell; **pdt-designer authors the design body**
> (`## Plan` / flow / mockup / `## Outcome`) and the bilingual KR sections.
> Design system is **reused from v0.4** (`docs/designer/design-system.md`) — no net-new DS.

## Request

Design the **A2 Artifacts viewer** (new left tab → main-panel viewer) per the v0.5 PRD
item A2 (`docs/prd/productune.md`, "A2 — Artifacts tab (new)").

- **In**: left list → main-panel viewer for `docs/prd/`, `docs/artifacts/<version>/`,
  `docs/designer/`. md → md viewer (code-block syntax highlight); mermaid → mermaid viewer;
  html → browser-style view via `<webview>` (reuse existing `browser` tab type).
- **Out**: editing artifacts (read-only this version); rich nav / TOC template (→ v0.6).
- Reuse the v0.4 design system + the Phase-4 Mermaid viewer.

### Request (KR)

A2 산출물 뷰어 설계 — 좌측 새 탭(목록) → 메인 패널 뷰어. `docs/prd/`,
`docs/artifacts/<version>/`, `docs/designer/` 의 산출물을 GUI 안에서 열람.
md → md 뷰어(코드블록 하이라이트) / mermaid → mermaid 뷰어 / html → `<webview>` 브라우저 뷰.
편집 기능 제외(읽기 전용), 풍부한 목차 템플릿 제외(v0.6). v0.4 디자인 시스템 재사용.

## Acceptance (P2 design deliverables)

- [ ] User flow — `docs/artifacts/v0.5/T-002-a2-flow.md` (entry → list → viewer per type).
- [ ] Hi-fi mockup — `docs/artifacts/v0.5/T-002-a2-mockup.html` (frontend-design skill;
      productune-internal → lucide-react, color emoji 금지).
- [ ] DS compliance — uses v0.4 design-system tokens; no net-new DS.
- [ ] Covers the A2 per-item AC from the PRD (each of md / mermaid / html opens in its
      correct viewer; html in `<webview>`; reachable GUI-only).

## Plan

_pdt-designer authors._

## Outcome

_pdt-designer fills on completion._
