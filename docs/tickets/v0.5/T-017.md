---
ticket_id: T-017
version: v0.5
phase: 3
type: impl
status: done
assignee: pdt-developer
created_at: 2026-06-02T14:00:00Z
started_at: 2026-06-02T15:50:00Z
completed_at: 2026-06-02T16:30:00Z
duration_min: 40
estimated_complexity: L5
qa_status: pass
qa_loops: 2
risk_flags: enum-drift, panel-unlock, broad-component-touch
slug: gui-doctrine-sync
---

# T-017: B1 — GUI ↔ doctrine sync

> v0.5 Track B, item B1. Full spec = **PRD §B1** (`docs/prd/productune.md` lines 379–389).
> Absorbs carried-forward T-P4-146 (MCP server add + rename), T-P4-147 (autosaveTriggers UI enable), T-P4-148 (PersonaDefTab persona-spec edit).

## Request

GUI 가 새 doctrine architecture 를 1:1 로 반영하도록 enum/schema 를 정합화하고, Phase-5 에 잠겨 있던 3개 패널을 해제한다. **doctrine 내용 변경은 범위 밖** (기존 doctrine 의 GUI 반영만).

- 4-tier memory / 9 ticket types / 7 status 를 TeamPanel · TicketsTab · VersionHistoryView · SkillMatrixTab · promotion-drain UI · statusline · file-watcher 전반에 반영.
- 현재 GUI `TaskType` enum 은 8종 (`packages/gui/src/lib/types.ts`) — doctrine 9종과 대조해 누락분 보강.
- 잠긴 3개 패널 해제: MCP server add+rename / autosaveTriggers UI / PersonaDefTab 편집.

## Acceptance (PRD §B1 AC)

- [ ] **[AC-1]** GUI 의 ticket-type / status / tier enum 이 doctrine 과 1:1 일치한다.
- [ ] **[AC-2]** MCP server add + rename 가 GUI 에서 동작한다.
- [ ] **[AC-3]** autosaveTriggers UI 가 활성화되어 있다.
- [ ] **[AC-4]** PersonaDefTab 에서 persona-spec 편집이 가능하다.
- [ ] **[AC-5]** tsc 통과 (`pnpm tsc --noEmit`).

## 추가 발견 (2026-06-02 live dogfood — issue tracker 버그)

B1 범위에 아래 robustness 2건을 포함한다 (PO 가 dogfood 중 발견):

- **R1 — 티켓 로더 파일명 정규식 (PO 가 선조치 완료)**: `electron/ipc/tickets.ts` 의 `/^T-[A-Z0-9-]+\.md$/` 가 소문자 든 파일명(`T-P4-048-em.md`)을 silently drop → 티켓 유실. PO 가 `/^T-[A-Za-z0-9-]+\.md$/` 로 수정함 (working tree, B1 커밋에 포함). B1 developer 는 이 변경을 유지 + 회귀 테스트 관점 확인.
- **R2 — status 어휘 분열**: `designer/ticket-schema.md` = `planned|in-progress|qa-pending|user-pending|done|blocked|cancelled` vs PO lifecycle + GUI = `todo|in-progress|review|user-verify|done|blocked|abandoned`. 같은 7-status 를 다른 단어로 표현 → designer-authored 티켓이 GUI 에서 "unknown status" fallback.
  - GUI: legacy synonym map 추가 (`planned→todo`, `qa-pending→review`, `user-pending→user-verify`, `cancelled→abandoned`) 로 방어 — unknown 으로 떨구지 말고 canonical 로 매핑.
  - doctrine: `designer/ticket-schema.md` 의 status enum 을 canonical 7 종으로 통일 — **이건 doctrine content 변경이므로 PO 가 doctrine-editing 플로우로 별도 처리** (developer 범위 아님).

## Out of scope

- doctrine *content* 변경 (이건 기존 doctrine 의 GUI 반영 작업) — 단, R2 의 `ticket-schema.md` 통일은 PO 가 doctrine-editing 으로 별도 수행.
