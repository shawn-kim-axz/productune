---
ticket_id: T-P4-011
version: v0.4
round: phase4-r1
type: impl
status: done
phase: 4
assignee: pdt-developer
created_at: 2026-05-04T02:00:00Z
started_at: 2026-05-04T03:30:00Z
completed_at: 2026-05-04T04:00:00Z
duration_min: null
estimated_complexity: L4
risk_flags: none
slug: slug-mode
qa_status: pending
qa_loops: 0
---

# T-P4-011: [새 프로젝트 만들기] — slug 입력 + mode 선택 + 폴더 자동 생성

**Round**: phase4-r1  **Stage**: impl  **Status**: todo  **Assignee**: pdt-developer

> Explorer 패널에서 "새 프로젝트 만들기" 클릭 시 wizard. planner/developer 선택 후 `~/productune/projects/<slug>/` 자동 생성 + init 실행.

## Request

### 1. IPC: 프로젝트 폴더 생성
- `ipcMain.handle('project:create', (_, { slug, mode }) => ...)` 
- `~/productune/projects/<slug>/` 생성 (slug 충돌 시 `-2`, `-3` suffix)
- `initProject()` 호출 → config 생성
- 생성된 경로 반환

### 2. GUI — NewProjectModal (`src/components/NewProjectModal.tsx`)
- Step 1: slug 입력 (영소문자+하이픈 validation)
- Step 2: planner / developer 라디오 선택 + 각 모드 1줄 설명
- [만들기] 클릭 → `api.createProject({ slug, mode })` → 성공 시 workspace 진입

### 3. App.tsx 상태 연동
- 생성 완료 → `currentProject` state 업데이트 → WorkspaceView 렌더

## Acceptance
- [ ] `~/productune/projects/<slug>/` 디렉터리 생성됨
- [ ] `.productune/config.json` 존재 (mode, slug 포함)
- [ ] slug 충돌 → suffix 자동 부여
- [ ] slug validation: 영소문자/숫자/하이픈만, 2자 이상
- [ ] modal → 성공 → workspace view 전환
