---
ticket_id: T-P4-081
version: v0.4
round: phase4-r4-fix
type: impl
status: done
phase: 4
assignee: pdt-developer
created_at: 2026-05-11T00:00:00Z
started_at: 2026-05-11T00:00:00Z
completed_at: 2026-05-11T00:00:00Z
duration_min: null
estimated_complexity: L2
risk_flags: ux-overlap, derive-root-cause, debug-instrumentation
bundle: T-P4-R4-fix
slug: presence-bar-badge-overlap-dev-derive-and-multi-done
qa_status: passed
qa_loops: 1
---

# T-P4-081: PresenceBar count badge overlap + dev derive 재진단 + multi-done artifact 합치기

## Background

사용자 dogfood (2026-05-11) — T-P4-080 land 후 manual 검증 중 3건 추가 발견:

1. **count badge 가 chip 라벨 텍스트 가림** — `designer` 라벨의 "er" 부분 위에 14×14 "2" 배지가 겹쳐 보임 (T-P4-068 §E badge spec 의 `right: -2` positioning). 사용자 directive: "숫자가 가리지 않게".
2. **dev derive 여전히 미작동** — T-P4-080 §C 의 `pdt-dev` case 추가에도 dev dot 회색 그대로. PO 가 실제로 `--agent pdt-developer` 또는 `pdt-dev` 둘 중 어느 string 으로 spawn 하는지, 그리고 store 에 task.persona 가 어떤 값으로 들어가는지 진단 필요. 본 ticket 에 임시 console.log 또는 window 노출 path 추가.
3. **designer × 2 done 후 PresenceBar chip 의 done tooltip 에 1 task 만 표시** — T-P4-076 derive 의 done 분기가 `doneTasks[0]` (last 1개) 의 description 만 `setPersonaState('done', artifact)` 로 전달. 사용자 의도: designer × 2 done 시 둘 다 보이게 (또는 count + 합쳐서).

## Doctrine references

- T-P4-068 §E — chip count badge spec (14×14, `right: -2`, persona color bg, 2px outline)
- T-P4-076 derive logic — `usePersonaPresenceDerive` 의 done transition 분기
- T-P4-077 normalizeBgPersona — `pdt-developer` / `pdt-dev` 매핑
- T-P4-080 §C — dev derive case 추가 (불완전)
- `docs/designer/design-system.md` §1.5.4 Feedback / §7 components / §1.5.3 Predictability

## Acceptance

### A) count badge — 라벨 occlude 회피

- [ ] `packages/gui/src/components/workspace/PersonaPresenceBar.tsx` 의 `PersonaChip` 의 badge 위치 / chip layout 조정:
  - 옵션 1 (권장): chip container 의 `padding-right` 를 14px 정도 추가 + badge `right: 0` 유지 → 라벨이 badge 영역 미점유
  - 옵션 2: badge 를 absolute 가 아닌 inline-flex 자식으로 chip 안 라벨 다음에 push (gap 사용)
  - 가장 간결한 path 선택 (over-engineering 회피). 회귀: 라벨 text + badge 가 시각적으로 겹치지 않아야.
- [ ] badge 사이즈 / 색상 / outline / 폰트 spec (T-P4-068 §E) 변경 X — positioning 만.
- [ ] count 0 → badge X / 1 → X / ≥2 → 숫자 (기존 정합 유지)

### B) dev derive root cause 재진단 + fix

본 ticket 에 진단 instrumentation 일시 추가 (이번 turn 안에서 사용자 검증 후 제거):

- [ ] `packages/gui/src/store/useBackgroundTasks.ts` 끝에 dev-only window 노출 추가:
  ```ts
  if (typeof window !== 'undefined') {
    (window as any).__bgTasks = useBackgroundTasks
  }
  ```
  사용자가 DevTools console 에서 `__bgTasks.getState().tasks` 로 store 상태 직접 확인 가능.
- [ ] `packages/gui/electron/po-runner.ts` 의 Bash 분기 (line 414~) regex 매칭 직후 임시 `console.log` 추가:
  ```ts
  console.log('[bg-task] matched agent string:', match[1])
  ```
  → main process console (terminal) 에 어떤 agent string 이 매칭되는지 dump.
- [ ] 사용자가 이 instrumentation 으로 root cause 확인 후 (e.g. `pdt-developer` 인지 `pdt-dev` 인지 다른 string 인지) — 이번 turn 안에서 dev 가 fix:
  - case (a) PO 가 다른 string (예: `pdt-developer` w/ 추가 suffix) → normalizeBgPersona regex/조건 보강
  - case (b) PO 가 정상 string 인데 store/derive 에서 mismatch → 코드 흐름 추적 후 fix
  - case (c) other timing/race issue → 패턴 식별 후 fix
- [ ] fix 완료 후 임시 console.log / window 노출 instrumentation 모두 **제거** (memory rule: no debug shim 영구 유지).

### C) multi-done artifact 합치기 — `usePersonaPresenceDerive`

- [ ] `packages/gui/src/components/workspace/PersonaPresenceBar.tsx` 의 `usePersonaPresenceDerive` 의 done 분기 (line ~54~73 부근) 수정:
  - 현재: 마지막 1개 task 의 description 만 `setPersonaState('done', last.description)`
  - 변경: 미처리 done/error task **모두** processedDoneIds 에 add + description 들을 `", "` 로 join 후 60c truncate
  - 예: designer A done ("§1.5.1 요약") + designer B done ("§1.5.5 요약") → tooltip "§1.5.1 요약, §1.5.5 요약" (60c 안에서 잘림)
- [ ] 회귀: single task done 정합 (1 task 면 join 결과 = single description), processedDoneIds Set 중복 방지 정합.
- [ ] count badge 는 working 시에만 (≥2) — 기존 정합 유지. done 시 badge 없음 (사용자 OQ 명시 X — 현 동작 유지).

### D) 회귀

- [ ] T-P4-076 derive 의 working transition 정합 (dev fix 가 designer / qa case 손상 X)
- [ ] T-P4-068 / T-P4-078 / T-P4-080 segment + chip hover popup 정합
- [ ] T-P4-077 BG IPC trigger 정합
- [ ] T-P4-079 multi-bubble chat 정합
- [ ] chip done 후 외부 click idle 복귀 (T-P4-049 dismiss 정합)

### E) 검증

- [ ] `cd packages/gui && pnpm exec tsc --noEmit` exit 0
- [ ] `cd packages/gui && pnpm build` exit 0
- [ ] locale check 통과 (변경 없음 예상)
- [ ] manual:
  - badge: designer × 2 dispatch 시 "2" 배지가 "designer" 라벨 위에 겹치지 않음
  - dev derive: dev 1명 dispatch → dev chip working blink + done 후 ✓
  - multi-done: designer × 2 done → designer chip tooltip 에 두 task description 합쳐 표시 (e.g. "§1.5.1 요약, §1.5.5 요약")
  - instrumentation 제거 confirm — code 에 console.log / `__bgTasks` window 노출 잔여 없음

## QA scenarios (light)

1. **badge non-overlap** — designer × 2 → "2" badge 가 "designer" 라벨 끝 너머 / 시각 겹침 없음
2. **dev derive fix** — dev 1명 spawn → PresenceBar dev dot working blink
3. **multi-done artifact** — designer × 2 done → tooltip "<desc A>, <desc B>" (60c truncate)
4. **single-done artifact** — designer × 1 done → tooltip "<desc>" (회귀 X)
5. **single working** — dev 1명 → dev dot blink, badge X (count 1)
6. **mixed working/done** — designer working + dev done → designer working blink + dev done ✓
7. **instrumentation cleanup** — 코드 grep 에 `__bgTasks` / `[bg-task] matched` 잔여 0

## Out of scope

- chip 의 시각 layout 전반 redesign (T-P4-049 spec 정합 유지)
- popup 의 row 표시 format 변경 (T-P4-068 §C 정합)
- PO doctrine 변경 (spawn 명령 형식 그대로)
- light theme

## Notes

- 사용자 dogfood (2026-05-11): T-P4-080 land 후 manual 검증 중 발견.
- L2 (badge positioning + dev derive 재진단 + multi-done 합치기 3 spot).
- §B 의 디버그 instrumentation 은 1-turn 한정 — fix 완료 후 즉시 제거 (memory rule). 사용자 검증 round 안에 dev 가 root cause 식별 + fix + cleanup 묶음.

## Persona Activity

| turn | persona | action | output |
|---|---|---|---|
| 1 | pdt-developer | impl + debug | badge: chipStyle paddingRight+16 when showBadge + badge right:0 (라벨 비침); dev derive root cause: normalizePersona에 `p === 'dev'` exact match 누락 → 추가 (WorkspaceShell이 IPC로 'dev' string 받아 재정규화 시 unknown 반환하던 문제); multi-done: doneTasks forEach all add + description join. instrumentation 없음(코드 분석으로 root cause 식별). tsc + build pass. |
