---
ticket_id: T-P4-160
version: v0.4
slug: ticket-folder-version-naming
type: impl
status: done
assignee: pdt-designer
created_at: 2026-05-21
estimated_complexity: S
risk_flags: []
---

| T-P4-160 | ticket-folder-version-naming | done |

## Request

Codify the rule that ticket files are emitted to `docs/tickets/<version>/T-NNN.md` where `<version>` = `po-state.current_version`. Add explicit folder auto-creation rule. Sync across all doc sources + designer variants.

## Inputs

- Background: `docs/tickets/v0.4/` rename completed in Phase 4. Next cycle (v1.0-mvp etc.) must auto-use correct versioned folder.
- Files affected: `sections/tickets.md`, `sections/_formats/ticket-frontmatter.md`, `sections/prd-and-output.md`, designer variants (fs/graphiti/keeper), `~/.claude/agents/pdt-designer.md` (blocked — see Outcome).

## Acceptance

- [x] `sections/tickets.md` §"Ticket file format" — explicit T-P4-160 folder rule added: `<version>` = `po-state.current_version`, auto-create folder, one `<version>` slug covers artifacts + tickets.
- [x] `sections/tickets.md` Plan doctrine — outdated sub-folder reference (`docs/artifacts/<version>/<ticket_id>/`) replaced with pointer to T-P4-153 path rule.
- [x] `sections/_formats/ticket-frontmatter.md` §"Ticket emit sequence" — folder rule note added.
- [x] `sections/prd-and-output.md` §"Artifact output paths" — "Ticket output path (T-P4-160)" parallel rule added.
- [x] `packages/core/po/sections/` mirror — all 3 files synced (tickets.md, ticket-frontmatter.md, prd-and-output.md).
- [x] Designer variants fs/graphiti/keeper — `Tickets:` line updated: `<version>` = `[ctx].version` (fallback: `po-state.current_version`); auto-create folder note added. Ticket path comment block added.
- [ ] `~/.claude/agents/pdt-designer.md` — **BLOCKED** by auto-mode self-modification guard. User must apply the diff manually (see Outcome §Manual patch).

## Out of scope

- Changing ticket id scheme (T-NNN monotonic, unchanged).
- Migrating existing `v0.4/` tickets.
- Per-project version override (future).

## §Plan

Designer direct-edit of section docs + variant files. No code changes. Mirror diff verified by file content parity.

## Outcome

All section docs + 3 variant files updated. `~/.claude/agents/pdt-designer.md` requires manual patch by user:

**Manual patch — `~/.claude/agents/pdt-designer.md`** (after PRD authoring `Tickets:` line):

Replace:
```
Tickets: start from `next_ticket_id` in `[ctx]`. Files `docs/tickets/<version>/T-NNN.md`.
```

With:
```
Tickets: start from `next_ticket_id` in `[ctx]`. Files `docs/tickets/<version>/T-NNN.md` — `<version>` = `[ctx].version` (fallback: `po-state.current_version`); auto-create folder if absent.
```

Also add after the artifact path rule comment block:
```
<!-- ticket path rule (T-P4-160) -->
Ticket output: `docs/tickets/<version>/T-NNN.md` — `<version>` = `[ctx].version` (fallback: `po-state.current_version`). Auto-create `docs/tickets/<version>/` if absent.
```

## Persona Activity

| Date | Persona | Result |
|---|---|---|
| 2026-05-21 | pdt-designer | Emitted + all docs updated; ~/.claude/agents manual patch surfaced |
