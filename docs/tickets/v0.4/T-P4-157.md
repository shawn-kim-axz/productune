---
ticket_id: T-P4-157
title: "Doctrine update: artifact flat-naming (3 categories) + po-state hygiene generalization"
version: v0.4
round: phase4
type: design
status: done
phase: 4
assignee: pdt-designer
estimated_complexity: L5
model: opus
effort: xhigh
risk_flags: none
slug: doctrine-artifact-path-hygiene
area_tags: [doctrine, po-state, artifacts, productune]
depends_on: [T-P4-153, T-P4-151]
created_at: 2026-05-21
---

# T-P4-157 — Doctrine update: artifact flat-naming + po-state hygiene generalization

## §1 Request

User 결정 (2026-05-21): 2 doctrine 영역 통합 업데이트.

1. **Artifact path rule** (T-P4-153 수정): sub-folder 방식 → 3-category flat naming.
   - Ticket: `docs/artifacts/<version>/<ticket-id>-<slug>.<ext>`
   - Version-loose: `docs/artifacts/<version>/<slug>.<ext>`
   - Global: `docs/artifacts/<slug>.<ext>`
   - Persona-maintained: `docs/<persona>/<slug>.<ext>` (artifacts 아님)
2. **Po-state hygiene 일반화** (T-P4-151 룰 교체): H1/H2/H3 → 단일 field staleness sweep. 5개 필드 커버.

---

## §2 Acceptance

### A1 — prd-and-output.md artifact rule 업데이트

3-category table (Ticket / Version-loose / Global) + persona-maintained note 존재.  
`grep "T-P4-153" ~/.productune/sections/prd-and-output.md` → 1 match.

### A2 — po-loop.md 단일 H 룰

H1/H2/H3 헤더 제거, 단일 `**Turn-start hygiene**` step.  
`grep "Field staleness sweep" ~/.productune/sections/po-loop.md` → 1 match.

### A3 — po-state-schema.md "Field staleness sweep" 섹션

`## Size budget` → `## Field staleness sweep` 교체.  
5개 필드 행 포함: `past_tickets` / `recent_turns` / `phase_history` / `pending_gate` / `versions[].outcome.observed_result`.  
Key paths 인라인 H1/H2/H3 라벨 → "Staleness hygiene" 교체.

### A4 — designer.md 3개 변형 패치

`<!-- artifact path rule (T-P4-153) -->` 블록 → 새 3-category flat 패턴.  
`type:design 4-artifact set` 경로 예시 → 새 flat 패턴 반영.

### A5 — mirror diff empty

`packages/core/po/sections/prd-and-output.md` ≡ `~/.productune/sections/prd-and-output.md`  
`packages/core/po/sections/po-loop.md` ≡ `~/.productune/sections/po-loop.md`  
`packages/core/po/sections/_formats/po-state-schema.md` ≡ `~/.productune/sections/_formats/po-state-schema.md`

---

## §Plan

### 파일 목록 (9개)

| # | 파일 | 변경 내용 |
|:--|:--|:--|
| 1 | `~/.productune/sections/prd-and-output.md` | `## Artifact output paths` → 3-category table + persona-maintained note |
| 2 | `~/.productune/sections/po-loop.md` | Step 1.1 H1/H2/H3 → 단일 H + 3 LLM-driven checks |
| 3 | `~/.productune/sections/_formats/po-state-schema.md` | `## Size budget` → `## Field staleness sweep`; Key paths H-label 교체 |
| 4 | `packages/core/po/sections/prd-and-output.md` | mirror of #1 |
| 5 | `packages/core/po/sections/po-loop.md` | mirror of #2 |
| 6 | `packages/core/po/sections/_formats/po-state-schema.md` | mirror of #3 |
| 7 | `packages/core/agents/variants/fs/pdt-designer.md` | artifact path comment + type:design 4-artifact set paths |
| 8 | `packages/core/agents/variants/graphiti/pdt-designer.md` | same |
| 9 | `packages/core/agents/variants/keeper/pdt-designer.md` | same |

### 실행 순서

1. T-P4-157 티켓 작성
2. #1 → #2 → #3 (source: ~/.productune)
3. #4 → #5 → #6 (mirror: packages/core/po)
4. #7 → #8 → #9 (designer variants)

---

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | grep verify only (no smoke gate) |
| **test target** | 9개 파일 변경; `grep "Field staleness sweep"` in po-loop.md → 1 match; `grep "T-P4-153"` in prd-and-output.md → 1 match; mirror diff empty |

---

## §Out of scope

- po-state.json 스키마 필드 추가/삭제 (field 정의 변경 아님 — 룰 문서화만)
- phase_history[] / versions[] 실제 jq 스크립트 자동화
- CLAUDE.md / system-prompt 파일 직접 수정 (variant md 업데이트로 갈음)

---

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-21T00:00Z | pdt-designer | opus/xhigh | 1 | plan inline §Plan (T-P4-157) + 9-file doctrine update |
