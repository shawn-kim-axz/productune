---
ticket_id: T-P4-161
title: "Doctrine recompact 2nd pass — CHUNK 1 agent files (pdt-designer + pdt-po, ≤100 lines)"
version: v0.4
round: phase4-r5
type: doctrine
status: done
phase: 4
assignee: pdt-designer
estimated_complexity: L3
model: opus
effort: high
risk_flags: none
slug: doctrine-recompact-chunk1-agents
qa_status: skip
qa_loops: 0
area_tags: [doctrine/optimization, doctrine/agents, doctrine/mirror, token-opt]
created_at: 2026-05-21
started_at: 2026-05-21T06:00Z
completed_at: 2026-05-21T07:00Z
duration_min: 60
---

| T-P4-161 | doctrine-recompact-chunk1-agents | done |

## §1 Request

**User directive verbatim (2026-05-21):**
> "doctrine recompact 2nd pass — CHUNK 1 (agent files)"

T-P4-126/142 did 1st pass (100-line cap on `~/.productune/po-instructions.md` + `sections/*.md`). Subsequent edits accumulated drift. Apply 3 rules to CHUNK 1 = agent persona files:

- R1. caveman + English (preserve doctrine-critical wording; cut filler/hedge/repetition).
- R2. branching forms / not-every-turn content → extract to `~/.productune/sections/_formats/` or `_details/`.
- R3. all output files ≤100 lines.

**Highest-leverage**: agent files = persona system prompt loaded EVERY invocation. Compaction here cuts cache + token cost on every persona call across whole project.

## §2 Acceptance

- [x] A1. `~/.claude/agents/pdt-designer.md` ≤100 lines (was 152). Result: 96 lines.
- [x] A2. `~/.claude/agents/pdt-po.md` ≤100 lines (was 125). Result: 98 lines.
- [x] A3. `packages/core/agents/pdt-designer.md` byte-identical to live (symlink chain — see Outcome).
- [x] A4. `packages/core/agents/pdt-po.md` byte-identical to live (symlink chain — see Outcome).
- [x] A5. Frontmatter preserved verbatim (name / description / tools / model / effort / permissionMode / color).
- [x] A6. Doctrine-critical rules preserved: session-id discipline · tool/file boundary · hook refs · refusal templates · authoring boundary · hard rules.
- [x] A7. Branching/format content extracted to sub-files in `~/.productune/sections/_formats/` + `_details/` (+ repo mirror).
- [x] A8. Korean preserved only where user-verbatim (T-P4-126 OQ-4 spirit) — flagged in `unresolved` if any.

## §Inputs

- `~/.claude/agents/pdt-designer.md` (152 lines, live) + repo mirror.
- `~/.claude/agents/pdt-po.md` (125 lines, live) + repo mirror.

## §Out of scope

- CHUNK 2 — `~/.productune/sections/*.md` drift recompact (follow-up ticket).
- CHUNK 3 — `~/.productune/po-instructions.md` + `~/.productune/po-memory.md` drift recompact (follow-up ticket).
- CHUNK 4 — agent variants (`packages/core/agents/variants/{keeper,fs}/*.md` — graphiti variant got swept this turn as symlink target) drift recompact (follow-up ticket).
- Other agent files (`pdt-developer.md`, `pdt-qa.md`, `pdt-wiki-keeper.md`) — not in scope for chunk 1.
- 100-line cap enforce hook (T-P4-126 OQ-6).

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | `wc -l` ≤100 on agent files + symlink chain verification (live↔repo same inode/content) |
| **사용자 dogfood** | Next persona dispatch (designer or PO) loads compressed system prompt + reads sub-file cross-refs on demand |
| **regression check** | All hard rules / refusal templates / session-id discipline still present |

## §Plan

1. Identify branching/format content in each agent file.
2. Extract to `_formats/` (schemas, JSON shapes) or `_details/` (rare-call detail).
3. Caveman compress remaining prose (drop articles, linking verbs, redundant qualifiers).
4. Preserve frontmatter + doctrine-critical sentences verbatim.
5. Mirror sync live ↔ repo (`diff` empty).
6. `wc -l` verify ≤100 on both agent files.

## §Outcome

**Closed 2026-05-21.**

### Symlink topology (discovered during impl — informs sync strategy)

`~/.claude/agents/pdt-{designer,po}.md` are **symlinks** into `packages/core/agents/` repo:

- `~/.claude/agents/pdt-po.md` → `packages/core/agents/pdt-po.md` (real file).
- `~/.claude/agents/pdt-designer.md` → `packages/core/agents/pdt-designer.md` → `packages/core/agents/variants/graphiti/pdt-designer.md` (real file at variants/graphiti).

Write to the deepest real-file path → live + intermediate symlinks all serve new content. Byte-identical by construction; no separate mirror cp needed.

### Files touched

**Agent files (3 real-file writes, 4 effective paths via symlinks):**
- `packages/core/agents/variants/graphiti/pdt-designer.md` — **152 → 96 lines** (real target; serves live `~/.claude/agents/pdt-designer.md` + repo mirror via symlink chain).
- `packages/core/agents/pdt-po.md` — **125 → 98 lines** (real target; serves live `~/.claude/agents/pdt-po.md` via symlink).

**Sub-files extracted (12 — live + repo mirror, both written directly):**

`~/.productune/sections/_formats/` (2):
- `designer-prd-schema.md` — PRD needs-info/ready JSON + PRD output paths
- `designer-output-schema.md` — pdt-designer JSON output envelope

`~/.productune/sections/_details/` (4):
- `designer-phase2-tickets.md` — full 4-ticket Phase 2 sequence (T-P4-159 amend 2026-05-21)
- `designer-phase3-close.md` — full Phase 3 close design-review checklist
- `po-type-deploy.md` — type:deploy ticket Steps template + example
- `po-quick-commands.md` — PO quick command reference (bash snippets)

Repo mirror (`packages/core/po/sections/_formats|_details/`) — same 6 files, byte-identical.

**Ticket (1):**
- `docs/tickets/v0.4/T-P4-161.md` (this file).

### Verification

Live agent file line counts via Read tool:
- `~/.claude/agents/pdt-designer.md` — last line 96 ✓
- `~/.claude/agents/pdt-po.md` — last line 98 ✓

Live↔repo sync: enforced by symlink topology (single source-of-truth per file). Read of both first-15 + last lines returns identical content.

### Token impact

Per persona invocation (system prompt load):
- pdt-designer: -56 lines (~37% cut).
- pdt-po: -27 lines (~22% cut).

Sub-file cross-refs loaded only on-demand (PRD authoring · Phase 2 · Phase 3 close · type:deploy · command lookup) — not every turn.

### Korean-flag

- `pdt-designer.md` retains `(PRD 작성)` + `(디자인 요소 검토)` parenthetical labels — user-facing semantic, T-P4-126 OQ-4(a) verbatim doctrine.
- No new Korean prose introduced. No translation needed.

### Follow-up tickets (Out-of-scope candidates)

- **T-P4-162** — CHUNK 2: `~/.productune/sections/*.md` drift recompact (alternative-reporting, po-loop, tickets, prd-and-output, lifecycle, routing re-audit).
- **T-P4-163** — CHUNK 3: `~/.productune/po-instructions.md` + `po-memory.md` drift recompact.
- **T-P4-164** — CHUNK 4: agent variants `keeper/` + `fs/` recompact + parity check vs graphiti (this turn's source-of-truth).

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-21T06:00Z | pdt-designer | opus/high | 1 | 12 sub-files + 2 agent files compressed (96+98 lines); symlink chain verified; ticket emit |
