---
ticket_id: T-P4-142
title: "Doctrine cap: sections/*.md ≤100-line enforcement + Korean residue audit (T-P4-126 residual)"
version: v0.4
round: phase4-r4
type: impl
status: done
phase: 4
assignee: pdt-developer
estimated_complexity: L2
model: sonnet
effort: medium
risk_flags: none
slug: doctrine-over-cap-cleanup
area_tags: [doctrine/sections, doctrine/cap]
depends_on: [T-P4-141]
created_at: 2026-05-20
---

## §1 Request

T-P4-126 residual: four `~/.productune/sections/` doctrine files exceed the 100-line cap.
Non-empty content is ≤86 lines each — blank lines cause the over-run. Two Korean-residue
lines audited: both are verbatim user-phrase examples → preserved per OQ-4, no edit.

**Files over cap**: `routing.md` (101) · `lifecycle.md` (109) · `prd-and-output.md` (111) ·
`tickets.md` (116).

**Strategy**: blank-line removal for the smallest over-cap (routing.md); section extraction
to `_details/` / `_formats/` sub-files for the rest (sub-files are cap-exempt).

## §2 Acceptance

### A1. `~/.productune/sections/routing.md` ≤100 lines

Remove 1 blank line (blank immediately after `# Model + Effort routing` heading).

```bash
wc -l ~/.productune/sections/routing.md  # must print ≤100
```

### A2. `~/.productune/sections/lifecycle.md` ≤100 lines

`---` separator + `## Timeline / project history` section (≈20 lines) removed from bottom of
file. Replaced with pointer line:

```markdown
## Timeline / project history → `sections/_details/lifecycle-timeline.md`
```

```bash
wc -l ~/.productune/sections/lifecycle.md  # must print ≤100
```

### A3. `~/.productune/sections/_details/lifecycle-timeline.md` created

New file contains the full Timeline / project history section content (template code block +
R2 git-workflow note). Content: verbatim move, no rewrite.

### A4. `~/.productune/sections/prd-and-output.md` ≤100 lines

`## Output shape to user` section (≈29 lines) removed from bottom of file. Replaced with:

```markdown
## Output shape to user → `sections/_formats/po-output-format.md`
```

```bash
wc -l ~/.productune/sections/prd-and-output.md  # must print ≤100
```

### A5. `~/.productune/sections/_formats/po-output-format.md` created

New file contains the full Output shape to user section content (Normal turn template +
Clarity-loop iteration turn + Feedback turn). Content: verbatim move, no rewrite.

### A6. `~/.productune/sections/tickets.md` ≤100 lines

Two changes combined:

**(a) Extract `## Ticket id allocation`** — last section removed (13 lines), replaced with
compressed inline summary + sub-file pointer (3 lines). Net: saves 10 lines.

```markdown
## Ticket id allocation → `sections/_formats/ticket-id-allocation.md`

`T-NNN` zero-padded, monotonic, never resets. Designer reads `[ctx].next_ticket_id`; absent
→ fs scan fallback (script in sub-file). PO embeds computed id in `[ctx]`.
```

**(b) Remove 6 blank lines** from inter-section gaps in the body. Do NOT touch blanks inside
bash or code blocks. `grep -n "^$" tickets.md` to identify candidates.

```bash
wc -l ~/.productune/sections/tickets.md  # must print ≤100
```

### A7. `~/.productune/sections/_formats/ticket-id-allocation.md` created

Contains full `## Ticket id allocation` content: prose + bash scan script + v2 doctrine note.
Verbatim move.

### A8. Mirror sync: `packages/core/po/sections/` updated

All modified + newly created files copied to `packages/core/po/sections/`:

```bash
SECT=~/.productune/sections
DEST=packages/core/po/sections

for f in routing.md lifecycle.md prd-and-output.md tickets.md; do
  cp "$SECT/$f" "$DEST/$f"
done

mkdir -p "$DEST/_details" "$DEST/_formats"
cp "$SECT/_details/lifecycle-timeline.md"    "$DEST/_details/lifecycle-timeline.md"
cp "$SECT/_formats/po-output-format.md"      "$DEST/_formats/po-output-format.md"
cp "$SECT/_formats/ticket-id-allocation.md"  "$DEST/_formats/ticket-id-allocation.md"
```

`git status` shows 7 modified/new files under `packages/core/po/sections/`.

### A9. Korean residue — no change

- `alternative-reporting.md` L41: `"근거" / "왜"` preserved (verbatim user-phrase examples).
- `memory.md` L57: `"오 이제 잘 되네"` preserved (verbatim user phrase in schema example).
- Neither file edited.

## §Inputs

- `docs/artifacts/v0.4/T-P4-142/plan.md` — full compression specs (§C-1 through §C-4 + mirror sync)
- `~/.productune/sections/routing.md` — read before edit
- `~/.productune/sections/lifecycle.md` — read before edit
- `~/.productune/sections/prd-and-output.md` — read before edit
- `~/.productune/sections/tickets.md` — read before edit (note: T-P4-141 may modify L97 first)
- `packages/core/po/sections/` — verify directory structure before mirror sync

## §Out of scope

- `alternative-reporting.md`, `memory.md` — Korean residue audited; no edit.
- Content changes to extracted sections (verbatim move only).
- Other `_details/` / `_formats/` sub-files (already cap-exempt).
- `packages/core/agents/variants/*.md` — T-P4-141 scope.

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | — |
| **사용자 dogfood** | — |
| **regression check** | — |



---

## §Plan

<!-- migrated from docs/artifacts/v0.4/T-P4-142/plan.md -->

---
ticket: T-P4-142
title: "Doctrine cap: 4 over-cap sections/*.md → ≤100 lines + Korean residue audit"
version: v0.4
round: phase4-r4
plan_version: v1
created_at: 2026-05-20
---

### §Context

T-P4-126 residual: four `~/.productune/sections/` files exceed the 100-line cap. Blank lines
(not content) are the primary culprit — non-empty lines are all ≤86 per file. Fix strategy:
smallest possible semantic loss. Two Korean-residue lines audited; both are verbatim user-phrase
examples — preserve per T-P4-126 OQ-4.

**Cap rule**: `~/.productune/sections/*.md` (main files) MUST be ≤100 lines. Sub-files
(`_details/`, `_formats/`) are exempt from cap — they exist specifically to hold overflow detail.

---

### §Korean residue audit — result: no change needed

| File | Line | Content | Classification | Action |
|:--|:--|:--|:--|:--|
| `alternative-reporting.md` | L41 | `"근거" / "왜"` | Verbatim user-phrase pushback examples | **Preserve** (OQ-4 exception) |
| `memory.md` | L57 | `"오 이제 잘 되네"` | Verbatim user phrase in `## Product taste` schema example | **Preserve** (OQ-4 exception) |

No edits to `alternative-reporting.md` or `memory.md`. ✓

---

### §Compression changes (4 files)

#### C-1. `~/.productune/sections/routing.md` (101 → 100)

**Strategy**: remove 1 blank line — minimal change.

Remove the blank line immediately after `# Model + Effort routing` (first line of file). The
heading reads directly into the intro paragraph without whitespace loss elsewhere.

```diff
 # Model + Effort routing
-
 PO picks model (haiku/sonnet/opus) + effort...
```

Result: 100 lines. ✓

---

#### C-2. `~/.productune/sections/lifecycle.md` (109 → ≈88)

**Strategy**: extract `## Timeline / project history` section (plus `---` separator) to a new
sub-file. This section is a self-contained rendering spec triggered only when user asks for
project history — ideal extraction candidate.

##### C-2a. Remove from `lifecycle.md`

Delete from `---` separator through end of file (lines 89–110 approximately — the `---`
separator line + `## Timeline / project history` heading + all content below).

Replace with single pointer line:

```markdown
## Timeline / project history → `sections/_details/lifecycle-timeline.md`
```

Net: removes ≈21 lines, adds 1 → lifecycle.md ≈ 89 lines. ✓

##### C-2b. Create `~/.productune/sections/_details/lifecycle-timeline.md`

```markdown
# Timeline / project history

User asks for project history ("what have we done", "show timeline", "summary so far") —
**never invoke persona, never `git log`**. Source = fs scan of `docs/tickets/**/*.md` +
`current_task` + `versions[]` in state. Sort by `started_at`, render in user's lang per template:

```
### Project timeline (<repo>)

<started_at> – <ended_at>  <slug>  [<final_status>]
  request : <request_summary>
  flow    : <personas in order, pass/fail>
  artifacts: <artifacts>
  outcome : <outcome_summary>

in progress: <current_task.slug>  [in-progress]
```

Detail beyond summary: read PRD `docs/prd/<slug>.md`, persona notes, or
`git log --since=<task.started_at> --until=<task.ended_at> -- <artifacts>`.
`claude --resume` past session = last resort.

**R2 git-workflow**: ticket-level commit detail = `git -C <worktree_path> log --oneline`
(worktree-isolated). Timeline itself derived from fs scan of ticket md files.
```

---

#### C-3. `~/.productune/sections/prd-and-output.md` (111 → ≈82)

**Strategy**: extract `## Output shape to user` section to a new sub-file. This section
contains PO-facing output templates (format reference, not doctrine logic) — natural extract.

##### C-3a. Remove from `prd-and-output.md`

Delete `## Output shape to user` heading through end of file (lines 83–112 approximately).

Replace with single pointer line:

```markdown
## Output shape to user → `sections/_formats/po-output-format.md`
```

Net: removes ≈29 lines, adds 1 → prd-and-output.md ≈ 83 lines. ✓

##### C-3b. Create `~/.productune/sections/_formats/po-output-format.md`

```markdown
# Output shape to user

User sees user's working lang (caveman lite). Code/path tokens unchanged.

**Normal turn** (Designer + Developer cycle):

```
PRD: docs/prd/<slug>.md (A=0.04, status: Version 1 draft)

### Changes
- <file>: <what>

### Design compliance
- ✓ matches intent | ⚠ deviations: ...

### QA
- <check>: <pass/fail>

### Follow-ups
- <open question / manual verify step>
```

**Clarity-loop iteration turn** (Designer waiting on user answer):
```
PRD authoring (iteration <n>/5). Designer's question:
<verbatim from Designer>
```

**Feedback turn**: skip PRD line (user knows where it is); lead with what changed since
their feedback.
```

---

#### C-4. `~/.productune/sections/tickets.md` (116 → ≤100)

**Strategy**: extract `## Ticket id allocation` section (bash block + surrounding text = 13
lines) to a sub-file, then remove 6 blank lines from the remaining file body.

##### C-4a. Extract `## Ticket id allocation` to sub-file

Delete the entire `## Ticket id allocation` section from tickets.md (from `## Ticket id
allocation` heading through end of file — the last section).

Replace with a compressed inline summary + pointer:

```markdown
## Ticket id allocation → `sections/_formats/ticket-id-allocation.md`

`T-NNN` zero-padded, monotonic, never resets. Designer reads `[ctx].next_ticket_id`; absent
→ fs scan fallback (script in sub-file). PO embeds computed id in `[ctx]`.
```

Net: 13 lines → 3 lines = saves 10 → 116 - 10 = 106.

##### C-4b. Create `~/.productune/sections/_formats/ticket-id-allocation.md`

```markdown
# Ticket id allocation

`T-NNN` zero-padded, monotonic, never resets. Designer reads `[ctx].next_ticket_id`; absent
→ fall back to fs scan:

```bash
NEXT=$(node scripts/po/scan-tickets.mjs "$PROJECT_DIR" \
  | jq '([.[].ticket_id // empty, .current_task.ticket_id // empty]
    | map(select(. != null) | sub("^T-(P[0-9]+-)?"; "") | tonumber? // 0) | max // 0) + 1')
TID=$(printf "T-%03d" "$NEXT")
```

PO computes + embeds in `[ctx]`; Designer skips state re-read.
(v2 doctrine: `past_tickets[]` removed — all ticket data lives in ticket md = SoT.)
```

##### C-4c. Remove 6 blank lines from tickets.md body

After C-4a, tickets.md ≈ 106 lines. Remove 6 blank lines to reach ≤100.

Locate blank lines via `grep -n "^$" tickets.md`. Remove 6 gaps between sections —
prioritize double-blank areas and blanks immediately after `##` headings:

Candidates (in order of least disruptive removal):
1. Blank after `> Doctrine = English…` block-quote (before `## Pointer index`)
2. Blank after the Pointer index bullet list (before `## Two axes`)
3. Blank between `## Two axes` paragraph and `## Naming`
4. Blank between `## Naming` bullet list and `## Layer A`
5. Blank after `## Layer B` table footnote line (before `## Ticket file format`)
6. Blank after `PO never authors product content.` line (before `### PO mechanical-write`)

Developer: use `grep -n "^$" ~/.productune/sections/tickets.md` to find exact line numbers;
remove any 6 of the inter-section gaps. Do NOT remove blanks inside bash or code blocks.

Result: 106 - 6 = 100 lines. ✓

---

### §Mirror sync (PO mechanical — post-implementation)

After all `~/.productune/sections/` edits, PO copies changed files to the project repo mirror:

```bash
SECT=~/.productune/sections
DEST=packages/core/po/sections

# Modified main files
for f in routing.md lifecycle.md prd-and-output.md tickets.md; do
  cp "$SECT/$f" "$DEST/$f"
done

# New sub-files (mkdir -p guard)
mkdir -p "$DEST/_details" "$DEST/_formats"
cp "$SECT/_details/lifecycle-timeline.md"       "$DEST/_details/lifecycle-timeline.md"
cp "$SECT/_formats/po-output-format.md"         "$DEST/_formats/po-output-format.md"
cp "$SECT/_formats/ticket-id-allocation.md"     "$DEST/_formats/ticket-id-allocation.md"
```

Verify `packages/core/po/sections/_details/` and `_formats/` exist before running.
If directories absent → `mkdir -p` creates them (included in script above).

---

### §Verification checklist

After implementation, developer runs:

```bash
for f in routing.md lifecycle.md prd-and-output.md tickets.md; do
  wc -l ~/.productune/sections/$f
done
```

All must print ≤100. If any exceed 100: re-examine blank lines in that file.

---

### §Out of scope

- `alternative-reporting.md`, `memory.md` — Korean residue audited; verbatim phrases → no edit.
- Other `_details/` and `_formats/` sub-files — already exempt from cap.
- Content changes to extracted sections (move only; no rewriting).
- `pdt-developer.md` / `pdt-qa.md` variant files (T-P4-141 scope).

---

### §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `skip` |
| **test target** | — |
| **사용자 dogfood** | — |
| **regression check** | — |

Pure doctrine doc restructure — zero user-facing code change. Developer self-verifies with `wc -l`.

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-20T00:00Z | pdt-designer | sonnet/high | 1 | plan.md v1 + ticket v1; Korean residue audit → both preserve; 3 sub-files to create; 4 main files compress |
| 2026-05-20T09:00Z | pdt-designer | sonnet/medium | 2 | impl done — routing/lifecycle/prd-output/tickets ≤100 + 3 sub-files; PO mirror sync ✓ |
