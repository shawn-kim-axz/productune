---
ticket_id: T-P4-154
title: "A1: main.ts IPC handler split — electron/ipc/* modules"
version: v0.4
round: phase4-r4
type: impl
status: done
assignee: pdt-developer
estimated_complexity: L4
model: sonnet
effort: xhigh
risk_flags: none
slug: main-ts-ipc-split
area_tags: [gui/electron, refactor, maintainability]
depends_on: []
created_at: 2026-05-20
---

# T-P4-154 — A1: main.ts IPC handler split

## §1 Request

`packages/gui/electron/main.ts` (2009줄) 를 기능별 IPC module 로 분리.
Pure refactor — 동작 / IPC channel 명 변경 0.

신규: `packages/gui/electron/ipc/` 하위 13개 module.  
`main.ts` → entry (~200줄): import + register() 호출 + app lifecycle 만.

---

## §2 Acceptance

### A1 — ipc/ 디렉토리 구성

```bash
ls packages/gui/electron/ipc/
# → onboarding.ts project.ts tickets.ts state.ts po.ts \
#   mcp.ts hooks.ts skills.ts deploy.ts settings.ts \
#   design.ts explorer.ts worktree.ts
```

### A2 — main.ts 라인 수

```bash
wc -l packages/gui/electron/main.ts
# → ≤ 250 lines
```

### A3 — IPC channel 명 보존

```bash
# ipcMain.handle 채널 전체 목록 비교 (refactor 전후 동일해야 함)
grep -rn "ipcMain.handle\|ipcMain.on" packages/gui/electron/ | sort
# 채널 목록 변화 없음 확인
```

### A4 — build 통과

```bash
pnpm -F gui build
# → exit 0
```

### A5 — smoke

앱 실행 → 프로젝트 오픈 → PO 메시지 전송 → Settings 저장 → MCP 서버 목록 로드 정상.

---

## §Plan

### 목표

2009줄짜리 main.ts 에서 ipcMain.handle/on 등록 + 전용 helpers 를 13개 module 로 이전.
main.ts = entry + register 호출 + app lifecycle (createWindow / buildAppMenu / app.whenReady).

---

### Module 경계 지도

| 파일 | 담당 채널 | 이전할 주요 함수/타입 | 예상 줄 수 |
|:--|:--|:--|--:|
| `ipc/onboarding.ts` | `onboarding:*` | spawnStreaming, openTerminalWith, prewarmPlaywrightMcp, writeOnboardingPending; GraphitiConfig / OnboardingRecord / OnboardingCompleteOpts interface | ~420 |
| `ipc/project.ts` | `project:*`, `projects:*`, `init:project`, `dialog:*`, `github:*`, `shell:openExternal`, `ping` | detectProductuneLayout, scanDescendantsForProductune | ~220 |
| `ipc/tickets.ts` | `tickets:scan` | parseFrontmatter, extractRequestSummary; ScannedTicket interface | ~110 |
| `ipc/state.ts` | `state:*`, `phase:approve` | ApprovePhaseArgs interface | ~90 |
| `ipc/po.ts` | `po:*`, `chat:*`, `browser:opened` | activePoChild / capturedPoSessionId module vars | ~70 |
| `ipc/mcp.ts` | `mcp:*` | readClaudeSettings (exported), writeClaudeSettings, readClaudeJson (exported), writeClaudeJson; McpServerConfig / McpServerEntry interface | ~140 |
| `ipc/hooks.ts` | `hooks:list` | HookRow interface; imports readClaudeSettings from ./mcp | ~45 |
| `ipc/skills.ts` | `skills:list` | collectMdFiles, parseSkillFrontmatter, inferPersonasFromPath; SkillEntry / SkillPersona | ~110 |
| `ipc/deploy.ts` | `deploy:*` | _pendingPrCtx module var; DeployProgressStep type | ~160 |
| `ipc/settings.ts` | `settings:*` | — (모두 @productune/core delegate) | ~60 |
| `ipc/design.ts` | `design:*` | — | ~45 |
| `ipc/explorer.ts` | `explorer:*`, `slash:listProjectFiles` | explorerWatcher/explorerWatchRoot/explorerDebounceTimer vars; startExplorerWatch, stopExplorerWatch; FsNode / ExplorerWatcher / QuickOpenFile; listProjectFilesRecursive | ~115 |
| `ipc/worktree.ts` | `worktree:*` | — | ~55 |

---

### 각 module 공통 패턴

```ts
// ipc/example.ts
import { ipcMain } from 'electron'
import path from 'path'
// ... 필요한 imports

// module-scoped state (해당 module 전용)
let someModuleVar: SomeType | null = null

export function register(): void {
  ipcMain.handle('channel:name', async (_event, arg) => {
    // handler body — 로직 변경 0
  })
  // ...
}
```

main.ts 에서:
```ts
import { register as registerOnboarding } from './ipc/onboarding'
// ... all 13 imports
registerOnboarding()
registerProject()
// ... all 13 register calls
```

---

### main.ts 최종 구조 (~200줄)

```ts
// ── Electron / Node imports ───────────────────────────────────────────────
import { app, BrowserWindow, ipcMain, shell, Menu, type MenuItemConstructorOptions } from 'electron'
import path from 'path'
import fs from 'fs'
import os from 'os'

// ── IPC module imports ────────────────────────────────────────────────────
import { register as registerOnboarding } from './ipc/onboarding'
import { register as registerProject }    from './ipc/project'
import { register as registerTickets }    from './ipc/tickets'
import { register as registerState }      from './ipc/state'
import { register as registerPo }         from './ipc/po'
import { register as registerMcp }        from './ipc/mcp'
import { register as registerHooks }      from './ipc/hooks'
import { register as registerSkills }     from './ipc/skills'
import { register as registerDeploy }     from './ipc/deploy'
import { register as registerSettings }   from './ipc/settings'
import { register as registerDesign }     from './ipc/design'
import { register as registerExplorer }   from './ipc/explorer'
import { register as registerWorktree }   from './ipc/worktree'

// ── Open Recent — deferred open-file queue (T-P4-111) ─────────────────────
let deferredOpenPath: string | null = null

app.on('open-file', (event, filePath) => {
  event.preventDefault()
  if (process.platform === 'darwin') { app.addRecentDocument(filePath) }
  const win = BrowserWindow.getFocusedWindow() ?? BrowserWindow.getAllWindows()[0] ?? null
  if (win && !win.isDestroyed()) {
    win.webContents.send('open-recent-project', filePath)
  } else {
    deferredOpenPath = filePath
  }
})

// ── IPC registration ──────────────────────────────────────────────────────
registerOnboarding()
registerProject()
registerTickets()
registerState()
registerPo()
registerMcp()
registerHooks()
registerSkills()
registerDeploy()
registerSettings()
registerDesign()
registerExplorer()
registerWorktree()

// ping (health check — main.ts 에 남기거나 project.ts 에 이동 가능)
ipcMain.handle('ping', () => 'pong')

// ── Window + app lifecycle ────────────────────────────────────────────────
function createWindow(): void { ... }
function buildAppMenu(): Menu { ... }
function sendToFocused(channel: string): void { ... }

app.whenReady().then(() => {
  Menu.setApplicationMenu(buildAppMenu())
  createWindow()
  app.on('activate', () => { if (BrowserWindow.getAllWindows().length === 0) createWindow() })
})

app.on('window-all-closed', () => { if (process.platform !== 'darwin') app.quit() })
```

---

### 주의사항

| 항목 | 처리 |
|:--|:--|
| `spawnStreaming` | onboarding.ts 내부 private — export 불필요 |
| `openTerminalWith` | onboarding.ts 내부 private |
| `readClaudeSettings` / `readClaudeJson` | mcp.ts 에서 `export` — hooks.ts 에서 `import { readClaudeSettings } from './mcp'` |
| `_pendingPrCtx` | deploy.ts module-level var (deploy:execute ↔ deploy:resolve-conflict 공유) |
| `activePoChild` / `capturedPoSessionId` | po.ts module-level var; 현재 코드 패턴 그대로 유지 |
| `GraphitiConfig` interface | onboarding.ts 에 2번 선언되어있음 — 하나로 통합 |
| `github:*` 핸들러 | project.ts 에 포함 (프로젝트 셋업 관련) |
| `state:openDeployModal` | state.ts 에 포함 (send-to-renderer 패턴 — deploy.ts 가 아닌 state.ts) |
| `T-P4-153` 에서 `design` → `artifacts` 변경 있으면 | design.ts 작성 시 최신 main.ts 기준으로 경로 반영 |

---

### 실행 순서

1. `packages/gui/electron/ipc/` 디렉토리 생성
2. `mcp.ts` 먼저 작성 (hooks.ts 가 readClaudeSettings import)
3. 나머지 12개 module 작성 (순서 무관)
4. `main.ts` 리팩터: register 호출 + app lifecycle 만 남기기
5. `pnpm -F gui build` → tsc 클린
6. 앱 실행 smoke

---

## §Out of scope

- IPC channel 명 변경
- handler 내부 로직 변경
- 신규 IPC 채널 추가
- `preload.ts` / `chat-store.ts` / `po-runner.ts` / `mechanical-write.ts` 변경

---

## §QA scope

| Field | Value |
|:--|:--|
| **QA invoke** | `manual smoke only` |
| **test target** | Electron app — IPC round-trips: project open / tickets:scan / settings:getUiLanguage / deploy modal open / MCP server list |
| **사용자 dogfood** | 앱 실행 → 프로젝트 오픈 → PO 메시지 전송 1회 → Settings 언어 저장 → 정상 동작 확인 |
| **regression check** | `packages/gui/electron/main.ts` 모든 ipcMain.handle 채널 목록 (refactor 전후 동일) |

---

## §Persona Activity

| ts | persona | model/effort | turn | result |
|:--|:--|:--|:--|:--|
| 2026-05-20T00:00Z | pdt-designer | opus/xhigh | 1 | plan inline §Plan (T-P4-154) |
| 2026-05-21T13:00Z | pdt-developer | sonnet/medium | 2 | impl done — main.ts 2009→197 + 13 ipc/ modules (project/tickets/mcp/settings/hooks/deploy/persona/etc) |
