---
id: T-310
slug: gui-model-effort-override
type: impl
status: done
assignee: developer
feature: gui-core
deps: [T-308, T-293]
created: 2026-07-04
closed: 2026-07-06
---
## Request
[[decision--v1-single-line]]: legacy T-PATCH-286(GUI 세션 model/effort 오버라이드) 미커밋 기능을 v1.1에 정식 재구현. prdt에선 설정 소스가 `.prdt/config.json`(gui_model/gui_effort 상당)이어야 함 — legacy `.productune/config.json` 키 계승 여부 포함 스펙 재확정. v0.6 diff는 참고 자료로만. 우선순위: T-293 뒤.

## Acceptance
- User can override PO session model/effort from the GUI for a prdt project; override reaches spawn args
- Config persisted in `.prdt/config.json`; no legacy path writes
- Gates green + QA smoke

## Outcome
- 신규 po-session-config.ts: `.prdt/config.json`의 gui_model/gui_effort jq식 merge(기존 키 보존·atomic tmp+rename·legacy/부재 거부·allowlist). buildClaudeArgs 순수 함수 추출 — 미설정 시 argv 바이트 동일, 설정 시 --model/--effort 삽입(실 CLI 플래그 확인). IPC 2종 + GeneralSettings 셀렉트 2개(legacy 자기 숨김) + locale en/ko.
- Dev 라이브(실 Settings UI 구동: 쓰기/해제/slug 보존/콘솔 에러 0) + QA smoke PASS(argv 불변·merge 안전·allowlist·게이트 vitest 24). v0.6 참고 patch는 스폰 미배선 상태였음 — 실제 배선은 본 티켓이 최초.
