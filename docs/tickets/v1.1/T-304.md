---
id: T-304
slug: renderer-project-kind-polog
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-287]
created: 2026-07-03
closed: 2026-07-04
---
## Request
A1/A4 이월: WorkspaceShell 'View PO Log'가 `<projectDir>/.productune/logs/po-session.log` 하드코딩 — prdt 프로젝트에선 존재하지 않는 legacy 경로(빈 로그 표시, 크래시 없음). renderer `Project` 타입에 project-kind가 없어 App.tsx setProject ~10곳 스레딩 또는 electron 측 경로 해석 IPC 중 택일 필요(해법 형태는 developer 판단). A8 배선과 함께 또는 직후 처리.

## Acceptance
- prdt project: 'View PO Log' resolves a prdt-correct log path (or the tab is hidden if no equivalent exists)
- Legacy project: unchanged

## Outcome
- .prdt/에 po-session.log 등가물 없음 확인(turns.jsonl은 원격측정용) → 숨김 선택. T-306 덕에 스레딩 불요 — WorkspaceShell `onViewLog={isPrdt ? undefined : handleViewLog}` + SessionHealthBanner CTA 조건부 렌더(버튼 자체 스킵). legacy 바이트 동일.
- 게이트 green(tsc·lint·vitest 10/10·build), QA smoke PASS(코드-레벨 — 배너 표출 조건이 세션 에러라 라이브 강제 비용 대비 충분 판정).
