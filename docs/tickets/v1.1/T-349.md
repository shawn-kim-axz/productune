---
id: T-349
slug: gui-impl-project-tab-history-tab
type: impl
status: open
assignee: developer
deps: [T-348]
feature: gui
created: 2026-07-14
---

## Request

**T-348 승인된 스펙 구현** (shawn 승인 2026-07-14). 스펙 SoT: `docs/artifacts/T-348-project-tab-history-tab-spec.md` (+ 목업 `T-348-project-tab-history-tab-mockup.html`).

1. **프로젝트 탭 재구성**: 카드 → PRD → 아티팩트(현재 버전 전용) → .ENV. PRD는 버전 탭 main pane에서 제거.
2. **프로젝트 히스토리 탭 신설**: 기존 아티팩트 탭 슬롯 재사용. 닫힌 버전 타임라인 — 상세는 Outcome 최상단, 그 버전 티켓들·회고·아티팩트. "닫힘" SoT = git tag (retro/티켓 상태는 보조).
3. **선결**: git tag 목록+날짜 IPC 신설 (현재 코드베이스에 없음 — spec §2.4).
4. activityBar 'artifacts'→'history' rename의 타입/기존 tab-id 영향 정리 (designer가 developer 확인 필요로 남긴 항목).

## Acceptance

- Project tab renders card → PRD → current-version artifacts → .ENV per spec; PRD no longer in the version tab main pane (its removal handled per spec §3).
- History tab lists closed versions (git-tag SoT) with outcome-first detail (tickets, retro link, that version's artifacts); empty state per spec when no version closed yet.
- New git-tag IPC is read-only, safe on repos with no tags, and covered by tests.
- Data derives from docs/tickets/v<N>/ + retro files + git tags only — no hand-maintained data.
- No regression: existing tab navigation/ids, legacy(.productune) projects, T-347 card behavior.

## Outcome
