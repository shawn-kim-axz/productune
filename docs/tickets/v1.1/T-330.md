---
id: T-330
slug: statusline-register-default-on
type: impl
status: done
assignee: developer
feature: install-cli
created: 2026-07-09
closed: 2026-07-13
---

## Request

**CLI 이슈**: statusline이 다른 기기에서 등록 안 됨.

- `install.sh`가 statusline을 **`--statusline` opt-in 플래그** 뒤에서만 등록함. 그래서 플래그 없이 다른 기기에 설치하면 statusline이 등록되지 않은 채로 남음.
- 수정 방향(Define에서 상세화): statusline 등록을 **기본 활성(default-on)**으로 전환 — 또는 fresh install에서 opt-in 플래그 없이도 등록되도록 보장.

## Acceptance

- (Define/설계 시 상세화) fresh install(`install.sh`)이 `--statusline` 플래그 없이도 statusline을 등록한다.
- 다른 기기에서 플래그 없이 설치해도 statusline이 정상 등록됨을 확인.
- default-on 전환 시 opt-out 경로(원치 않을 때 끄는 방법)를 함께 검토.

## Outcome

- 2026-07-09: 사용자 캡처(CLI 기능). 다음 버전으로 이월(백로그) — 이번 버전 스코프 아님, 버전 미배정.
- 2026-07-13: 구현 완료. `packages/core/scripts/install.sh` §6을 opt-in → default-on으로 전환.
  - 로직: `.statusLine`이 settings.json에 전혀 없을 때만(fresh install, 또는 legacy `statusline-productune.sh` 정리 직후) 자동 등록. 이미 등록된 statusLine(우리 것이든 사용자 커스텀이든)은 절대 덮어쓰지 않음 — "다른 기기에서도 등록 안 됨" 버그의 근본 원인(무조건 opt-in)만 제거하고, 기존 설치 클로버 위험은 그대로 차단.
  - opt-out: `--no-statusline` 플래그 신설(설계 검토 항목 반영). `--statusline`은 기존과 동일하게 강제 재등록(기존 커스텀 statusLine 위에도 덮어씀)으로 유지.
  - 인자 파싱을 `${1:-}` 단일 위치 인자 → `for arg in "$@"` 루프로 변경(두 플래그 어느 위치에 와도 인식).
  - 테스트: `packages/core/test/scripts/install-legacy-cleanup.test.ts`의 statusline 관련 검증에 "legacy 제거 후 default-on으로 prdt statusline이 등록된다" 단언 추가. 신규 `packages/core/test/scripts/install-statusline-default-on.test.ts` 5건 추가(빈 HOME 신규 설치 default-on, 빈 settings.json default-on, `--no-statusline` opt-out, 기존 커스텀 statusLine 무클로버, `--statusline` 강제 재등록). 실제 install.sh를 샌드박스 HOME/PRDT_HOME/CLAUDE_DIR에서 end-to-end 구동해 검증(mock 아님).
  - 검증: `cd packages/core && npm test` → 9 files / 31 tests all pass. 별도로 샌드박스에서 install.sh를 flag 없이 1회 실행 → `.statusLine.command`에 `statusline-prdt.sh` 등록, `PRDT_STATUSLINE_INSTALLED=true` 확인(수동 재현으로 버그 재현 시나리오가 고쳐졌음을 확인).
  - 문서: `packages/core/README.md`의 "add --statusline to opt in" 문구를 default-on 반영 문구로 갱신. install.sh 상단 주석 + §4 jq 주석(§6 opt-in 언급)도 default-on으로 갱신.
  - files_written: `packages/core/scripts/install.sh`, `packages/core/README.md`, `packages/core/test/scripts/install-legacy-cleanup.test.ts`, `packages/core/test/scripts/install-statusline-default-on.test.ts` (신규), `docs/tickets/v1.1/T-330.md`.
  - 커밋은 PO가 처리(미커밋 상태로 반환).
