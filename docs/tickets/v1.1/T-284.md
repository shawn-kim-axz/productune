---
id: T-284
slug: adapter-a1-path-constants
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-283]
created: 2026-07-03
closed: 2026-07-03
---
## Request
A1 (선행): `.productune` 문자열이 electron ~15개 파일에 산재, 중앙 상수 없음. 중앙 경로 상수 도입 후 project-kind 감지(`.prdt/` vs `.productune/`)로 store 레벨 dual-mode 분기. 옛 프로젝트는 기존 코드 그대로, prdt 프로젝트만 새 경로. `project.ts:114-177` 감지 마커에 `.prdt` 추가 (briefs/·po.lock은 prdt 감지에서 제외).

## Acceptance
- Single path-constant module; no scattered `.productune` literals on touched paths
- Project-kind detection: `.prdt/` project resolves prdt paths, legacy project behavior unchanged
- GUI opens both a legacy and a prdt project without error

## Outcome
- 구현 (2026-07-03): `electron/project-paths.ts` 신설 — detectProjectKind(`.prdt` 우선) + 경로 리졸버 SoT. electron 8파일(chat-store·ipc/state·ipc/surface·po-runner·po-session-cycle·subagent-cost·ipc/onboarding·ipc/project)의 project-dir `.productune` 리터럴 경유 교체. `detectProductuneLayout`에 `.prdt` 분기(self-current·no-heal·briefs/·po.lock 제외). 홈디렉토리 `~/.productune`는 A6/A7 스코프로 보류.
- QA 1차 FAIL (HIGH 1건): `@productune/core` pending-promotions.ts statePath 하드코딩 → prdt 프로젝트에서 매 턴 유령 `.productune/po-state.json` 생성. rules.ts·worktree.ts 동일 패턴.
- 수정: core `src/state/project-kind.ts` 신설(gui와 동일 시맨틱), 하드코딩 3곳 교체. 회귀 테스트 3건 fail→pass.
- QA 재검증 PASS: prdt 스크래치에서 promotion/rules/worktree 전부 `.prdt/` 해석·유령 파일 미생성, legacy 바이트 동일, core(21/21)·gui 게이트 green.
- 잔여 LOW 1건 → T-298 발행 (hooks.ts pre-push 스크립트 `.productune/git-rules.json` 하드코딩).
- 이월: renderer 'View PO Log' legacy 경로(표시 전용) → A4/A8 편입 권장. gui/core 감지 로직 중복 — 공유 패키지 ADR 후보 (inbox 기록).
- 미커밋 — 커밋은 사용자 요청 시.