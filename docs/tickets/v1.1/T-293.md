---
id: T-293
slug: legacy-tree-cleanup
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-308, T-311]
created: 2026-07-03
closed: 2026-07-06
---
## Request
v1 브랜치 legacy 트리 정리 (flip 유의점 §3: "어댑터 작업 때 함께") — 구 full/lite 트리 삭제, 이름 정리(`prdt-install.sh`→`install.sh` 등). ~~main과의 merge 마찰 회피를 위해 A1~A8 완료 + main 최종 merge **후** 실행~~ → 전제 변경([[decision--v1-single-line]], 2026-07-04): **T-308(v0.6 delta 반영) 완료 후** 실행. main merge는 더 이상 없음 — v1 단일 라인.

## Acceptance
- Legacy full/lite trees removed from v1; prdt scripts renamed to canonical names
- Fresh install from cleaned tree passes (install → prdt doctor clean)
- T-308 and T-311 closed at execution time (no main merge — superseded; legacy support demoted to read-only first)

## Outcome
- 1차 dispatch는 정당한 blocked(legacy 트리가 GUI 온보딩·core 게이트·CI의 살아있는 의존) → T-311(조회 전용 강등) 선행 후 재개.
- 삭제 735파일: doctrine/·skills/(590)·pdt agents·config 3종(model-prices.json은 살아있는 소비처라 존치)·legacy CLI/hooks 18·CI 스크립트 5. 개명: prdt-install/uninstall→install.sh/uninstall.sh + 참조 26파일 스윕 + deprecation forwarder(전 기기 prdt update 1회 후 제거). prdt-flip.sh·statusline-prdt.sh·hook 3종 이름 유지(설계 SoT·설치본 참조).
- QA PASS 5/5(0.96): grep -a 무참조 재증명(NUL footgun 회피)·개명 스윕 완결·양 픽스처 라이브 생존·게이트 green·격리 fresh install+idempotent 재설치 실측. doctor discipline 0.
- 후속 inbox: NUL 위생·가격표 이원화·forwarder 제거 조건·스킬팩 배포 스토리·CI main 트리거·subagent-cost 주석.
