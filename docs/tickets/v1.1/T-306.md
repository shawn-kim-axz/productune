---
id: T-306
slug: prdt-version-field-bridge
type: impl
status: open
assignee: developer
feature: gui-adapter
deps: [T-291]
created: 2026-07-04
---
## Request
A8 이월 (flip-blocker 후보): GUI는 `current_version`+`versions[]`를 전방위로 읽지만 prdt po-state는 flat `version` 문자열. A8이 제도 UI는 숨겼으나 내비 배선이 죽어 있음 — prdt에서 poStateVersion=null → PRD 자동 내비·버전 키 티켓 리뷰 자동 오픈·아티팩트 버전 스코핑 비활성(우회로는 있음: breadcrumb 버전 표시, ActivityBar 티켓 보드, explorer PRD). store 레벨 `version`→`current_version` 정규화로 복원.

## Acceptance
- prdt project: PRD auto-nav, version-keyed ticket auto-open, artifact version scoping function with flat version
- Legacy versions[] behavior unchanged

## Outcome
