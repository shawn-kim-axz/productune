---
id: T-306
slug: prdt-version-field-bridge
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-291]
created: 2026-07-04
closed: 2026-07-04
---
## Request
A8 이월 (flip-blocker 후보): GUI는 `current_version`+`versions[]`를 전방위로 읽지만 prdt po-state는 flat `version` 문자열. A8이 제도 UI는 숨겼으나 내비 배선이 죽어 있음 — prdt에서 poStateVersion=null → PRD 자동 내비·버전 키 티켓 리뷰 자동 오픈·아티팩트 버전 스코핑 비활성(우회로는 있음: breadcrumb 버전 표시, ActivityBar 티켓 보드, explorer PRD). store 레벨 `version`→`current_version` 정규화로 복원.

## Acceptance
- prdt project: PRD auto-nav, version-keyed ticket auto-open, artifact version scoping function with flat version
- Legacy versions[] behavior unchanged

## Outcome
- store-ingress 브리지 bridgePrdtVersion(copy-on-write, legacy 참조 passthrough sameRef 테스트 고정, versions[] 무합성) + prdCandidatePaths 쌍둥이 prdt 단일 PRD.md 분기 + electron 직독 5사이트(state 감시자·project recents·subagent-cost 스탬프·po-session-cycle stage 경계·artifacts flat scope) + rename-guard legacy-only 게이트(prdt 버전 범프 오탐 사전 차단).
- Dev 라이브 검증 + QA fresh-eyes PASS: prdt PRD/티켓 자동오픈·4-stage 복원, legacy 픽스처(issue-tracker) 픽셀 불변, A8 억제 회귀 없음(제도 UI 게이트가 isPrdtPoState 우선 확인), 게이트 green.
- 잔여(무해, inbox): QuickOpen v: prdt 미표기, flipped repo 아티팩트 리스팅 noisy, smoke visual-layout test-infra 갭.
