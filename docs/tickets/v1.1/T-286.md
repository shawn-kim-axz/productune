---
id: T-286
slug: adapter-a3-ticket-status
type: impl
status: open
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
---
## Request
A3 (T2): ticket status 동의어 테이블(useTicketScan.ts)에 2줄 추가 — `open→in-progress`, `dropped→abandoned`. "디렉토리=버전" 가정 지점에 `backlog/` 디렉토리 라벨 처리 1건 (tickets.ts).

## Acceptance
- prdt tickets with status open/dropped render correctly in GUI
- backlog/ directory shows a backlog label, not as a version

## Outcome
