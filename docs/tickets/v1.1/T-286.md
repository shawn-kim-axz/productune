---
id: T-286
slug: adapter-a3-ticket-status
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
closed: 2026-07-03
---
## Request
A3 (T2): ticket status 동의어 테이블(useTicketScan.ts)에 2줄 추가 — `open→in-progress`, `dropped→abandoned`. "디렉토리=버전" 가정 지점에 `backlog/` 디렉토리 라벨 처리 1건 (tickets.ts).

## Acceptance
- prdt tickets with status open/dropped render correctly in GUI
- backlog/ directory shows a backlog label, not as a version

## Outcome
- useTicketScan.ts 동의어 2줄(`open→in-progress`·`dropped→abandoned`) + 회귀 테스트 2케이스. tickets.ts: backlog/ 직속 + version-less → 'backlog' 라벨, 그 외 null 폴백 유지(legacy Unassigned 버킷 보호 — 의도적 협소 특례).
- QA smoke PASS: acceptance 전항 + v1.1/ 티켓 version=null 렌더는 감사 문서 §4 명시 의도(versions 배열 뷰는 A8 스코프)로 스코프 갭 아님 판정. 게이트 green.
