---
id: T-290
slug: adapter-a7-usagebar-source
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
closed: 2026-07-04
---
## Request
A7 (T2): UsageBar 데이터 소스 교체 — `usage-state.json` 소멸(v1 statusline 순수 표시 전용), 대체 원천 = hook#3의 turns.jsonl. CostArchivePanel 필드 대조 1회. **cost_source 뱃지**: 행이 `cost_source:"estimated"`면 비용 표시에 "추정" 뱃지, `"reported"`/부재는 무표기.

## Acceptance
- UsageBar renders from turns.jsonl for prdt projects (no usage-state.json dependency)
- estimated rows show the badge; reported/absent rows do not
- CostArchivePanel fields verified against v1 turns.jsonl schema

## Outcome
- costArchive.ts: resolveTurnsPath→A1 turnsJsonlPath(+.productune 하드코딩 결함 수정 — A1 스윕/재감사 양쪽 누락이었음, inbox 기록), cost_source→hasEstimated 전파. UsageBar prdt 분기: 프로젝트 누적 비용 + '추정' 뱃지(isPrdtPoState 재사용, IPC 신설 없음). CostArchivePanel 뱃지 전 지점 + locale en/ko. usageWatch는 legacy 전용 불변.
- QA PASS: 실물 turns.jsonl(43줄) 재계산 — per-session MAX 집계로 이중계상 없음 확인($112.83 정합; 단순 합산이면 $443 오류였을 것). 필드 대조·legacy 불변·게이트 green.
- 대체 지표(누적 비용, 세션 스코프 아님)는 ADR 후보로 inbox — Designer/PO 후속 결정 대상.
