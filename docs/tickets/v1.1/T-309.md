---
id: T-309
slug: composer-queueing-activity
type: impl
status: done
assignee: developer
feature: gui-core
deps: [T-308, T-293]
created: 2026-07-04
closed: 2026-07-06
---
## Request
[[decision--v1-single-line]]: legacy T-PATCH-284(컴포저 큐잉 + PO 활동 라인) 미커밋 기능을 v1.1에 정식 재구현. v0.6 더티 트리 diff(ChatPanel·PersonaPresenceBar·poEvents 등, 폐기 전 patch로 보존)는 참고 자료로만 — 스펙 확정·테스트·QA 포함 신규 구현. 우선순위: T-293 뒤.

## Acceptance
- Composer queues user input while PO turn is streaming; queued items flush in order on turn end
- PO activity line renders current activity; legacy behavior unaffected paths verified
- Gates green + QA smoke

## Outcome
- 재구현(참고 patch에서 T-284 의도만 취함, T-286 훈크(model/effort/실측 토큰/dots 애니메이션) 제외 — YAGNI):
  - `workspace.ts`: `queuedMessages`(FIFO) + `enqueueMessage`/`removeQueuedMessage`/`clearQueue` + `latestPoTool`(활동 라인용) 추가. 순수 헬퍼 `enqueueText`/`removeQueuedAt`/`joinQueueForFlush`를 export해 store 액션과 단위 테스트가 같은 로직을 공유(zustand가 vitest.setup.ts에서 전역 mock되어 실제 store 인스턴스를 테스트할 수 없음 — dedupeMessagesById 선례 그대로 따름). project 전환/resetSession 시 큐·activity 초기화.
  - `poEvents.ts`: `onMsgId`(턴 시작)에서 `latestPoTool` 리셋, `onAnnounce`의 `level:'tool'`(PO 자신의 tool_use — po-runner.ts:~1558, T-PATCH-108부터 이미 toolName/toolInput 존재해 **po-runner.ts 변경 불요**)에서 `latestPoTool` 세팅, `onDone`에서 리셋.
  - `ChatPanel.tsx`: 컴포저가 스트리밍 중에도 계속 live(이전엔 WorkingIndicator+stop 한 줄로 collapse) — Cmd+Enter가 스트리밍 중이면 `enqueueMessage`로 분기, 유휴 시 기존 즉시 전송(`sendText`로 추출). 큐는 `streaming: true→false` 이펙트에서 **자연 종료(onDone)에서만** flush(newline-join, `joinQueueForFlush`) — abort는 `abortedRef`로 큐 보존(유저가 막 취소한 턴에 조용히 얹혀 보내지 않도록). 큐 칩(개별 X 제거) 컴포저 위 렌더. PO 활동 라인(`PoActivityLine`, 구 `WorkingIndicator` 재배치+확장)은 메시지 리스트 하단, "스트리밍 && 아직 prose 없음"일 때만 렌더 — `latestPoTool` 있으면 `humanizePoTool`(Read/Edit/Write/Bash/Grep/Task/Skill/WebFetch 등) 우선, 없으면 기존 health verb(delegating/thinking/…) fallback.
  - locale en/ko 동시 추가: `queuePlaceholder/queueHint/queueRemove`, `activity.{read,edit,write,bash,grep,task,skill,web}`.
- **AskUserQuestion dock 상호작용(명시 확인)**: pendingQuestion 중엔 컴포저 전체(큐 UI 포함)가 questionDock으로 대체되는 기존 분기라 그 창에서 새 enqueue는 물리적으로 불가; 이미 쌓인 큐 항목은 dock 노출과 무관한 독립 상태라 이후 실제 onDone 시점에 정상 flush됨 — 코드 검토로 확인, 별도 분기 불요.
- legacy 프로젝트 무영향: 변경분 어디에도 poState 종류(prdt vs legacy) 분기 없음 — 라이브 확인 시 이 worktree(prdt) 전환 전 초기 화면(legacy 스타일 v0.6 productune 루트)도 동일 ChatPanel로 정상 렌더됨을 목격.
- 검증: 단위 테스트 `composerQueue.test.ts`(큐 순서/제거/flush-join, out-of-range 등 9case) 신규 + 기존 22개 전부 green. 게이트(`pnpm build`: locale parity·tsc --noEmit·vite build ×3) green. 라이브: playwright `_electron`으로 실제 앱 기동 → 이 worktree를 fixture로 전환(`open-recent-project` IPC, 다이얼로그 불요) → `po:sendMessage`/`chat:appendMessage`/`chat:setClaudeSessionId` 메인프로세스 핸들러만 no-op 스텁(실 PO 스폰·디스크 쓰기 없이) → `po:onMsgId`/`po:onAnnounce`(tool)/`po:onDone` 이벤트를 직접 주입해 스트리밍 모사: 컴포저 유지·활동 라인 humanize("Reading some-file.md")·Cmd+Enter 큐잉·칩 개별 제거·자연 종료 시 flush(합쳐진 텍스트가 새 유저 메시지로 등장, flush 자체가 새 턴을 열어 컴포저가 다시 streaming 표시로 전환되는 것까지 확인) 전부 통과, 콘솔 에러 0. 검증용 스펙 파일은 메인프로세스 IPC를 몽키패치하는 성격이라 상시 스위트에는 남기지 않고 세션 종료 시 삭제(테스트 산출물 `.productune/chat.json` 미생성 확인).
- 발견(범위 밖, PO 보고용): `tests/smoke.spec.ts`의 세 번째 케이스가 이 개발 머신의 실제 recents 목록(여러 프로젝트 존재) 때문에 `button:visible` strict-mode 위반으로 실패 — stash로 A/B 비교해 T-309 변경과 무관한 기존 결함(테스트 헬퍼가 다중 매치를 처리 못 함)임을 확인. 별도 티켓 후보.
- QA smoke PASS(fresh-eyes): abort 비flush·flushingRef 재진입 가드·프로젝트 전환 큐 격리·질문 dock 상호작용·게이트(vitest 22)·locale parity 전부 확인.
