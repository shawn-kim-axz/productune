---
id: T-351
slug: gui-env-rename-order-artifacts-toggle
type: impl
status: done
assignee: developer
deps: [T-349]
feature: gui
created: 2026-07-14
closed: 2026-07-14
---

## Request

**UI 조정 (shawn, 2026-07-14, T-349 위 후속)**: 프로젝트 탭에서

1. "프로젝트 .ENV" 섹션명 → **"환경변수"** rename.
2. 환경변수 섹션을 **산출물(아티팩트) 섹션보다 위**로 배치 — 순서: 카드 → PRD → 환경변수 → 산출물.
3. 산출물 섹션은 **기본 toggle open**.
4. 토글 버튼 위치를 **title 텍스트 우측**으로 변경.

## Acceptance

- Project tab section order: card → PRD → 환경변수 → 산출물; the env section is titled "환경변수" (ko; en counterpart consistent).
- Artifacts section renders expanded by default; toggle control sits to the right of the section title text.
- No regression to T-349 behavior (artifact counts, archive sub-toggle, PRD compact section).

## Outcome

- Renamed the env section title: `workspace.projectEnv.sectionTitle` en "Project .env" → "Environment Variables", ko "프로젝트 .env" → "환경변수" (`packages/gui/src/locales/en.json`, `ko.json`).
- Reordered `LeftSidebar.tsx` project-tab body: `<SidePanelProjectEnv />` now renders before `<SidePanelArtifacts />` — final order card → PRD (compact) → 환경변수 → 산출물 → (legacy past versions, hidden for prdt).
- `SidePanelArtifacts.tsx`: `expanded` state default flipped `false → true` (artifacts section opens expanded on mount).
- `SidePanelArtifacts.tsx` header row: moved the collapse/expand chevron from leading the title to trailing it — title text and count badge now come first, followed by a flex spacer, then the chevron at the row's right edge. Removed `flex:1` from `secHdrText` (no longer needed now the title doesn't stretch).
- T-349 behavior preserved untouched: artifact counts (flat+archived), archive sub-toggle (still its own separate collapsed-by-default control inside the section body), "open all" overflow row, session-file dim overlay, PRD compact section — none of this logic was touched, only the header's default state and DOM order of header children.
- Verified: `node scripts/check-locale-keys.js` (en/ko key parity + t() resolution), `bash scripts/check-locale-protected.sh`, `tsc --noEmit` — all green. No existing test suite covers these two components (UI glue, per developer habit judgment call — no test added).

files_written:
- packages/gui/src/components/workspace/LeftSidebar.tsx
- packages/gui/src/components/workspace/SidePanelArtifacts.tsx
- packages/gui/src/components/workspace/SidePanelProjectEnv.tsx
- packages/gui/src/locales/en.json
- packages/gui/src/locales/ko.json
