---
id: T-298
slug: prepush-hook-rules-path
type: impl
status: open
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
---
## Request
QA(A1 재검증)에서 발견된 LOW 잔여 결함: `packages/core/src/git-workflow/hooks.ts:11` `installPrePushHook`이 생성하는 pre-push 셸 스크립트가 `.productune/git-rules.json`을 하드코딩. prdt 프로젝트에 훅 설치 시 `.prdt/git-rules.json`을 읽지 못해 protectedBranches가 기본값 `main`으로 조용히 폴백(크래시·유령 파일 없음, 안전한 방향의 기능 저하). 생성 스크립트가 project-kind에 맞는 경로를 쓰도록 수정.

## Acceptance
- Pre-push hook installed in a prdt project reads `.prdt/git-rules.json`; legacy project hook unchanged
- Regression test covers the generated script's path for both project kinds

## Outcome
