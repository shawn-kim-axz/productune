---
id: T-292
slug: gui-askuserquestion-wait
type: qa
status: done
assignee: qa
feature: gui-adapter
created: 2026-07-03
closed: 2026-07-04
---
## Request
어댑터 시 확인 항목 (감사 문서 2026-07-03 추가): PO의 AskUserQuestion을 GUI가 stream-json으로 받아 자체 UI 카드로 띄울 때, CLI 60초 무응답 타임아웃과 독립적으로 **GUI가 응답 대기를 소유**하는지 검증. 질문 카드가 조용히 만료되면 UX 파손 — PO habit(타임아웃 시 텍스트 질문+무기한 대기)과 정합 확인.

## Acceptance
- Question card stays answerable past the CLI 60s timeout, or the PO's text-fallback question is surfaced in chat
- No silent expiry: unanswered question is always visible and actionable

## Outcome
- QA 코드-레벨 판정(grill): ① 카드/dock은 타이머 없음 — 무기한 표시·클릭 가능, GUI가 대기 소유(acceptance 문면 충족) ② 단 raw AskUserQuestion 경로에서 CLI가 내부 60s 후 답 없이 진행하면 oqPending이 stall 감지를 무력화해 GUI가 만료를 감지·표출 못 함 — 카드가 의미상 stale이 될 수 있고 지각 답변(--resume)이 오바인딩될 위험. 실측 재현은 미수행(비용), confidence 0.55.
- 처분: prdt는 po habit 텍스트-질문 원칙으로 원천 차단(레이스 자체가 발생 안 함) — flip-blocker 아님. GUI 방어(만료 신호/expired 카드 상태)는 backlog T-307로 분리(재현 절차·수정 지점 제안 포함). legacy(v0.6) habit의 raw tool 허용 조항은 legacy 관심사로 이관 판단.
