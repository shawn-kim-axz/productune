---
id: T-283
slug: gui-audit-reverify
type: qa
status: done
assignee: qa
feature: gui-adapter
created: 2026-07-03
closed: 2026-07-03
---
## Request
GUI↔discipline 결합 감사(`docs/prdt-v1-gui-coupling.md`)는 v1 분기점 커밋 `00342cb` 기준. 이후 main의 GUI 변경(v0.6 워킹트리 포함)이 감사에 미포함 — 어댑터(A1~A8) 착수 전 main 최신 기준으로 결합 지점 1회 재검증. 신규/이동 결합 지점은 감사 문서에 append.

## Acceptance
- Re-audit run against latest main GUI tree; delta (new/moved coupling points) appended to docs/prdt-v1-gui-coupling.md
- A1~A8 ticket scopes adjusted if delta found (or "no delta" recorded)

## Outcome
- QA 재감사 완료 (2026-07-03). 대상: productune GUI 트리 branch v0.6, 미커밋 워킹트리 포함.
- v0.6 HEAD == 감사 기준 커밋 `00342cb` — 이후 커밋 없음. 유일한 차이는 워킹트리 diff (packages/gui 10파일, +1079/-297, T-PATCH-284·286 기능분).
- **판정: no delta (구조적)** — persona id·envelope 파싱·gate/promotion/versions·ticket status·phase/stage·경로·온보딩 훅 결합 지점 전부 불변. po-runner.ts 라인 이동분 신규 좌표를 감사 문서에 표로 append (39줄).
- 신규 read-site 1건: `readGuiModelConfig()`(po-runner.ts:71)가 `.productune/config.json`의 `gui_model`/`gui_effort` 읽음 → A1 스윕 목록에 1건 추가. A2~A8 스코프 영향 없음.
