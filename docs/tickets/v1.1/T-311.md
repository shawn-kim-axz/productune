---
id: T-311
slug: legacy-support-readonly
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-308]
created: 2026-07-06
closed: 2026-07-06
---
## Request
사용자 결정 (2026-07-06, "GUI 아직 잘 안 써서 legacy 지원 당장 끊어도 됨"): GUI legacy dual-mode를 **조회 전용으로 강등** — T-293의 선행 조건. ① legacy 온보딩 배선 중단: installClaudeHooks(coreDir)/installLegacyHooks(hook 18개+statusline-productune) 미실행, pdt-*/variants agent 심링크·po-instructions 복사 중단 (legacy 프로젝트 파일/티켓 **조회**는 유지) ② core 게이트 재배선: init-parity.mjs가 scripts/productune 실행하는 의존 제거(대체 or 은퇴) ③ CI fresh-install-smoke의 legacy install.sh 의존 정리 ④ electron-builder의 ../core 번들 사유 재검토(조회에 필요한 최소만).

## Acceptance
- Opening/onboarding a legacy project performs zero legacy installs (no hook registration, no pdt-* symlinks); file/ticket viewing still works
- core test gate green without executing legacy CLI; CI workflow adjusted
- prdt onboarding path (T-305) unaffected

## Outcome
- installClaudeHooks legacy/undefined→no-op, installLegacyHooks+PDT_BASENAMES 삭제, 잔존 프로비저닝 provisionUserGlobals(env-only) 추출 + 실경로 downgrade 테스트. init-parity 은퇴(legacy CLI 실행 제거), CI 스모크 prdt-install.sh로 교체(비대화형·스텁 불요), electron-builder ../core 번들 제거.
- QA PASS 5/5 (설치 0건 실측: settings.json/agents md5 무변화 · legacy 조회 라이브 생존 · prdt 무영향 · core/gui 게이트 green · CI dry-run 통과) + grill 발견 1건(완료 화면 거짓 체크) → completionStepKeys 6→3 정직화 + locale 정리로 수정, 게이트 재green.
- T-293 인계: scripts/ci/{run-install-smoke.sh,check-env-key-parity.mjs} dead 파일 삭제 · settings 화면 'install.sh' 안내 문구 스윕 · step(d) post-bash-strip-cost.sh 훅 의존 처분 · packaged .dmg 검증은 backlog 후보.
