---
id: T-342
slug: gui-model-version-always-visible
type: impl
status: done
assignee: developer
feature: gui
deps: [T-338]
created: 2026-07-13
closed: 2026-07-13
---

## Request

**GUI follow-up (shawn, 2026-07-13)**: 선택 UI(스위처 모달, FreshComposer, GeneralSettings)에서 미관측 모델이 "Claude Sonnet"으로만 나옴 — 사용자는 **항상 "Claude Sonnet 5"처럼 버전이 보이길** 원함. T-338의 관측-기반-만 정책(미관측 = 무버전)은 사용자 기대 미달로 판명.

- 방향:
  1. **번들 기본 매핑**: 빌드 시점의 alias→모델명 기본값("Claude Sonnet 5" 등)을 포함해 관측 전에도 버전 표시.
  2. **관측 우선**: 라이브 스트림에서 실 id가 관측되면 번들 값을 덮어씀 (T-338 observedByAlias 유지).
  3. **디스크 persist**: 관측 id를 로컬에 저장해 앱 재실행에도 유지 — 번들 매핑이 구식이 되어도 한 번 관측되면 자가 교정됨.
- 드리프트는 (2)+(3)의 자가 교정으로 수용 — "버전 발명 금지"보다 "항상 버전 표시"가 사용자 결정.

## Acceptance

- On first app run with no observations, the switcher modal / FreshComposer / GeneralSettings options all show versioned names (e.g. "Claude Sonnet 5") from the bundled defaults.
- Once a real id is observed, it overrides the bundled default and persists across app restarts (per machine).
- Sprite chips / send badge behavior unchanged except they may also use the bundled default before first capture (short form "Sonnet 5").
- No regression in T-334/335/338 behavior.

## Outcome

`packages/gui/src/store/poModel.ts`에 `BUNDLED_DEFAULT_ID: Record<PoModel,string>` 추가 (opus→`claude-opus-4-8`, sonnet→`claude-sonnet-5`, fable→`claude-fable-5` — model-prices.json 최신 항목 + 이 앱이 실제로 구동 중인 sonnet-5을 근거로 선택). `poModelOptionLabel`(wide, 스위처 모달/FreshComposer/GeneralSettings 공용)과 `poModelLabel`(short, PO 스프라이트 칩/배지)의 미관측 폴백을 bare alias → `BUNDLED_DEFAULT_ID`로 교체 — `observedByAlias`가 있으면 여전히 우선(변경 없음). `usePoModel` 스토어를 zustand `persist` + `createJSONStorage(() => localStorage)`로 감싸고 `partialize`로 `observedByAlias`만 `pdt:po-model-observed` 키에 저장(프로젝트 스코프 없음 — 머신 레벨 사실이라는 기존 T-338 의도와 일치); `model`/`supported`/`realModelId`/`loadedProjectDir`는 세션·프로젝트 스코프 유지로 persist 대상에서 제외해 T-335 "재시작 시 unknown" 보장을 깨지 않음. 스위처/FreshComposer/GeneralSettings 세 소비 지점은 이미 `poModelOptionLabel`을 호출하므로 코드 변경 없이 자동으로 버전 표시 폴백을 얻음 — 각 위치의 스테일 주석만 T-342 반영으로 갱신.

테스트(`poModel.test.ts`): 기존 "미관측 → 무버전" 기대값 3건을 "미관측 → BUNDLED_DEFAULT_ID" 기대값으로 갱신 + 신규 케이스(BUNDLED_DEFAULT_ID 전체 매핑, 관측이 번들 기본값을 덮어씀) 추가. 디스크 persist 메커니즘 자체는 zustand `persist`(기존 `useSurfacedArtifacts.ts`와 동일 패턴, 검증된 라이브러리)에 위임 — vitest 환경이 `node`(jsdom 아님)라 `zustand/middleware`는 전역 스텁으로 무력화되어 있고, 이 파일만 unmock하면 `localStorage` 부재로 깨지므로 별도 통합 테스트는 만들지 않음(순수 함수 폴백 로직만 유닛 테스트, persist 배선은 검증된 미들웨어에 위임).

검증: `pnpm exec tsc --noEmit` clean, `pnpm run lint` clean (로케일 키/보호 토큰 OK), `pnpm test` — 21 files / 64 tests 전부 green.

files_written:
- packages/gui/src/store/poModel.ts
- packages/gui/src/store/poModel.test.ts
- packages/gui/src/components/workspace/PoModelSwitchModal.tsx (주석만)
- packages/gui/src/components/FreshComposer.tsx (주석만)
- packages/gui/src/components/workspace/GeneralSettings.tsx (주석만)
