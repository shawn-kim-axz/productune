---
id: T-289
slug: adapter-a6-onboarding
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
closed: 2026-07-04
---
## Request
A6 (T2): prdt용 온보딩 경로 신설 — installClaudeHooks의 hook 18개 하드코딩(PDT_BASENAMES) 대신 prdt는 **hook 3종 + statusline-prdt.sh** 설치. 기존 목록은 legacy 분기 유지. `~/.prdt/prdt.env` 경로 교체 (productune.env 계승 확정안).

## Acceptance
- prdt onboarding installs exactly the 3 prdt hooks + statusline-prdt.sh
- Legacy onboarding path unchanged
- po-runner reads ~/.prdt/prdt.env for prdt projects

## Outcome
- installClaudeHooks dual-mode: prdt 분기는 hook 3종+statusline-prdt.sh만, settings.json 형태는 prdt-install.sh §4/§6과 deep-equal 동일(CLI-parity 테스트) — ~/.prdt 미러 경로 지정, 미러 부재 시 warn 스킵, idempotent. legacy 분기 바이트 동일.
- po-runner canSpawnClaude: prdt 프로젝트는 ~/.prdt/prdt.env 존재 게이트(legacy productune.env 불변, 원래 presence-check만이라 필드 매핑 불요).
- 게이트: vitest 9/9(신규 11케이스), tsc+build+lint green — 전부 mkdtemp 픽스처 HOME, 실기기 무접촉.
- 미해결→분리: prdt 분기가 live wizard에서 도달 불가(전역 온보딩에 projectDir 없음) — 호출부 배선은 T-305 발행. statusline 자동등록 vs CLI opt-in 드리프트는 코스메틱(inbox 기록).
- QA 생략 판단: 도달 불가 분기라 런타임 리스크 0 + CLI-parity/idempotency 테스트 증적.
