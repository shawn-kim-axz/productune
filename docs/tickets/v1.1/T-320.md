---
id: T-320
slug: launcher-migrated-project-regression
type: qa
status: done
assignee: qa
feature: gui-adapter
deps: [T-306]
created: 2026-07-07
closed: 2026-07-07
---

## Request

런처(HomeView populated launcher)에서 **마이그레이션된 `.prdt` 프로젝트가 정상 카드로 뜨는지** 회귀 커버리지가 비어 있다. Ship-entry 관측 중 발견: main 브랜치 구코드에서 paepyeong이 "폴더 없음"으로 떴고, v1 코드에선 정상일 것으로 추정되나 **실제 live 관측 증거가 없다**.

핵심 갭: `recents:listWithMeta`의 `exists` 판정은 `configPath(projectDir)` → `.prdt/config.json` 존재 여부로 계산되는데, 이 코드 패스(open-감지 경로와 별개)에 대한 assertion이 없다. `.prdt` 프로젝트 + 옛 `.productune`이 `.productune.migrated`로 리네임된 상태에서 카드가 `exists:true`로 떠야 한다.

## Acceptance

- Regression test proving `recents:listWithMeta` returns `exists:true`, correct `version`/`stage`, and non-missing footer for a `.prdt` migrated project fixture (a projectDir containing `.prdt/config.json` + `.prdt/po-state.json` and a sibling `.productune.migrated/`, i.e. no live `.productune/`).
- The test must fail against the pre-`.prdt` (legacy-only `.productune/config.json`) resolution to prove it actually guards the regression.
- Live observation (playwright-electron per fact--gui-testing-env: seed a `.prdt` recents entry, reload) that the populated launcher renders the migrated project card WITHOUT "폴더 없음", with its version chip / stage badge.
- Build green · lint clean · typecheck clean · test green.

## Outcome

- **Gap closed.** `recents:listWithMeta`의 `.prdt` 마이그레이션 프로젝트 exists 판정에 커밋된 회귀 테스트가 landed.
- **Dev**: 핸들러 바디를 `buildRecentsWithMeta(homeDir=os.homedir())`로 추출(무동작-변경 refactor, onboarding.ts DI idiom 재사용) → `project.test.ts`에 `.prdt` 마이그레이션 fixture(config+po-state, sibling `.productune.migrated/`, live `.productune/` 없음) 회귀 케이스 2건: (1) exists:true/version:v1.1/stage:build/slug assert, (2) legacy-only 해석 exists:false discrimination assert.
- **QA (grill)**: `detectProjectKind`를 legacy-only로 강제 → 테스트 fail 재현 후 원복(회귀 포착 실증). `git diff`로 추출 refactor byte-identical(런타임 불변) 확인. 앞선 live 관측(playwright-electron)에서 마이그레이션 카드가 "폴더 없음" 없이 version chip + Build badge로 정상 렌더 확인.
- build green · lint clean · typecheck clean · test green (16 files / 26 tests).
- files: `packages/gui/electron/ipc/project.ts`, `packages/gui/electron/ipc/project.test.ts`.
