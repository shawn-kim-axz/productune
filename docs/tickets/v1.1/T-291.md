---
id: T-291
slug: adapter-a8-t3-hide
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-284, T-287]
created: 2026-07-03
closed: 2026-07-04
---
## Request
A8 (T3, 제도 소멸 UI 숨김/대체 — prdt 프로젝트에서만): gate(PendingGateChip·PhaseTransitionGate·GateMarker·`phase:approve` IPC) · promotion drain(IPC 6종 포함) · versions[] 배열 뷰(→단일 버전 + 위키 retro--v* 나열) · PRD 스냅샷 뷰(→retro--v<N>.md 표시 or 숨김) · artifacts manifest pane(→디렉토리 리스팅 fallback, design.ts walk 재사용) · mechanical-write IPC 무효화 · TYPE_TO_PHASE 제거/교체.

A4(T-287)에서 이관 (2026-07-03): ① 단계 표시 컴포넌트 배선 — store `stageDisplay`(데이터 레이어 완성)를 PhaseBreadcrumb/WorkspaceShell/SidePanelCurrentVersion 계열에 페인팅, gate/versions 숨김과 같은 컴포넌트라 동시 처리 ② EntryGate가 `phase_history.length` 를 작업 흔적 신호로 사용 — prdt po-state엔 없어 resume 시 FreshComposer 오라우팅 가능성, prdt 분기 필요.

## Acceptance
- prdt project: no gate/promotion/versions-array/PRD-snapshot/manifest UI rendered; no dead IPC fired
- Legacy project: all of the above unchanged
- Artifacts pane falls back to directory listing for prdt projects

## Outcome
- prdt 분기(isPrdtPoState/detectProjectKind 재사용, 신규 감지 0): 4-stage 컬러 breadcrumb 배선(A4 이관 완결), PendingPromotionDrain·PendingGateChip 억제(promotion/mechanical-write IPC 사멸), versions[] UI→단일 버전 힌트, PrdSection→master PRD.md 고정, artifacts:listTree 디렉토리 리스팅 fallback(design walk 재사용, manifest.json 제외), EntryGate prdt 작업 흔적(version/current_task/stage) 분기.
- QA 라이브 1차(playwright _electron 실구동): acceptance 5항 GREEN + grill이 WelcomePanel legacy 5-step 누수 발견 → dev fix(MainPanel prdt 게이트 + WelcomePanel prdt variant) → QA 재검증 GREEN(스크린샷·IPC 감사 0건·유령 파일 0·legacy 불변, confidence 0.93).
- 이월: version 필드 브리지(내비 복원)는 T-306 — graceful 저하 소비처 8군데 목록 inbox 적재. `current_version==null` predicate는 prdt false-positive 패턴으로 기록.
