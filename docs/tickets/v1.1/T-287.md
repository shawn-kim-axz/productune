---
id: T-287
slug: adapter-a4-stage-mapping
type: impl
status: done
assignee: developer
feature: gui-adapter
deps: [T-284]
created: 2026-07-03
closed: 2026-07-03
---
## Request
A4 (T2): po-state `stage`(define/build/ship/retro) 4종 표시 매핑 추가 — phase 1..5 코드(PHASE_NAMES·PHASE_DEFS·EntryGate·phase-mapping·workspace store)는 legacy 분기에 격리. locale에 stage 4종 표시 문자열 추가 (gate/phase 키는 방치 가능).

## Acceptance
- prdt project: stage string renders with its own display definition (name/color)
- Legacy 5-phase rendering untouched for legacy projects
- No crash when po-state has 4-field prdt shape

## Outcome
- 데이터 레이어 완성: phase-mapping.ts STAGE_DEFS(4종 name/color)·isPrdtPoState(typeof stage 판별, IPC 신설 없음)·getActiveStageDef + types Stage/CurrentTask.assignee + store `stageDisplay` additive 파생 + locale en/ko 4키 + 테스트 17 assertions.
- 컴포넌트 페인팅은 gate/versions 숨김(T3)과 같은 컴포넌트에 얽혀 **T-291(A8)로 이관** — acceptance 1항은 mapping-resolve 수준으로 충족, 시각 배선은 A8에서 완결. EntryGate phase_history 오라우팅 가능성도 T-291에 기록.
- 'View PO Log' renderer kind 스레딩은 별도 T-304 발행. QA smoke PASS(실물 픽스처 양쪽, legacy 바이트 동일, 게이트 green).
