---
id: T-362
slug: productune-repo-meta-split-dogfood
type: ops
status: open
assignee: po
feature: meta-split
deps: [T-366]
created: 2026-07-16
---

## Request

**Dogfood (shawn, 2026-07-16)**: T-361 방식대로 **productune repo 자체를 분리 적용** — `github.com/shawn-kim-axz/productune`(origin)에는 실행 코드만 남기고, `docs/`·`.prdt/` 등 메타는 메타 전용 로컬 git으로 이관.

- 의존: T-361 (분리 방식·경계 확정 후 진행).
- **히스토리 처리 (shawn 결정, 2026-07-16)**: (a) 추적 제거만 — rewrite 없음. 모든 repo를 새 private org(다른 계정)로 이관 예정이라 기존 공개 repo의 과거 메타 노출은 감수.
- **이관 계획 (shawn, 2026-07-16)**: v1.2 ship 시 기존 origin에 최종 1회 push → 새 org의 새 repo로 origin 이전. 이 패턴이 다른 prdt 관리 repo migration의 표준.

## Acceptance

- origin의 `git ls-files`에 메타 파일이 없다.
- 메타는 로컬 메타 git에서 기존 히스토리(또는 이관 시점 스냅샷) 포함 관리된다.
- 이관 후 prdt CLI·GUI가 productune 프로젝트에서 정상 동작한다.
