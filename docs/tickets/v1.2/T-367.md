---
id: T-367
slug: meta-history-surfaces
type: impl
status: done
assignee: developer
feature: meta-split
deps: [T-364]
created: 2026-07-16
closed: 2026-07-16
---

## Request

메타 히스토리 열람 surface — CLI `prdt meta log` + GUI Version History 메타 트랙 (PRD §CLI·GUI parity).

- 둘 다 core의 히스토리 조회 API만 호출 (surface에 git 로직 금지)
- 원격 backup opt-in 설정 노출 (core remote add API — 수동 push 안내까지만)
- **autosave wiring 편입 (PO, T-364 QA 관찰)**: `commitMeta`를 실제 autosave 라이프사이클 beat에 거는 연결 — CLI·GUI 양 surface가 같은 신호에서 호출해야 parity 성립. 현재 core에 API만 있고 호출처 없음.

## Acceptance

- `prdt meta log` and the GUI history view show the same meta commit timeline from the same core API.
- Remote backup can be configured from either surface; no automatic push occurs.
