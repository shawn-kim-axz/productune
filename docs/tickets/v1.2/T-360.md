---
id: T-360
slug: gui-remove-artifacts-open-all
type: impl
status: done
assignee: developer
feature: gui
created: 2026-07-15
closed: 2026-07-15
---

## Request

**GUI UX (shawn, 2026-07-15)**: 아티팩트 사이드패널의 **"+ open all (N)" 버튼 제거** — "함정" 버튼. 아티팩트가 쌓인 상태에서 누르면 N개가 한꺼번에 열려 사고 유발, 실용 가치 없음.

- 위치: `packages/gui/src/components/workspace/SidePanelArtifacts.tsx` — `openAll` 행(버튼 + hover state + `openAllRow` 스타일 + i18n 키 `workspace.artifacts.openAll`).
- 개별 아티팩트 열기는 그대로 유지 — 제거 대상은 일괄 열기 진입점만.

## Acceptance

- "open all" row no longer renders in the artifacts side panel; per-item open unaffected.
- Dead code removed (hover state, style const, i18n key if unused elsewhere).
- Build green, GUI smoke on the artifacts panel passes.

## Outcome

- Removed the "+ open all (N)" row from `SidePanelArtifacts.tsx` (JSX block, `openAllHover` state, `openAllRow` style const). Also removed `AUTO_OPEN_LIMIT`/`hasOverflow`, which existed only to gate that row and became dead once it was gone. Per-item `ArtifactRow` open path untouched.
- Removed i18n key `workspace.artifacts.openAll` from `en.json`/`ko.json` — confirmed via grep it had no other callers.
- Verified: `pnpm run build` (locale-key check, locale-protected check, `tsc --noEmit`, `vite build`) green. `pnpm run smoke` — 2/3 pass; the 3rd (onboarding CTA strict-mode violation) reproduces identically on `main` before this change (stash/pop diff), so it's a pre-existing environment issue (multiple existing project entries in the picker), not a regression from this ticket.
- Not committed per this-device policy — commit only on shawn's explicit request.
