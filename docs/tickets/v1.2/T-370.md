---
id: T-370
slug: meta-split-review-correctness-fixes
type: impl
status: done
assignee: developer
feature: meta-split
created: 2026-07-16
closed: 2026-07-16
---

## Request

Ship-entry 누적 code-review(2026-07-16)의 correctness 발견 5건 패치 — C1·C2는 배포 차단.

- **C1 (HIGH)**: legacy `.productune` 프로젝트에서 마이그레이션 진입 가능 + beat 경로 불일치 → 분리 후 작업 기록이 코드 repo에도 meta repo에도 저장 안 됨 (데이터 보호 실패). GUI Meta 3개 섹션(`GeneralSettings.tsx:82-93` gate)을 prdt 프로젝트로 제한(`isPrdtPoState` 패턴) — hook은 `.prdt/meta.git` 하드코딩(`prdt-post-dispatch.sh:74`) 유지.
- **C2 (MED)**: `runMetaMigration`(meta-migrate.ts:236-249) 코드 repo 커밋 실패 시 staged 상태(managed .gitignore add + 전체 `rm --cached`) 잔류 + 재진입 plan이 `already-split` 오분류(:170) → untrack 커밋 영구 미생성, 사용자 다음 커밋에 대량 삭제 혼입. 커밋 실패 catch에서 unstage 롤백(`git reset -- …`) 후 fail 반환.
- **C3 (LOW)**: `meta-cli.ts:43` usage 문자열에 `migrate-plan|migrate-run` 누락.
- **C4 (LOW)**: `MetaMigrateSection.tsx:138` — run 시점 refusal 원시 토큰(`staged-changes` 등)이 배너에 노출 → 번역 매핑.
- **C5 (LOW)**: `meta-migrate.ts:271-272` vs 205-209 — 추적 meta 파일이 work-tree에서 전멸한 mixed repo에서 성공한 untrack이 "verification mismatch"로 보고되는 모순 — 판정/메시지 정합.

## Acceptance

- C1: migration/backup/track sections unreachable on legacy `.productune` projects; regression test or observed gate behavior.
- C2: commit failure leaves the code repo index clean (no staged untrack residue) and a re-run resumes to completion; regression test with a failing-commit fixture.
- C3–C5 fixed; core+gui suites, build, tsc green.

## Outcome

5건 전부 패치, 커밋 `40f9f82` (developer, 2026-07-16).

- C1: `GeneralSettings.tsx`(백업+분리)와 `HistoryPane.tsx`(타임라인)를 `isPrdtPoState`로 게이트 — 다른 prdt-only 표면과 동일 신호. 회귀 테스트 `GeneralSettings.metaGate.test.tsx` (정적 렌더 + 자식 sentinel mock — 자식의 IPC self-hide가 게이트 부재를 가리지 못하게): legacy/null po-state 숨김 · prdt 노출 3케이스.
- C2: `runMetaMigration` 커밋·스테이징 실패 catch에서 `git reset -q -- .gitignore <allowlist>` scoped 롤백 (plan이 pre-staged 사용자 변경을 거부하므로 staged 전부가 마이그레이션 소유 — 안전). failing pre-commit fixture 회귀 테스트: 실패 후 index clean·meta 재추적·plan `eligible`(already-split 오분류 소멸)·hook 제거 후 재실행 완주 verified.
- C3: usage 문자열에 `migrate-plan|migrate-run` 추가.
- C4: run refusal 토큰 → 번역 매핑 (`staged-changes`는 기존 `stagedBlocked` 재사용, `refusalAlreadySplit`/`refusalNoGit` 신규 키 en·ko).
- C5: 검증 판정을 "meta 추적 0 = work-tree에 meta 파일이 실존할 때만 mismatch"로 정합 (`ls-files --others` + info/exclude — commitMeta staging과 동일 exclude 처리), 메시지는 실제 실패 조건만 열거. 전멸 mixed repo 회귀 테스트 추가.

증빙: core 134 · gui 128 tests green, core build(tsc+esbuild)·gui build(locale check+tsc+vite) green. 추가로 배포 번들 `meta-cli.cjs`를 스크래치 mixed repo에 직접 구동해 C2 롤백→재개 완주·C3 usage를 실행 관찰로 확인.
