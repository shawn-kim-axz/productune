---
id: T-359
slug: gui-anchor-ds-adoption
type: design
status: open
assignee: designer
feature: gui
created: 2026-07-15
---

## Request

**v1.2 트랙 (shawn 결정, 2026-07-15) → v1.3 이월 (shawn 결정, 2026-07-16 — v1.2는 code/meta 분리 트랙으로 전환)**: productune GUI에 **Anchor Design System(english variant)** 적용 — 기존 커스텀 다크 테마를 Anchor 토큰 기반으로 리스킨.

- **소스**: `/Users/shawn.axz-pc/Documents/dev/ntf-products/_design/english/` — `DESIGN.md`(v0.1 사용 규칙) + `tokens.json`(atomic + semantic light/dark 192개) + `Button.md`(대표 컴포넌트 스펙)
- **동기**: 션님 프로젝트 대부분이 prdt로 돌아가는 상황 — 매일 쓰는 GUI의 시각 품질/일관성 업리프트. Light/Dark 자동 스왑 확보 부수 효과 (기존 backlog a11y 건 `--text-faint` 대비 미달도 자연 해소 기대).
- **적용 원칙**: Anchor는 axz 플랫폼용 DS — **Foundation(color·typography·radius·shadow·spacing) + Button 계약을 차용**하되, category color 등 도메인 토큰은 무관하므로 제외. Semantic 토큰만 사용, Primitive 직접 참조·인라인 HEX 금지 (DESIGN.md §7).
- **갭 주의 (DESIGN.md §10)**: Layout·Grid·Responsive·Button 외 컴포넌트 매트릭스는 v0.1 미정의 — 갭 영역은 Anchor 규칙을 발명하지 말고 **productune 자체 결정**으로 채우고 design.md에 그 경계를 명시.
- 상세 스코프(적용 surface 범위, 단계 분할)는 v1.2 PRD에서 확정.

## Acceptance

- `docs/design.md` updated: Anchor token adoption scope, mapping strategy (existing CSS vars → semantic tokens), gap-fill decisions with Anchor/productune boundary marked.
- GUI renders on Anchor semantic tokens — no inline HEX, no primitive refs in product styles.
- Light/Dark both render correctly via token swap.
- Button-class controls conform to Button.md valid-combination table.

## Outcome
