---
id: T-307
slug: gui-stale-question-signal
type: impl
status: open
assignee: developer
feature: gui-adapter
created: 2026-07-04
---
## Request
T-292 QA 발견: PO 세션의 raw AskUserQuestion tool_use에서 CLI가 내부 60초 무응답 후 "답 없이 진행"해도 GUI는 감지·표출하지 못한다 — oqPending이 silence/stall 타임아웃을 턴 종료까지 무력화하고, actionable-메시지 감지망(toolErrorInfo)은 is_error 패턴만 잡음. 카드는 계속 클릭 가능하지만 의미상 stale — 지각 답변이 --resume으로 들어가면 모델이 미해결 질문에 바인딩 못 하고 새 지시로 오처리할 위험. prdt는 PO habit(텍스트 질문 원칙)으로 원천 차단되어 있어 우선순위 낮음.

수정 방향(QA 제안): oqPending 윈도우 중 stdout 재개 첫 이벤트를 감지해 system 메시지 표출, 또는 oqPending 해제 시 카드를 'expired' 상태로 patch. 착수 시 실측 선행: CLI의 무응답 합성 tool_result 타이밍/형태 캡처(T-292 Outcome의 재현 절차 참조).

## Acceptance
- When the CLI proceeds without an answer, the GUI surfaces an explicit signal (system message or expired card state) — no semantically dead question left clickable as-is
- Live repro captured once (synthetic tool_result timing/shape documented)

## Outcome
