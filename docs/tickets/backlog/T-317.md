---
id: T-317
slug: review-simplify-batch
type: impl
status: open
assignee: developer
created: 2026-07-06
---
## Request
v1.1 누적 code-review의 reuse/simplify 8건 (전부 비차단, 동작 변화 없음 — tidy 라운드):
1. project-kind 감지 이중 구현 — gui가 core를 re-export (역방향 import는 이미 열려 있음)
2. legacy/prdt 필드 coalescing 3중 구현 → resolveStage/resolveVersion 헬퍼 단일화
3. UsageBar fmtCost/usePrdtCost가 CostArchivePanel 복제 → 공유 훅
4. model/effort allowlist main/renderer 사본 → shared 상수 or IPC 전달
5. PrdtHookInstallBanner가 SessionHealthBanner info-variant 수작업 미러 → 공용 Banner 컴포넌트
6. dead derived state stageDisplay/deriveStageDisplay 삭제 (읽는 곳 없음)
7. WorkspaceShell displayVersion 분기 불요 (브리지가 이미 미러)
8. detectProjectKind 무캐시 반복 existsSync·turns.jsonl 3중 재집계 — per-projectDir memo (효율 LOW)

## Acceptance
- Each item applied or waived with reason; behavior-preserving (refactor commits separate); gates green
## Outcome
