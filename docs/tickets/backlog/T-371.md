---
id: T-371
slug: meta-split-reuse-simplify
type: impl
status: open
assignee: developer
feature: meta-split
created: 2026-07-16
---

## Request

v1.2 누적 code-review의 reuse/simplify 발견분 (배포 비차단, 백로그):

- B1: `scrubbedGitEnv`·private `metaGit` 러너 TS 2중 구현(meta-git.ts/meta-migrate.ts) — 병렬 개발 종료로 사본 사유 소멸, 통합. (python 사본은 parity 테스트 고정이라 유지)
- B2: MetaBackupSection/MetaMigrateSection 스타일 상수 byte-identical 중복 — 공용 추출
- B3: 대규모 repo 견고성 — `add -f -- <files>` argv ARG_MAX + metaGit timeout 10s vs 수천 파일 첫 스냅샷 → `--pathspec-from-file=-` 또는 chunking
- B4: hook 이중 발화(PostToolUse:Agent + SubagentStop)로 dispatch당 tick 2회 — 레이스는 자가치유 확인, 낭비만 → debounce
- B5: `useVersionHistory.ts:93-103` 프로젝트 전환 시 이전 metaCommitsRaw 잔상 — effect 진입 시 clear 한 줄
- B6: python `_meta_stage_and_commit` 최종 commit rc 미확인 — 실패 스냅샷도 `meta_git:"ok"` 보고 (첫 beat self-heal이라 낮음)
- B7 (QA 관찰, T-370): `meta:migrateRun` IPC 핸들러가 렌더 게이트에만 의존 — defense-in-depth로 핸들러 수준 legacy 거부 추가 검토

## Acceptance

- Each item resolved or explicitly re-judged; suites/build green.
