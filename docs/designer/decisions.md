# pdt-designer project decisions

> Non-trivial design decisions, one dated line each. PO appends on user approval.

- (2026-05-04) phase4-gui-full-cycle: planner/developer mode 분기 제거 → 단일 GUI 모드. PRD Phase 4 명칭/AC/OQ 갱신. service-flow A1 "Welcome / 모드 선택" → "First-run wizard (Engine / Wiki / API Key)" 재정의.
- (2026-05-04) workspace-shell: chat-in-center → chat-on-right. 4-column grid 48/240/1fr/360. ActivityBar VSCode 패턴 (좌 48px, 아이콘 3종). PO chat 360px 우측 상시 고정. Persona panel 위치 TBD (Slice 5 재방문).
- (2026-05-06) GUI PO 세션 단일화: 멀티 채팅방 X → 프로젝트당 단일 PO 세션. 스토리지 = `<projectDir>/.productune/chat.json` 단일 파일. T-P4-042 deprecated, T-P4-041 이 단일 세션 흡수. CLI/non-GUI 는 multi-session 가능성 보존.
- (2026-05-07) productune GUI design system 정식 spec land (`docs/designer/design-system.md`). shadcn-style token (color/spacing/typo/radius/elev/icon/motion). lucide-react@1.14.0 default. brand orange = persona-po alias 의도 정합. dark-only first, light theme = Phase 5.
- (2026-05-07) doctrine prose convention: persona/sections .md = English-only + caveman-lite. persona/stage/Phase/status enum verbatim (T-P4-057 linter 정합).
- (2026-05-07) delegation chunking ceilings: designer 1~2 산출물/1~3 결정/1 sub-area/5-10min; developer 1 ticket; qa 1 ticket. T-P4-065 reject dogfood 후 박음, hard rule 아닌 guideline.
- (2026-05-08) T-P4-065 통합: Phase 5단 (PRD/Design/Build/Deploy/Close) + ticket stage→type rename + ChatPanel selector 제거 + po-state slim + PRD/flow/mockup 정정 — 6 sub-area plan + 통합 ticket. 사용자 가시 = doctrine 어휘 동일 → axis 충돌 해소. ticket type axis (6 enum 유지) 분리. po-state past_tickets[] 통째 제거 + ticket md = SoT. PhaseStrip default 1 dot + hover expand 5 dot. 색 5 hex (deploy #FB923C / close #34D399). schema_version 1→2 통합 jq idempotent.
- (2026-05-15) T-P4-119 — GUI IPC subscription uplift: ChatPanel `useEffect` 등록 대신 `packages/gui/src/store/poEvents.ts` 모듈 top-level 1회 register. JS load 시점 listener bind 보장 → component lifecycle 종속 race 영구 차단. StrictMode/HMR 가드 = 모듈 스코프 `let registered = false` flag + `import.meta.hot?.dispose` offFns 일괄 해제. `inFlightMsgId`/`inFlightKind` 는 useRef → `useWorkspace` store state 승격해 module-level handler 가 `getState/setState` 로 접근. 향후 IPC subscription 은 컴포넌트 useEffect 가 아닌 `store/*` 모듈 top-level 에서 등록 패턴.
- (2026-05-15) T-P4-120 — Alternative-reporting protocol GA. PO/designer/developer/qa 의 N≥2 alternative 보고는 mandatory format: 각 옵션마다 Pros + Cons 두 블록 + anchor citation `[<axis>]` 또는 `[<axis> · <level>]` (anchor source = `~/.productune/po-memory.md ## User knowledge state`). 추천 라인도 anchor citation 의무. Vague-descriptor blacklist: `cleanest/simpler/easier/more-elegant/cleaner/nicer/better-without-axis` standalone 금지 — permitted-when-anchored 예외만. 5 file 동시 adoption (`po-instructions.md` Files/Hard rules/Step 1 + `sections/po-loop.md` Step 1.1/Step 1 #6/Step 3 #14c + `sections/alternative-reporting.md` 신규 + `sections/delegation.md` `[ctx].user_knowledge_state` schema + `po-memory.md` UKS section normative). repo mirror (`packages/core/po/**`) sync 는 별도 turn.
- (2026-05-18) T-P4-121 — PO mechanical wiki write doctrine landed. persona 측 `mcp__graphiti__*` 4 도구 (write + read 모두) 일괄 제거 + agent frontmatter `mcpServers:` graphiti 항목 제거. wiki write 유일 경로 = PO 가 `claude --print` (no `--agent`) subprocess 으로 `mcp__graphiti__add_memory` 직접 호출. `sections/memory.md` 라인 33+57 본문 직접 재작성 (cross-ref redirect 거부 — single source of truth 유지). 4-layer root cause (env / claude mcp add / hyphen / subagent non-inheritance) 는 `docs/designer/R1-po-mechanical-wiki-write.md` 에 work-note 로 보존 예정. doctrine prose + agent config 정합 lock — 향후 MCP availability 디버깅 시 anchor.
- (2026-05-19) T-P4-126 — doctrine MD optimization landed (hard cutover). 13 main files all ≤86 non-empty lines (was up to 261), 17 sub-files in `sections/_formats/` + `sections/_details/`. bootstrap-doctrine.sh + init.ts recursive sweep — sub-files seed on install. Korean→English (verbatim user-phrase 2 line preserved). Token reduction ~30–40% per Step 1.1 turn-start. doctrine layout SoT — future reorg tickets reference this pattern.
- (2026-05-19) T-P4-138 — ticket status i18n key 패턴: `workspace.tickets.status.<status>` + defaultValue: status fallback. TicketDashboardView + VersionHistoryView 단일 소스. 향후 enum i18n(`type`, `priority`, `qa_status`) 추가 시 동일 namespace 패턴 (`workspace.tickets.<field>.<value>`) 따를 것 — namespace 분기 / pluralization key / 한·영 평등 keyset 일관성 강제.
- (2026-05-19) T-P4-140 — MCP config tier mapping (4 tier):
  · **local** = `~/.claude.json` → `projects[<projectDir>].mcpServers` (`claude mcp add` 기본 scope)
  · **project** = `<projectDir>/.mcp.json` (git-shared, 팀 공유)
  · **user** = `~/.claude.json` 최상단 `mcpServers` (글로벌 — user의 모든 프로젝트)
  · **productune** = `~/.claude/settings.json` → `mcpServers` (productune hooks 파일 reuse — deprecated 권장)
  GUI `mcp:getServers` IPC reads 3 tier (local + project + productune); user tier 무시 (onboarding에서 거의 미사용). GUI `mcp:save` → local tier (`~/.claude.json projects[<dir>]`) 로 통일. UI 행에 source 라벨 (`[local]/[project]/[productune]`) 표시. 향후 MCP IPC 추가 / 디버깅 시 file-location 정합 anchor.
- (2026-05-19) T-P4-140 — TeamPanel sidebar 패턴: **nav-row pattern unified**. 모든 sidebar 섹션 = 단일 nav-row 버튼 (클릭 → main panel openTab 으로 detail view 열기). Inline list / collapsible 섹션 (T-P4-130 MCP 식) 금지. Sidebar = nav only, detail = main panel tab. T-P4-099 확립 → T-P4-130 일시 위반 → T-P4-140 재확립.
- (2026-05-20) T-P4-141 — Persona Activity row 작성 책임: **PO 단독 enforcement**. pdt-developer/pdt-qa 6 variant에 "DO NOT write Persona Activity rows yourself" section 명시 + tickets.md L97 강조. persona는 JSON `notes` 에 action+result ≤80자 반환 → PO가 변환·append. designer variants 는 ticket 생성 시 초기 row 자가-write 유효 (변경 없음).
- (2026-05-20) T-P4-142 — Over-cap (100-line) compression pattern: 트리비얼 위반(1-2줄)은 blank-line 제거로 해결. 본격 위반은 self-contained 섹션 (Timeline / Output shape / Ticket-ID allocation 등 reference 성격) 을 `_details/` 또는 `_formats/` sub-file 로 추출. cap 본문 prose 자체 깎기 금지 — 내용 손상 위험.
