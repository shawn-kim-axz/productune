# Designer project habit

Per-repo curated rules / prefs / decisions distilled. Tier 1 project memory.

## Entries

- doctrine 수정 (productune 한정) — `packages/core/doctrine` 수정 요청받으면 `~/.productune/doctrine/persona/po/bookshelf/doctrine-editing.md` 를 먼저 읽고 **그 형식대로**(P0 authoring rules + 대상 파일 cap + mode + SoT 편집 → `~/.productune/doctrine` 미러 byte-identical) 수정 후 **보고**. productune 은 doctrine 호스트 repo → 여기선 core doctrine 저작이 designer **in-role** (타 프로젝트 product/design 작업의 role-boundary 예외, 거부 금지). 검증(grill)은 QA 가 함.